---
name: AI Video Avatar Meeting (ReuniÃµes Conduzidas por IA de VÃ­deo)
skill_id: 86
description: Protocolo para conduzir reuniÃµes de apresentaÃ§Ã£o, onboarding e vendas usando avatar de IA de vÃ­deo que simula uma pessoa real â€” com voz, rosto e personalidade consistente, RAG das informaÃ§Ãµes do cliente para nÃ£o inventar nada, e integraÃ§Ã£o com Google Calendar + Notion para registro completo.
---

# AI Video Avatar Meeting Protocol ðŸŽ¥

**Objetivo:** "Cada reuniÃ£o de 40 minutos acontece sem vocÃª estar presente â€” e a qualidade Ã© melhor do que na maioria das reuniÃµes humanas."

## Por que Isso Muda o Jogo

O tempo de reuniÃ£o Ã© o gargalo de escala de qualquer negÃ³cio de serviÃ§os. Uma plataforma com 1.000 revendedoras nÃ£o consegue fazer reuniÃ£o de onboarding humana para todas. Com avatar de IA: escala infinita, disponibilidade 24/7, qualidade consistente, zero cansaÃ§o.

**Modelo de negÃ³cio adjacente:** Este sistema pode ser vendido como serviÃ§o avulso. Cada reuniÃ£o por IA = R$40. Com volume, renda passiva sobre renda passiva.

---

## 1. Stack TecnolÃ³gico DisponÃ­vel em 2026 ðŸ› ï¸

```
OPÃ‡Ã•ES DE AVATAR DE VÃDEO:

TIER 1 â€” Qualidade MÃ¡xima (pago):
â”œâ”€â”€ HeyGen: avatar realista + voz clonada + streaming
â”‚   â†’ API disponÃ­vel, integraÃ§Ã£o via n8n
â”‚   â†’ PreÃ§o: ~$59/mÃªs para 5h de vÃ­deo gerado
â”œâ”€â”€ Synthesia: mÃºltiplos avatares, 120+ idiomas
â”‚   â†’ Bom para apresentaÃ§Ãµes gravadas
â””â”€â”€ D-ID: streaming de vÃ­deo em tempo real (mais conversacional)
    â†’ API: api.d-id.com
    â†’ Suporta conversaÃ§Ã£o em tempo real com LLM

TIER 2 â€” Open Source (self-hosted no VPS):
â”œâ”€â”€ SadTalker: animar foto com Ã¡udio
â”œâ”€â”€ MuseTalk: lip-sync em tempo real
â””â”€â”€ LivePortrait: animaÃ§Ã£o de retrato de alta qualidade

TIER 3 â€” SoluÃ§Ã£o HÃ­brida (RECOMENDADA):
â†’ Avatar prÃ©-gravado para introduÃ§Ãµes e mÃ³dulos fixos
â†’ Componente conversacional em tempo real para perguntas
â†’ Voz clonada com ElevenLabs para consistÃªncia
```

---

## 2. Arquitetura da ReuniÃ£o por IA ðŸ—ï¸

```
PRÃ‰-REUNIÃƒO (automÃ¡tico, 30min antes):
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. Buscar dados da revendedora no Supabase â”‚
â”‚ 2. RAG: carregar contexto relevante        â”‚
â”‚    â†’ HistÃ³rico de conversas               â”‚
â”‚    â†’ Produtos de interesse                â”‚
â”‚    â†’ DÃºvidas anteriores                   â”‚
â”‚    â†’ Stage do onboarding                  â”‚
â”‚ 3. Preparar script personalizado           â”‚
â”‚ 4. Enviar link da reuniÃ£o via WhatsApp     â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜

DURANTE A REUNIÃƒO (D-ID + LLM em tempo real):
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ AVATAR FALA â†’ REVENDEDORA RESPONDE         â”‚
â”‚                                            â”‚
â”‚ [Ãudio da revendedora]                     â”‚
â”‚     â†’ [Whisper: transcriÃ§Ã£o]               â”‚
â”‚     â†’ [Claude: gerar resposta com RAG]     â”‚
â”‚     â†’ [ElevenLabs: voz clonada]            â”‚
â”‚     â†’ [D-ID: sincronizar lÃ¡bios no avatar] â”‚
â”‚     â†’ [Avatar responde em vÃ­deo]           â”‚
â”‚                                            â”‚
â”‚ LatÃªncia total: ~2-3 segundos              â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜

PÃ“S-REUNIÃƒO (automÃ¡tico, imediato):
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. Salvar transcriÃ§Ã£o completa no Supabase â”‚
â”‚ 2. Gerar resumo da reuniÃ£o                 â”‚
â”‚ 3. Identificar prÃ³ximos passos             â”‚
â”‚ 4. Enviar resumo por WhatsApp              â”‚
â”‚ 5. Criar tarefas no Notion                 â”‚
â”‚ 6. Marcar prÃ³xima reuniÃ£o se necessÃ¡rio    â”‚
â”‚ 7. Atualizar stage do onboarding           â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

---

## 3. O Sistema RAG da ReuniÃ£o (NÃ£o Inventa Nada) ðŸ§ 

```python
# Sistema de contexto para o avatar â€” zero alucinaÃ§Ã£o
class MeetingRAGSystem:
    
    def __init__(self, client_id: str):
        self.client_data = self.load_client_data(client_id)
        self.knowledge_base = self.load_knowledge_base()
        
    def load_client_data(self, client_id: str) -> dict:
        """Carrega TODOS os dados do cliente do Supabase"""
        return {
            "nome": ...,
            "historico_conversas": ...,  # Ãºltimas 30 conversas
            "produtos_interesse": ...,
            "pedidos_anteriores": ...,
            "duvidas_frequentes": ...,
            "stage_onboarding": ...,  # onde estÃ¡ no funil
            "observacoes_manuais": ...  # notas do responsÃ¡vel
        }
    
    def load_knowledge_base(self) -> VectorStore:
        """Base de conhecimento da empresa â€” embeddings no Supabase pgvector"""
        return load_vectors([
            "faq_plataforma.pdf",
            "catalogo_produtos.json",
            "politica_entregas.md",
            "contrato_modelo.pdf",
            "precos_e_margens.json",
            "casos_de_sucesso.md"
        ])
    
    def get_context_for_question(self, question: str) -> str:
        """Busca contexto relevante para responder sem inventar"""
        
        # Busca semÃ¢ntica na base de conhecimento
        relevant_docs = self.knowledge_base.similarity_search(
            question, k=3, score_threshold=0.7
        )
        
        # Contexto do cliente
        client_context = self.get_relevant_client_history(question)
        
        return f"""
        INFORMAÃ‡Ã•ES DO CLIENTE:
        {client_context}
        
        INFORMAÃ‡Ã•ES DA EMPRESA (use APENAS isso para responder):
        {format_docs(relevant_docs)}
        
        REGRA CRÃTICA: Se a resposta nÃ£o estÃ¡ no contexto acima, diga:
        "NÃ£o tenho essa informaÃ§Ã£o agora, mas verifico e te respondo em breve."
        NUNCA invente valores, prazos ou polÃ­ticas.
        """
    
    def get_meeting_script(self, meeting_type: str) -> str:
        """Script base da reuniÃ£o por tipo"""
        scripts = {
            "onboarding": ONBOARDING_SCRIPT,
            "apresentacao": APRESENTACAO_SCRIPT,
            "followup_30dias": FOLLOWUP_30_SCRIPT,
            "renovacao": RENOVACAO_SCRIPT
        }
        return personalize_script(
            scripts[meeting_type], 
            self.client_data
        )
```

---

## 4. ImplementaÃ§Ã£o via n8n + D-ID API âš™ï¸

```javascript
// n8n Workflow: ReuniÃ£o Conversacional em Tempo Real

// SETUP DA SESSÃƒO D-ID
async function initMeeting(clientData) {
    const didSession = await fetch('https://api.d-id.com/talks/streams', {
        method: 'POST',
        headers: {
            'Authorization': `Basic ${D_ID_KEY}`,
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            source_url: 'https://seu-servidor.com/avatar/foto_avatar.jpg',
            driver_url: 'bank://lively/driver-06',
            config: {
                stitch: true,
                fluent: true,
                pad_audio: 0.0
            }
        })
    });
    return didSession.json();
}

// PROCESSAR MENSAGEM DO USUÃRIO EM TEMPO REAL
async function processUserMessage(audioBlob, sessionId, ragContext) {
    
    // 1. Transcrever Ã¡udio
    const transcription = await transcribeAudio(audioBlob);  // Whisper API
    
    // 2. Buscar contexto RAG
    const context = await ragContext.get_context_for_question(transcription);
    
    // 3. Gerar resposta com Claude
    const response = await anthropic.messages.create({
        model: 'claude-sonnet-4-6',
        max_tokens: 200,  // Respostas curtas para reuniÃ£o
        system: `
            VocÃª Ã© Ana, assistente da [Nome da Empresa].
            Fale de forma natural, como em uma conversa.
            Mantenha respostas com menos de 3 frases.
            Use o contexto abaixo para responder.
            
            ${context}
        `,
        messages: [{ role: 'user', content: transcription }]
    });
    
    const textResponse = response.content[0].text;
    
    // 4. Converter para voz (ElevenLabs)
    const audioResponse = await generateVoice(textResponse, VOICE_ID);
    
    // 5. Enviar para D-ID sincronizar com avatar
    await fetch(`https://api.d-id.com/talks/streams/${sessionId}`, {
        method: 'POST',
        body: JSON.stringify({
            script: {
                type: 'audio',
                audio_url: audioResponse.url
            }
        })
    });
    
    // 6. Logar a troca de mensagens
    await supabase.from('meeting_transcript').insert({
        session_id: sessionId,
        human_message: transcription,
        ai_response: textResponse,
        timestamp: new Date().toISOString()
    });
}
```

---

## 5. Scripts de ReuniÃ£o por Tipo ðŸ“

### Script: Onboarding de Nova Revendedora (40 min)

```
[0-5 min] BOAS-VINDAS E QUEBRA-GELO
"OlÃ¡ [Nome]! Sou a Ana, assistente digital da [Empresa]. 
Que bom ter vocÃª conosco! Hoje vamos fazer seu onboarding 
completo na plataforma. Tudo bem por aÃ­?"

[5-15 min] APRESENTAÃ‡ÃƒO DA PLATAFORMA
â†’ Mostrar tela da plataforma (screen share via browser)
â†’ Guiar pela interface
â†’ Explicar cada funcionalidade

[15-25 min] COMO FAZER O PRIMEIRO PEDIDO
â†’ DemonstraÃ§Ã£o prÃ¡tica
â†’ Tirar dÃºvidas em tempo real

[25-35 min] COMO ENCONTRAR CLIENTES (o diferencial)
â†’ EstratÃ©gias de venda
â†’ Scripts de WhatsApp
â†’ Como usar o aplicativo da revendedora

[35-40 min] PRÃ“XIMOS PASSOS
â†’ "Quando planeja fazer seu primeiro pedido?"
â†’ Marcar check-in em 7 dias
â†’ Compartilhar recursos (links, PDF)
```

---

## 6. Modelo de NegÃ³cio: Vendendo ReuniÃµes por IA ðŸ’°

```
COMO MONETIZAR ESTA INFRAESTRUTURA:

1. SERVIÃ‡O DE REUNIÃƒO AVULSO:
   â†’ R$40 por reuniÃ£o de atÃ© 60 minutos
   â†’ Cliente envia: contexto, script, avatar desejado
   â†’ VocÃª entrega: link de reuniÃ£o com IA configurada
   â†’ Margem: ~85% (custo real ~R$6 em API + processamento)

2. PLANO MENSAL PARA EMPRESAS:
   â†’ R$500/mÃªs para atÃ© 20 reuniÃµes
   â†’ R$900/mÃªs para atÃ© 50 reuniÃµes
   â†’ Empresas de RH (entrevistas iniciais) sÃ£o grande mercado

3. INTEGRADO NA PLATAFORMA DE REVENDEDORAS:
   â†’ IncluÃ­do na mensalidade de R$1.500
   â†’ Diferencial vs concorrentes que fazem reuniÃ£o manual
   â†’ Revendedoras podem fazer reuniÃ£o com clientes delas usando o sistema
```

---

## Trigger desta Skill

- "Agendar reuniÃ£o de onboarding para [revendedora]"
- "Criar reuniÃ£o de apresentaÃ§Ã£o da plataforma"
- "Configurar avatar para reuniÃµes automÃ¡ticas"
- "Quero vender reuniÃµes por IA como serviÃ§o"
- Quando novo lead aprovado: agendar reuniÃ£o de onboarding
- Follow-up de 30 dias: reuniÃ£o de checagem automÃ¡tica
---