---
name: Internationalization & Localization (i18n/l10n â€” Escalar AlÃ©m do Brasil)
skill_id: 80
description: Protocolo completo para internacionalizar produtos digitais: arquitetura i18n desde o dia 1, frameworks de traduÃ§Ã£o (next-intl, i18next), localizaÃ§Ã£o cultural (datas, moedas, formatos), SEO multilÃ­ngue, e estratÃ©gia de entrada em mercados internacionais.
---

# Internationalization & Localization Protocol ðŸŒ

**Objetivo:** "Um produto feito sÃ³ em PT-BR Ã© uma empresa que decidiu que 96% do mundo nÃ£o importa."

## Por que Esta Era a Lacuna

Todo o Antigravity foi construÃ­do para o mercado brasileiro â€” n8n, Supabase, React, Python. Mas quando um produto cresce e o fundador quer entrar nos EUA, Europa ou Latam, descobre que retroativamente internacionalizar Ã© 5x mais custoso do que ter feito certo desde o inÃ­cio. Esta skill resolve isso.

---

## 1. Arquitetura i18n: DecisÃµes que Custam Caro se Erradas ðŸ—ï¸

```typescript
// ERRADO â€” string hardcoded no cÃ³digo:
<h1>Bem-vindo ao produto!</h1>
<button>Cancelar assinatura</button>

// CERTO â€” usando chaves de traduÃ§Ã£o:
<h1>{t('welcome.heading')}</h1>
<button>{t('billing.cancel_subscription')}</button>

// ESTRUTURA DE ARQUIVOS RECOMENDADA:
/locales
  /pt-BR
    common.json      // strings genÃ©ricas usadas em todo o app
    auth.json        // login, signup, reset password
    dashboard.json   // painel principal
    billing.json     // planos, pagamentos
    errors.json      // mensagens de erro
  /en-US
    common.json
    auth.json
    ...
  /es-419             // espanhol Latam (es-419 = espanhol AmÃ©rica Latina)
    common.json
    ...

// Exemplo de arquivo de traduÃ§Ã£o:
// locales/pt-BR/billing.json
{
  "cancel_subscription": "Cancelar assinatura",
  "cancel_confirm": "Tem certeza? VocÃª perderÃ¡ acesso em {{date}}.",
  "plan_price": "{{price}} por {{period}}",
  "upgrade_cta": "Fazer upgrade para {{plan_name}}"
}

// locales/en-US/billing.json
{
  "cancel_subscription": "Cancel subscription",
  "cancel_confirm": "Are you sure? You'll lose access on {{date}}.",
  "plan_price": "{{price}} per {{period}}",
  "upgrade_cta": "Upgrade to {{plan_name}}"
}
```

---

## 2. ImplementaÃ§Ã£o com Next.js + next-intl ðŸ› ï¸

```typescript
// InstalaÃ§Ã£o:
// npm install next-intl

// next.config.js
import createNextIntlPlugin from 'next-intl/plugin';
const withNextIntl = createNextIntlPlugin();
export default withNextIntl({
  // seu config do Next.js
});

// middleware.ts (roteamento por locale)
import createMiddleware from 'next-intl/middleware';
export default createMiddleware({
  locales: ['pt-BR', 'en-US', 'es-419'],
  defaultLocale: 'pt-BR',
  localeDetection: true  // detecta pelo Accept-Language header
});

// app/[locale]/layout.tsx
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';

export default async function LocaleLayout({ children, params: { locale } }) {
  const messages = await getMessages();
  return (
    <NextIntlClientProvider locale={locale} messages={messages}>
      {children}
    </NextIntlClientProvider>
  );
}

// Componente usando traduÃ§Ãµes:
import { useTranslations } from 'next-intl';

export function BillingCard({ price, date, planName }) {
  const t = useTranslations('billing');
  
  return (
    <div>
      <h2>{t('plan_price', { price, period: t('monthly') })}</h2>
      <button onClick={handleCancel}>
        {t('cancel_subscription')}
      </button>
      <p>{t('cancel_confirm', { date: formatDate(date, locale) })}</p>
    </div>
  );
}

// Server component com traduÃ§Ãµes:
import { getTranslations } from 'next-intl/server';

export async function generateMetadata({ params: { locale } }) {
  const t = await getTranslations({ locale, namespace: 'metadata' });
  return { title: t('dashboard.title') };
}
```

---

## 3. LocalizaÃ§Ã£o: AlÃ©m da TraduÃ§Ã£o ðŸŒ

Traduzir texto Ã© sÃ³ 30% do trabalho. O que realmente diferencia:

```typescript
// DATAS E HORÃRIOS (sempre usar Intl API):
const formatDate = (date: Date, locale: string) => {
  return new Intl.DateTimeFormat(locale, {
    day: '2-digit',
    month: 'long',
    year: 'numeric'
  }).format(date);
  // pt-BR: "18 de fevereiro de 2026"
  // en-US: "February 18, 2026"
  // de-DE: "18. Februar 2026"
};

// MOEDAS (nunca converter â€” sempre mostrar na moeda local):
const formatCurrency = (amount: number, currency: string, locale: string) => {
  return new Intl.NumberFormat(locale, {
    style: 'currency',
    currency: currency  // 'BRL', 'USD', 'EUR'
  }).format(amount);
  // pt-BR + BRL: "R$ 1.990,00"
  // en-US + USD: "$1,990.00"
  // de-DE + EUR: "1.990,00 â‚¬"
};

// PLURALIZAÃ‡ÃƒO (regras diferentes por idioma):
// PortuguÃªs: 0 itens, 1 item, 2+ itens
// InglÃªs: 0 items, 1 item, 2+ items
// Russo: regras diferentes para 1, 2-4, 5+

// next-intl suporta ICU message format:
// "VocÃª tem {count, plural, =0 {nenhum item} one {# item} other {# itens}}"

// DIREÃ‡ÃƒO DO TEXTO (RTL para Ã¡rabe, hebraico):
// next-intl + tailwind:
// dir={locale === 'ar' || locale === 'he' ? 'rtl' : 'ltr'}
// Classes tailwind: rtl:mr-4 â†’ aplica margin-right sÃ³ em RTL

// TELEFONES (formatos diferentes):
// Brasil: (11) 99999-9999
// EUA: (555) 555-5555
// Usar libphonenumber-js para validar e formatar

// FORMULÃRIOS:
// Nome: Ordem diferente em culturas asiÃ¡ticas (sobrenome primeiro)
// EndereÃ§o: formatos completamente diferentes por paÃ­s
// Use address-book ou react-hook-form-address para normalizar
```

---

## 4. SEO Internacional e hreflang ðŸ”

```html
<!-- Em cada pÃ¡gina, adicionar hreflang para todas as versÃµes: -->
<head>
  <link rel="alternate" hreflang="pt-BR" href="https://seusite.com/pt-BR/features" />
  <link rel="alternate" hreflang="en-US" href="https://seusite.com/en-US/features" />
  <link rel="alternate" hreflang="es-419" href="https://seusite.com/es-419/features" />
  <link rel="alternate" hreflang="x-default" href="https://seusite.com/features" />
</head>

<!-- Next.js (app router) â€” gerar automaticamente: -->
export async function generateMetadata({ params: { locale } }) {
  const t = await getTranslations({ locale, namespace: 'metadata' });
  
  return {
    title: t('features.title'),
    description: t('features.description'),
    alternates: {
      canonical: `/${locale}/features`,
      languages: {
        'pt-BR': '/pt-BR/features',
        'en-US': '/en-US/features',
        'es-419': '/es-419/features',
      }
    }
  };
}

// ESTRUTURA DE URL recomendada:
// âœ… seusite.com/en-US/features (subpath â€” mais fÃ¡cil de manter)
// âœ… en.seusite.com/features (subdomain â€” melhor para SEO avanÃ§ado)
// âŒ seusite.com/features?lang=en (query param â€” ruim para SEO)
```

---

## 5. GestÃ£o de TraduÃ§Ãµes em Escala ðŸ“‹

```
FLUXO DE TRADUÃ‡ÃƒO (produto em crescimento):

FASE 1 â€” Fundador traduz (0â†’1):
â†’ Escrever strings em PT-BR + EN-US manualmente
â†’ Usar Claude para traduÃ§Ã£o inicial de PT-BR â†’ EN-US
â†’ Revisar traduÃ§Ãµes de marketing com falante nativo
â†’ Strings tÃ©cnicas (UI): Claude Ã© suficiente

FASE 2 â€” AutomaÃ§Ã£o com Claude (1â†’10):
â†’ Pipeline n8n: detecta strings novas em locales/pt-BR
â†’ Claude traduz para todos os idiomas ativos
â†’ Pull Request automÃ¡tico com as traduÃ§Ãµes
â†’ Review humano opcional (para strings crÃ­ticas: CTAs, billing)

FASE 3 â€” Plataforma especializada (10â†’100):
â†’ Phrase, Lokalise ou Crowdin para gestÃ£o profissional
â†’ Integra com GitHub: strings novas â†’ tarefa de traduÃ§Ã£o automÃ¡tica
â†’ MemÃ³ria de traduÃ§Ã£o: reutiliza traduÃ§Ãµes anteriores
â†’ GlossÃ¡rio: termos do produto sempre traduzidos igual

CLAUDE PROMPT PARA TRADUÃ‡ÃƒO DE ALTA QUALIDADE:
"VocÃª Ã© um tradutor especializado em produtos SaaS.
Traduza do PT-BR para [idioma].
Contexto: interface de [tipo de produto] para [pÃºblico].
Tom: [profissional/casual/tÃ©cnico].
GlossÃ¡rio obrigatÃ³rio: [termos do produto que nÃ£o devem ser traduzidos].
Preserve variÃ¡veis como {{variable}} e {{count}} intactas.
Strings a traduzir: [JSON]"
```

---

## 6. EstratÃ©gia de Entrada em Mercados ðŸš€

```
SEQUÃŠNCIA RECOMENDADA DE EXPANSÃƒO:

1. PT-BR â†’ ES-419 (Latam):
   - Maior afinidade cultural
   - Mercado de 650M de pessoas
   - Espanhol Latam (es-419) cobre MÃ©xico, ColÃ´mbia, Argentina, Chile
   - AtenÃ§Ã£o: Espanha (es-ES) Ã© diferente â€” vocabulÃ¡rio e tom distintos

2. ES-419 â†’ EN-US:
   - Mercado mais competitivo
   - Mas tambÃ©m maior e mais disposto a pagar por SaaS
   - Exige marketing e positioning totalmente diferentes

3. EN-US â†’ mercados europeus:
   - GDPR mais rigoroso que LGPD (DPA obrigatÃ³rio, Privacy Shield questÃµes)
   - Considerar DE-DE (maior mercado tech da Europa)
   - FR-FR, NL-NL dependendo do vertical

PRICING POR MERCADO:
â†’ NÃ£o use conversÃ£o simples de cÃ¢mbio
â†’ Latam: 40-60% do preÃ§o em USD (poder aquisitivo)
â†’ EUA/Europa: paridade ou premium
â†’ Pesquisar benchmarks de concorrentes por mercado
â†’ Stripe suporta pricing por paÃ­s nativamente
```

---

## Trigger desta Skill

- Iniciar desenvolvimento de qualquer produto (implementar i18n desde o inÃ­cio)
- "Quero lanÃ§ar nos EUA / Latam / Europa"
- "Preciso adicionar inglÃªs ao produto"
- "Como organizar as traduÃ§Ãµes do projeto?"
- Problema de formataÃ§Ã£o de data, moeda ou nÃºmero por paÃ­s
- Configurar SEO para mÃºltiplos idiomas
---