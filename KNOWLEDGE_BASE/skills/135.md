---
name: RAG Cache Manager (The 80/20 Efficiency Engine)
description: Protocol for checking if a request has been answered before (80%) and only generating new tokens for the unique delta (20%).
---

# RAG Cache Manager Protocol â™»ï¸

**Objective:** "Never Think Twice."

## 1. The "DejÃ  Vu" Check (Vector Lookup) ðŸ”
*   **Before Generation:** The Agent scans `episodic_memory_compressor` and `knowledge_graph.json`.
*   **Query:** "Has this exact question been asked regarding this exact file in the last 30 days?"
*   **Match Threshold:** > 95% similarity.
*   **Action:**
    *   **Yes:** Retrieve the *old answer*. Do NOT call the LLM. Cost: 0 Gen Tokens.
    *   **No:** Proceed to Generation.

## 2. The Delta Generator (The 20% Rule) ðŸ“
*   **Scenario:** User asks for "The same landing page but blue".
*   **Old Way:** Regenerate 500 lines of React code with `bg-blue-500`. (Cost: 500 tokens).
*   **New Way:**
    1.  Retrieve old code from Cache.
    2.  Identify the *Delta* (Change `bg-red-500` to `bg-blue-500`).
    3.  Output *Only the Diff* or the specific CSS line. (Cost: 5 tokens).
    4.  Client-side patching applies the change.

## 3. The "Freezing" Standard ðŸ§Š
*   **Stable Assets:** Files like `Terms_of_Service.md` or `Core_Utils.ts` rarely change.
*   **Tagging:** Mark these as **[FROZEN]** in the Knowledge Graph.
*   **Impact:** The Agent is FORBIDDEN from reading/generating them again unless receiving an override command `force_update`.
---