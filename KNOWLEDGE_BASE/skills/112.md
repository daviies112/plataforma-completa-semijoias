---
name: Email Marketing & Automation (O Canal que o Antigravity Ignorou)
skill_id: 78
description: Protocolo completo para email marketing â€” desde configuraÃ§Ã£o de domÃ­nio com alta entregabilidade atÃ© sequÃªncias de nurturing com IA, broadcasts segmentados, automaÃ§Ãµes de ciclo de vida e analytics de conversÃ£o. Integra Resend, Loops, e Mailchimp via n8n.
---

# Email Marketing & Automation Protocol ðŸ“§

**Objetivo:** "WhatsApp abre em 98% dos casos. Email converte em 42x ROI. Ambos sÃ£o necessÃ¡rios."

## Por que Esta Era a Lacuna

A Skill 74 (WhatsApp Business) cobre o canal de maior abertura do Brasil. Mas email Ã© o canal de maior ROI de marketing da histÃ³ria â€” R$42 de retorno para cada R$1 investido. O Antigravity tinha zero protocolo para email, fazendo com que qualquer produto construÃ­do nascesse sem a camada de nurturing e retenÃ§Ã£o mais testada e confiÃ¡vel do mercado.

---

## 1. Infraestrutura de Email: Entregabilidade Primeiro ðŸ—ï¸

Antes de qualquer automaÃ§Ã£o, o domÃ­nio precisa ter autoridade. Email sem configuraÃ§Ã£o correta vai para spam â€” e spam tem 0% de conversÃ£o.

```bash
# CHECKLIST DE CONFIGURAÃ‡ÃƒO DE DOMÃNIO (obrigatÃ³rio antes de qualquer envio)

# 1. SPF Record (autoriza servidores a enviarem pelo seu domÃ­nio)
# Adicionar no DNS como TXT record:
v=spf1 include:_spf.resend.com include:amazonses.com ~all

# 2. DKIM (assina criptograficamente cada email)
# Gerado pelo provedor (Resend, SES). Adicionar como CNAME:
resend._domainkey.seudominio.com â†’ value-do-provedor.dkim.resend.com

# 3. DMARC (polÃ­tica de autenticaÃ§Ã£o)
# Adicionar como TXT record:
_dmarc.seudominio.com â†’ "v=DMARC1; p=quarantine; rua=mailto:dmarc@seudominio.com; pct=100"

# 4. Custom tracking domain (para links rastreÃ¡veis)
# CNAME: track.seudominio.com â†’ tracking.resend.com

# VERIFICAÃ‡ÃƒO:
# â†’ MXToolbox.com: verificar SPF, DKIM, DMARC
# â†’ Mail-tester.com: score deve ser â‰¥ 9/10 antes de qualquer campanha
# â†’ Google Postmaster Tools: monitorar reputaÃ§Ã£o do domÃ­nio

# WARM-UP (para domÃ­nios novos):
# Semana 1: 50 emails/dia para lista engajada
# Semana 2: 200 emails/dia
# Semana 3: 500 emails/dia
# Semana 4: 1.000 emails/dia
# Semana 5+: escalar progressivamente
```

---

## 2. Stack de Email por Caso de Uso ðŸ› ï¸

```
TRANSACIONAL (confirmaÃ§Ãµes, recibos, senhas):
â†’ Resend (recomendado 2026)
â†’ RazÃµes: SDK TypeScript/Python excelente, dashboard limpo, preÃ§o
â†’ PreÃ§o: 3.000 emails/mÃªs grÃ¡tis, $20/mÃªs para 50k
â†’ Alternativa: AWS SES (mais barato em escala, mais complexo)

MARKETING (newsletters, campanhas, nurturing):
â†’ Loops.so (para SaaS â€” integra diretamente com Stripe, Supabase)
â†’ Brevo (ex-Sendinblue) para uso geral, GDPR compliant
â†’ Klaviyo para e-commerce com segmentaÃ§Ã£o avanÃ§ada
â†’ Mailchimp para quem quer simplicidade sem cÃ³digo

SEQUÃŠNCIAS AVANÃ‡ADAS + n8n:
â†’ Resend para envio + n8n para orquestraÃ§Ã£o da lÃ³gica
â†’ Supabase para armazenar estado (qual passo da sequÃªncia)
â†’ Claude para personalizaÃ§Ã£o dinÃ¢mica do conteÃºdo
```

---

## 3. SequÃªncias de Email com IA (Nurturing Personalizado) ðŸ¤–

```javascript
// n8n Workflow: SequÃªncia de Onboarding com IA
// Trigger: novo usuÃ¡rio se cadastra no Supabase

// ESTRUTURA DA SEQUÃŠNCIA (7-email onboarding):
const onboarding_sequence = [
  { day: 0,  subject: "Bem-vindo(a), {first_name}! Por onde comeÃ§ar",    type: "welcome" },
  { day: 1,  subject: "O seu primeiro {produto_feature} em 5 minutos",   type: "activation" },
  { day: 3,  subject: "Como {empresa_similar} fez X com {produto}",      type: "social_proof" },
  { day: 7,  subject: "VocÃª usou {feature_mais_usada}?",                 type: "engagement" },
  { day: 14, subject: "Resultado: usuÃ¡rios que fazem X tÃªm Y% mais ROI", type: "value_proof" },
  { day: 21, subject: "Uma pergunta direta, {first_name}",               type: "reply_request" },
  { day: 30, subject: "Seu relatÃ³rio de 30 dias",                        type: "milestone" }
];

// PERSONALIZAÃ‡ÃƒO COM CLAUDE (n8n Code Node):
async function personalizeEmail(user, template_type) {
  const userContext = {
    name: user.first_name,
    plan: user.subscription_plan,
    features_used: user.features_used,  // do Supabase
    industry: user.company_industry,
    company_size: user.company_size
  };
  
  const response = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: { 'x-api-key': process.env.ANTHROPIC_KEY, 'Content-Type': 'application/json' },
    body: JSON.stringify({
      model: 'claude-sonnet-4-6',
      max_tokens: 800,
      messages: [{
        role: 'user',
        content: `Escreva um email de ${template_type} para este usuÃ¡rio:
        ${JSON.stringify(userContext)}
        
        Regras:
        - Tom: profissional mas caloroso
        - MÃ¡ximo 150 palavras no corpo
        - Um Ãºnico CTA claro
        - Personalizar com o contexto da indÃºstria
        - NÃ£o mencionar planos ou preÃ§os
        
        Retornar JSON: { subject, preview_text, body_html, cta_text, cta_url }`
      }]
    })
  });
  
  return JSON.parse(response.content[0].text);
}

// ENVIO VIA RESEND:
async function sendEmail(to, email_content) {
  const resend = new Resend(process.env.RESEND_KEY);
  return await resend.emails.send({
    from: 'equipe@seudominio.com',
    to: to,
    subject: email_content.subject,
    html: email_content.body_html,
    headers: {
      'X-Entity-Ref-ID': generateUnsubscribeToken(to)  // para opt-out
    }
  });
}
```

---

## 4. SegmentaÃ§Ã£o AvanÃ§ada e Broadcasts ðŸŽ¯

```sql
-- SEGMENTOS NO SUPABASE (base para todos os broadcasts)

-- UsuÃ¡rios de alto valor sem engajamento recente (risco de churn)
CREATE VIEW segment_at_risk AS
SELECT 
  u.id, u.email, u.first_name,
  MAX(e.created_at) as last_event,
  COUNT(DISTINCT e.event_name) as unique_events_30d,
  u.subscription_plan,
  u.mrr
FROM users u
LEFT JOIN analytics_events e ON e.user_id = u.id 
  AND e.created_at > NOW() - INTERVAL '30 days'
WHERE u.subscription_plan != 'free'
  AND u.mrr > 50  -- usuÃ¡rios pagantes
GROUP BY u.id, u.email, u.first_name, u.subscription_plan, u.mrr
HAVING MAX(e.created_at) < NOW() - INTERVAL '14 days'  -- sem atividade hÃ¡ 14 dias
  OR COUNT(DISTINCT e.event_name) < 3;  -- baixo engajamento

-- Feature nÃ£o adotada (cross-sell/upsell)
CREATE VIEW segment_feature_gap AS
SELECT u.id, u.email, u.first_name, 
       'feature_x' as missing_feature
FROM users u
WHERE NOT EXISTS (
  SELECT 1 FROM feature_usage fu 
  WHERE fu.user_id = u.id AND fu.feature = 'feature_x'
)
AND u.subscription_plan = 'pro';  -- tem o plano mas nÃ£o usa

-- Script n8n para broadcast segmentado:
-- 1. Query Supabase para segment_at_risk
-- 2. Para cada usuÃ¡rio: Claude gera email personalizado (win-back angle)
-- 3. Resend envia com tag 'win-back' para tracking
-- 4. Supabase log: email_sent com timestamp e segment
```

---

## 5. Analytics e MÃ©tricas que Importam ðŸ“Š

```javascript
// MÃ‰TRICAS OBRIGATÃ“RIAS (Supabase + Resend Webhooks)

// Resend envia eventos via webhook para n8n:
// opened, clicked, bounced, complained, unsubscribed

// Tabela de tracking:
CREATE TABLE email_events (
  id UUID DEFAULT gen_random_uuid(),
  email_id TEXT,          -- Resend message ID
  user_id UUID REFERENCES users(id),
  event_type TEXT,        -- opened, clicked, bounced, etc.
  sequence_name TEXT,     -- qual sequÃªncia
  step_number INT,        -- qual passo
  link_clicked TEXT,      -- qual link (se clicked)
  created_at TIMESTAMPTZ DEFAULT NOW()
);

// MÃ‰TRICAS SAUDÃVEIS (benchmarks SaaS 2026):
// Open rate: > 25% (transacional: > 45%)
// Click rate: > 3% (transacional: > 8%)
// Unsubscribe: < 0.5% por envio
// Bounce rate: < 2% (hard bounce: limpar imediatamente)
// Spam complaint: < 0.08% (crÃ­tico â€” acima disso: reputaÃ§Ã£o destruÃ­da)

// DASHBOARD MENSAL (Antigravity Meta Monitor integra aqui):
const email_dashboard = {
  sequences_active: 3,
  emails_sent_30d: 12450,
  avg_open_rate: '31.2%',
  avg_click_rate: '4.8%',
  conversions_from_email: 23,  // usuÃ¡rios que upgradaram apÃ³s email
  revenue_attributed: 'R$4.600',
  best_performing_email: 'day_3_social_proof',
  worst_performing_email: 'day_21_reply_request'
};
```

---

## 6. Compliance LGPD/GDPR no Email ðŸ”

```
REGRAS OBRIGATÃ“RIAS:
âœ… Double opt-in para lista de marketing (nÃ£o para transacional)
âœ… Link de descadastro em TODOS os emails (exceto transacional crÃ­tico)
âœ… Honrar unsubscribe em < 10 dias Ãºteis (LGPD exige)
âœ… Guardar prova de consentimento: quando, qual checkbox, IP
âœ… NÃ£o compartilhar lista com terceiros sem consentimento explÃ­cito

IMPLEMENTAÃ‡ÃƒO (Supabase):
CREATE TABLE email_consents (
  user_id UUID REFERENCES users(id),
  consent_type TEXT,        -- 'marketing', 'transactional'
  consented BOOLEAN,
  consented_at TIMESTAMPTZ,
  ip_address TEXT,
  consent_source TEXT,      -- 'signup_form', 'checkout', 'settings'
  withdrawn_at TIMESTAMPTZ  -- quando cancelou
);

-- Antes de qualquer envio de marketing:
-- WHERE consented = true AND withdrawn_at IS NULL
```

---

## Trigger desta Skill

- Configurar email para um novo produto ou SaaS
- Criar sequÃªncia de onboarding ou nurturing
- Problema de entregabilidade (emails caindo em spam)
- Configurar broadcasts segmentados
- Integrar email com Supabase, Stripe ou n8n
- "Como envio email para usuÃ¡rios que nÃ£o usaram X feature?"
---