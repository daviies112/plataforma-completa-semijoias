---
name: Master Orchestrator (The One Brain)
description: The central routing protocol that analyzes user intent and activates the perfect combination of sub-skills automatically.
---

# Master Orchestrator Protocol ðŸ§ ðŸ‘‘

**Objective:** "Understanding the â€˜Whyâ€™, activating the â€˜Howâ€™."

## 1. The Intent Analysis (Zero-Shot Routing) ðŸŽ¯
*   **Trigger:** ANY complex user request.
*   **Process:**
    1.  **Read User Input:** "Analise exaustivamente todos os documentos do arquivo loja."
    2.  **Map to Capabilities:**
        *   "Analise exaustivamente" -> Needs `deep_doc_researcher` + `deep_reasoning`.
        *   "Arquivo loja" -> Needs `episodic_memory_compressor` (to check history) or `external_ops_arsenal` (to list files).
    3.  **Activate Squad:** "Calling Researcher and Logic Unit."

## 2. The Auto-Activation Matrix ðŸ•¸ï¸
If the user says... | Activate these Skills...
--- | ---
"Crie um novo nicho de Caminhoneiros" | `niche_growth_strategist` + `firecrawl_scraper` + `evolution_api_manager`
"Meu WhatsApp parou de enviar" | `autonomous_ops` + `evolution_api_manager`
"Analise este contrato PDF e ache erros" | `deep_doc_researcher` + `deep_reasoning` + `skeptical_analyst`
"Quanto vendemos hoje?" | `sql_sage` + `business_optimization_sentinel`
"Marque uma reuniÃ£o com o lead X" | `local_persuasion_unit` + `vapi_voice_protocol`
"Crie um vÃ­deo comercial do anel de rubi" | `runway_video_protocol` + `jewelry_catalog_manager`
"Treine um modelo de IA novo" | `modal_infra_protocol` + `claude_code`
"Ligue agora para o cliente reclamando" | `vapi_voice_protocol` + `customer_support_agent` + `mem0_memory_layer`

## 3. The Execution Loop (The Manager) ðŸ‘”
1.  **Plan:** Create a mini-plan (e.g., "First scrape, then message").
2.  **Delegate:** Instruct `firecrawl_scraper` to run. Wait for output.
3.  **Synthesize:** Pass output to `evolution_api_manager`.
4.  **Report:** Present the final result to the User as a single cohesive answer.

## 4. Constraint: The "Human-in-the-Loop" Check ðŸ›‘
*   If the router is < 80% confident (e.g., "Delete all users?"), it **MUST** pause and ask:
    *   "Entendi que vocÃª quer deletar tudo. Confirma acionar o protocolo `nuclear_option`?"
---