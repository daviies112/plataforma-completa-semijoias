---
name: TotalExpress Logistics Agent (Correio Inteligente para Revendedoras)
skill_id: 84
description: Protocolo completo para integraÃ§Ã£o com TotalExpress via MCP e n8n â€” rastrear encomendas, resolver problemas de entrega, comunicar automaticamente com revendedoras e escalar para humano apenas quando necessÃ¡rio. Elimina o principal gargalo de operaÃ§Ã£o de plataformas de revenda.
---

# TotalExpress Logistics Agent Protocol ðŸ“¦

**Objetivo:** "A revendedora nunca precisa perguntar 'cadÃª meu pedido?' â€” o sistema fala antes dela."

## Por que LogÃ­stica Ã© o Maior Gargalo

Em plataformas de revenda (semijoias, cosmÃ©ticos, alimentos), o maior volume de suporte Ã© sobre entregas. "Onde estÃ¡ minha maleta?", "Meu pedido foi devolvido", "A transportadora disse que tentou mas ninguÃ©m estava em casa". Sem automaÃ§Ã£o, cada resposta consome minutos preciosos repetitivamente. Com esta skill, o agente resolve 80% dos casos automaticamente.

---

## 1. Arquitetura MCP para TotalExpress ðŸ—ï¸

### Criar o MCP Server Customizado

```python
# mcp_totalexpress/server.py
from fastmcp import FastMCP
import requests
from datetime import datetime

mcp = FastMCP("totalexpress-logistics")

TOTALEXPRESS_BASE_URL = "https://api.totalexpress.com.br/v1"
API_TOKEN = os.getenv("TOTALEXPRESS_API_TOKEN")

@mcp.tool()
def rastrear_encomenda(codigo_rastreio: str) -> dict:
    """
    Rastreia uma encomenda TotalExpress e retorna status atual + histÃ³rico.
    Use quando revendedora perguntar 'onde estÃ¡ meu pedido?'
    """
    response = requests.get(
        f"{TOTALEXPRESS_BASE_URL}/tracking/{codigo_rastreio}",
        headers={"Authorization": f"Bearer {API_TOKEN}"}
    )
    data = response.json()
    
    # Classificar o status em categorias compreensÃ­veis
    status = classify_status(data['status_code'])
    
    return {
        "codigo": codigo_rastreio,
        "status": status['categoria'],  # em_transito, entregue, problema, devolvido
        "descricao": status['descricao_humana'],
        "ultima_atualizacao": data['ultima_atualizacao'],
        "previsao_entrega": data.get('previsao'),
        "historico": data['eventos'][-5:],  # Ãºltimos 5 eventos
        "requer_acao": status['requer_acao'],  # bool
        "acao_recomendada": status.get('acao')
    }

@mcp.tool()
def listar_pedidos_problema() -> list[dict]:
    """
    Lista todos os pedidos com status problemÃ¡tico (devolvidos, extraviados,
    tentativa falha hÃ¡ mais de 2 dias). Use para revisÃ£o diÃ¡ria.
    """
    response = requests.get(
        f"{TOTALEXPRESS_BASE_URL}/shipments",
        headers={"Authorization": f"Bearer {API_TOKEN}"},
        params={
            "status": "problema,devolvido,tentativa_falha",
            "days": 7
        }
    )
    return response.json()['shipments']

@mcp.tool()
def solicitar_nova_tentativa(codigo_rastreio: str, observacao: str = "") -> dict:
    """
    Solicita nova tentativa de entrega para pedido com tentativa falha.
    """
    response = requests.post(
        f"{TOTALEXPRESS_BASE_URL}/shipments/{codigo_rastreio}/retry",
        headers={"Authorization": f"Bearer {API_TOKEN}"},
        json={"observacao": observacao}
    )
    return response.json()

@mcp.tool()
def criar_devolucao(codigo_rastreio: str, motivo: str) -> dict:
    """
    Inicia processo de devoluÃ§Ã£o de uma encomenda.
    """
    response = requests.post(
        f"{TOTALEXPRESS_BASE_URL}/shipments/{codigo_rastreio}/return",
        headers={"Authorization": f"Bearer {API_TOKEN}"},
        json={"motivo": motivo}
    )
    return response.json()

@mcp.tool()
def gerar_etiqueta(pedido: dict) -> str:
    """
    Gera etiqueta de envio TotalExpress para novo pedido.
    Retorna URL do PDF da etiqueta.
    """
    response = requests.post(
        f"{TOTALEXPRESS_BASE_URL}/shipments",
        headers={"Authorization": f"Bearer {API_TOKEN}"},
        json={
            "destinatario": pedido['revendedora'],
            "endereco": pedido['endereco'],
            "peso": pedido['peso_kg'],
            "valor_declarado": pedido['valor'],
            "servico": "E" if pedido['urgente'] else "X"  # Expresso vs EconÃ´mico
        }
    )
    return response.json()['etiqueta_url']

if __name__ == "__main__":
    mcp.run()
```

---

## 2. Classificador Inteligente de Status ðŸ§ 

```python
STATUS_MAP = {
    # CÃ³digos de status TotalExpress â†’ categorias humanas
    "01": {
        "categoria": "coletado",
        "descricao_humana": "Maleta coletada e a caminho do centro de distribuiÃ§Ã£o",
        "requer_acao": False
    },
    "02": {
        "categoria": "em_transito",
        "descricao_humana": "Em trÃ¢nsito entre filiais â€” tudo certo!",
        "requer_acao": False
    },
    "03": {
        "categoria": "saiu_entrega",
        "descricao_humana": "Saiu para entrega hoje! Aguarde o entregador",
        "requer_acao": False
    },
    "04": {
        "categoria": "entregue",
        "descricao_humana": "Entregue com sucesso âœ…",
        "requer_acao": False
    },
    "05": {
        "categoria": "tentativa_falha",
        "descricao_humana": "Tentativa de entrega sem sucesso â€” destinatÃ¡rio ausente",
        "requer_acao": True,
        "acao": "solicitar_reagendamento"
    },
    "06": {
        "categoria": "devolvido",
        "descricao_humana": "Pedido devolvido ao remetente",
        "requer_acao": True,
        "acao": "iniciar_reenvio_ou_reembolso"
    },
    "07": {
        "categoria": "extraviado",
        "descricao_humana": "Pedido extraviado â€” acionar suporte TotalExpress",
        "requer_acao": True,
        "acao": "acionar_seguro_e_suporte"
    },
    "08": {
        "categoria": "aguardando_retirada",
        "descricao_humana": "Aguardando retirada na agÃªncia mais prÃ³xima",
        "requer_acao": True,
        "acao": "notificar_revendedora_retirada"
    }
}
```

---

## 3. AutomaÃ§Ã£o no n8n â€” Fluxos Completos âš™ï¸

### Fluxo 1: Resposta AutomÃ¡tica ao "Onde estÃ¡ meu pedido?"

```
[WhatsApp Webhook] 
    â†’ [Detectar intenÃ§Ã£o: "rastreio/pedido/entrega"] 
    â†’ [Buscar cÃ³digo rastreio do cliente no Supabase]
    â†’ [MCP Tool: rastrear_encomenda]
    â†’ [Switch: status do pedido]
        
CASO entregue:
    â†’ "Sua maleta foi entregue no dia X! Aproveite muito ðŸŽ‰"
    
CASO em_transito:
    â†’ "Sua maleta estÃ¡ a caminho! PrevisÃ£o: {{previsao_entrega}}"
    
CASO tentativa_falha:
    â†’ "Ops! O entregador tentou mas nÃ£o encontrou ninguÃ©m em casa."
    â†’ "Quer que ele tente novamente? Responda SIM para reagendar"
    â†’ [Aguardar resposta]
    â†’ SE SIM: MCP Tool: solicitar_nova_tentativa
    
CASO aguardando_retirada:
    â†’ "Sua maleta estÃ¡ disponÃ­vel para retirada na agÃªncia {{agencia}}"
    â†’ EndereÃ§o + horÃ¡rio de funcionamento
    
CASO extraviado:
    â†’ "Identificamos um problema com sua entrega ðŸ˜”"
    â†’ "JÃ¡ acionamos o suporte da TotalExpress. Retorno em 24-48h"
    â†’ [Notificar humano responsÃ¡vel via Slack]
```

### Fluxo 2: Monitor DiÃ¡rio de Problemas (9h todos os dias)

```
[Schedule: todo dia 9h]
    â†’ [MCP Tool: listar_pedidos_problema]
    â†’ [Para cada pedido problemÃ¡tico:]
        â†’ [Buscar dados da revendedora no Supabase]
        â†’ [Classificar antiguidade do problema]
        
        SE tentativa_falha < 48h:
            â†’ Enviar WhatsApp automÃ¡tico de reagendamento
        
        SE tentativa_falha > 48h:
            â†’ Escalar para humano (prioridade alta)
        
        SE devolvido:
            â†’ WhatsApp automÃ¡tico + oferecer reenvio ou crÃ©dito
        
    â†’ [Gerar relatÃ³rio diÃ¡rio de logÃ­stica]
    â†’ [Enviar resumo para responsÃ¡vel]
```

### Fluxo 3: GeraÃ§Ã£o AutomÃ¡tica de Etiqueta

```
[Trigger: novo pedido aprovado no Supabase]
    â†’ [Buscar dados do pedido]
    â†’ [MCP Tool: gerar_etiqueta]
    â†’ [Salvar URL da etiqueta no Supabase]
    â†’ [Enviar PDF da etiqueta via WhatsApp para responsÃ¡vel de envio]
    â†’ [Atualizar status pedido: "etiqueta_gerada"]
```

---

## 4. Scripts de ComunicaÃ§Ã£o com Revendedoras ðŸ’¬

```
MENSAGENS AUTOMÃTICAS POR STATUS:

âœ… PEDIDO ENVIADO (logo apÃ³s coleta):
"Oi [Nome]! Sua maleta foi enviada hoje ðŸš€
CÃ³digo de rastreio: {{codigo}}
PrevisÃ£o de entrega: {{previsao}}
Acompanhe aqui: totalexpress.com.br/rastreio/{{codigo}}"

ðŸ”” DIA DA ENTREGA (saiu para entrega):
"[Nome], sua maleta saiu para entrega hoje! 
Fique de olho, o entregador chegarÃ¡ ainda hoje ðŸ“¦"

âŒ TENTATIVA FALHA (imediatamente apÃ³s atualizaÃ§Ã£o):
"[Nome], o entregador tentou entregar sua maleta mas nÃ£o encontrou ninguÃ©m em casa.
O que prefere?
1ï¸âƒ£ Nova tentativa de entrega amanhÃ£
2ï¸âƒ£ Retirar na agÃªncia mais prÃ³xima
3ï¸âƒ£ Alterar endereÃ§o de entrega

Responda com o nÃºmero da opÃ§Ã£o."

â†©ï¸ PEDIDO DEVOLVIDO:
"[Nome], sua maleta foi devolvida para nÃ³s ðŸ˜”
Vamos resolver juntas! Pode ser por:
â€¢ EndereÃ§o incorreto
â€¢ 3 tentativas sem sucesso

O que prefere?
1ï¸âƒ£ Reenviar para o mesmo endereÃ§o (verificar horÃ¡rio disponÃ­vel)
2ï¸âƒ£ Enviar para outro endereÃ§o
3ï¸âƒ£ CrÃ©dito para prÃ³ximo pedido"
```

---

## 5. Fallback: Quando Escalar para Humano ðŸ™‹

```
ESCALAÃ‡ÃƒO AUTOMÃTICA PARA HUMANO:

SituaÃ§Ã£o 1 â€” Pedido extraviado
â†’ Humano aciona seguro + processo interno TotalExpress
â†’ Prazo: resoluÃ§Ã£o em 5-10 dias Ãºteis

SituaÃ§Ã£o 2 â€” Revendedora insistente/nervosa
â†’ Detectar tom negativo na mensagem (NLP)
â†’ Transferir para humano com contexto completo do problema

SituaÃ§Ã£o 3 â€” Problema repetido (2Âª vez no mÃªs)
â†’ Revendedora estÃ¡ tendo problemas frequentes
â†’ Humano toma iniciativa de ligar e resolver de vez

CONTEXTO PASSADO PARA HUMANO:
- Nome e contato da revendedora
- HistÃ³rico do pedido (cÃ³digo, data, status)
- HistÃ³rico de conversas relacionadas
- Status atual do MCP
- AÃ§Ãµes jÃ¡ tomadas automaticamente
```

---

## 6. Conectar no Antigravity (claude.ai) via MCP

```json
// Adicionar em mcp_config.json do Antigravity:
{
  "mcpServers": {
    "totalexpress": {
      "command": "python",
      "args": ["/home/user/mcp_totalexpress/server.py"],
      "env": {
        "TOTALEXPRESS_API_TOKEN": "{{TOTALEXPRESS_TOKEN}}"
      }
    }
  }
}
```

---

## Trigger desta Skill

- "Onde estÃ¡ o pedido da revendedora X?"
- "Tem pedidos com problema hoje?"
- "A TotalExpress tentou entregar mas nÃ£o conseguiu"
- "Gere a etiqueta de envio para o pedido #Y"
- Diariamente Ã s 9h: relatÃ³rio de pedidos problemÃ¡ticos
- Quando revendedora pergunta sobre entrega no WhatsApp
---