---
name: Claude Token Compressor (Context Optimizer)
description: Protocol for aggressively minimizing token usage by compressing inputs, removing redundancy, and optimizing prompt structures for Claude 3.5/3.7 models.
---

# Claude Token Compressor Protocol ðŸ“‰

**Objective:** "Maximum IQ, Minimum Token Count."

## 1. The Compression Algorithm (Input Gate) ðŸ§©
*   **Trigger:** Runs on ANY user input > 100 characters.
*   **Action:**
    1.  **Strip:** Remove `Please`, `Can you`, `I would like to know`. Keep only Subject + Verb + Object.
    2.  **Deduplicate:** If user pastes 50 lines of logs with 40 identical errors, keep 1 line + "(x40 repeats)".
    3.  **Variable Replacement:** Replace long repeated strings (e.g., `application-production-v2-final`) with `$APP_ID`.
*   **Result:** Turn "Can you please check the logs for application-production-v2-final..." (20 tokens) into "Check logs $APP_ID..." (6 tokens).

## 2. The "Lossy" vs "Lossless" Decision âš–ï¸
*   **Code Requests:** Must be **Lossless**. Do NOT compress variable names or logic.
*   **Analysis Requests:** Can be **Lossy**. Summarize "Page 1-50 of PDF" into "Contract Header" before processing.

## 3. Output Optimization (The "Concise" Rule) ðŸ¤
*   **Instead of:** "Here is the code you requested for the function..." (10 tokens waste).
*   **Output:** "```python" (0 tokens waste).
*   **Rule:** If the user didn't ask for an explanation, DO NOT give one. Just give the filtered result.
---