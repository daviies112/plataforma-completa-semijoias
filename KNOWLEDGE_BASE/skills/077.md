---
name: Intelligence Router (Roteador de IA â€” Modelo Certo para Cada Task)
description: O cÃ©rebro que decide qual IA usar para cada tarefa. Em 2026, usar apenas um modelo Ã© tecnologia de 2023. Este roteador analisa cada task em tempo real e direciona para o modelo com melhor custo-qualidade-velocidade: Claude, Gemini, GPT, DeepSeek, modelos locais e mais.
---

# Intelligence Router Protocol ðŸ§­

**Objetivo:** "A inteligÃªncia certa, no momento certo, pelo custo certo."

## Contexto: Por que isso Ã© a skill mais importante que faltava

Em 2026, <mark>37% das empresas usam 5+ modelos em produÃ§Ã£o</mark>. RouteLLM demonstrou <mark>85% de reduÃ§Ã£o de custo mantendo 95% de qualidade</mark>. O consensus dos melhores devs de 2026 Ã© claro: use o "Triple Stack" â€” Claude para cÃ³digo complexo, Gemini para contexto massivo e multimodal, GPT para raciocÃ­nio abstrato e criatividade.

**O Antigravity sem este roteador Ã© como ter uma caixa de ferramentas mas usar sempre o mesmo martelo.**

---

## 1. O Mapa de InteligÃªncia por Modelo (Fevereiro 2026) ðŸ—ºï¸

### Claude (Anthropic) â€” O Especialista
```
âœ… MELHOR PARA:
â†’ CÃ³digo complexo e debugging (80.9% SWE-bench â€” #1 mundial)
â†’ RaciocÃ­nio longo e multi-step com integridade
â†’ AnÃ¡lise de documentos longos (200K tokens, alta retenÃ§Ã£o)
â†’ SeguranÃ§a: menor taxa de prompt injection (4.7%)
â†’ Escrita tÃ©cnica e anÃ¡lise de texto
â†’ Tarefas agenticas via Claude Code / MCP

âŒ EVITAR QUANDO:
â†’ AnÃ¡lise de arquivos enormes (>500K tokens) â€” usar Gemini
â†’ GeraÃ§Ã£o criativa rÃ¡pida sem complexidade
â†’ Perguntas simples que nÃ£o justificam o custo
â†’ Budget apertado (mais caro por token)

ðŸ’° CUSTO: Alto. Usar para tasks que merecem.
âš¡ VELOCIDADE: MÃ©dia-alta (Sonnet > Haiku >> Opus)
```

### Gemini 3 Pro (Google) â€” O Processador Massivo
```
âœ… MELHOR PARA:
â†’ AnÃ¡lise de codebase inteiro (1M+ tokens em contexto)
â†’ Multimodal: imagem, Ã¡udio, vÃ­deo, diagrama â†’ insights
â†’ IntegraÃ§Ã£o com Google Workspace (Docs, Sheets, Drive)
â†’ Tasks com arquivos muito grandes (contratos, bases de dados)
â†’ Speed: resposta rÃ¡pida com custo baixo (Gemini Flash)
â†’ Research em fontes visuais

âŒ EVITAR QUANDO:
â†’ CÃ³digo muito complexo que requer integridade perfeita
â†’ Tasks onde "overpraising" do modelo pode distorcer feedback
â†’ Tasks que requerem raciocÃ­nio Ã©tico profundo

ðŸ’° CUSTO: Baixo a mÃ©dio (Flash Ã© muito barato)
âš¡ VELOCIDADE: Mais rÃ¡pido dos trÃªs principais
```

### GPT-5.2 (OpenAI) â€” O RaciocÃ­nio Abstrato
```
âœ… MELHOR PARA:
â†’ RaciocÃ­nio matemÃ¡tico e lÃ³gico abstrato (100% AIME 2025)
â†’ ARC-AGI-2: 52.9% â€” melhor em problemas genuinamente novos
â†’ Criatividade e brainstorming (narrativas, marketing, ideaÃ§Ã£o)
â†’ Memory persistente entre sessÃµes (recurso Ãºnico em 2026)
â†’ Tasks de "Deep Think" para bottlenecks arquiteturais

âŒ EVITAR QUANDO:
â†’ CÃ³digo que precisa de extrema confiabilidade (Claude Ã© melhor)
â†’ Contextos muito longos (Gemini ganha)
â†’ Budget controlado (caro no modo Thinking)

ðŸ’° CUSTO: VariÃ¡vel (barato no padrÃ£o, caro no Thinking)
âš¡ VELOCIDADE: MÃ©dia
```

### DeepSeek V3 â€” O Disruptor EconÃ´mico
```
âœ… MELHOR PARA:
â†’ Audit de cÃ³digo: encontrar bugs que Claude e GPT deixaram passar
â†’ Performance e eficiÃªncia: otimizaÃ§Ã£o de algoritmos
â†’ Custo: performance quase frontier por fraÃ§Ã£o do preÃ§o
â†’ VerificaÃ§Ã£o independente de outputs de outros modelos
â†’ RaciocÃ­nio matemÃ¡tico e lÃ³gico

âŒ EVITAR QUANDO:
â†’ Dados sensÃ­veis (servidor na China â€” compliance issue)
â†’ Tasks que requerem memÃ³ria de sessÃ£o
â†’ IntegraÃ§Ã£o com ecossistemas proprietÃ¡rios

ðŸ’° CUSTO: Muito baixo
âš¡ VELOCIDADE: Alta
```

### Modelos Locais (Llama, Qwen, Mistral) â€” A Privacidade Total
```
âœ… MELHOR PARA:
â†’ Dados confidenciais que nÃ£o podem sair da mÃ¡quina
â†’ Volume altÃ­ssimo de tasks simples (sem custo por token)
â†’ Tasks offline ou edge computing
â†’ Prototipagem rÃ¡pida sem preocupaÃ§Ã£o com billing

âŒ EVITAR QUANDO:
â†’ Tasks complexas que precisam de frontier model
â†’ VocÃª nÃ£o tem GPU adequada

ðŸ’° CUSTO: Zero (custo de infra prÃ³pria)
âš¡ VELOCIDADE: Depende do hardware
```

---

## 2. A Matriz de DecisÃ£o AutomÃ¡tica ðŸ§ 

O Intelligence Router classifica cada task em 3 dimensÃµes antes de decidir:

```
DIMENSÃƒO 1: COMPLEXIDADE
  Simples (pergunta direta, formataÃ§Ã£o, busca) â†’ modelo barato
  MÃ©dia (cÃ³digo funcional, anÃ¡lise bÃ¡sica) â†’ Sonnet / Gemini Pro
  Alta (arquitetura, debugging complexo, raciocÃ­nio multi-step) â†’ Opus / GPT Thinking

DIMENSÃƒO 2: TIPO DE TAREFA
  [C] CÃ³digo                 â†’ Claude Opus (primary) / DeepSeek (audit)
  [A] AnÃ¡lise/Documentos     â†’ Gemini (grande) / Claude (mÃ©dio)  
  [M] Multimodal             â†’ Gemini (lÃ­der) / GPT-4o (segundo)
  [R] RaciocÃ­nio Abstrato    â†’ GPT-5.2 Thinking / Claude Extended
  [E] Escrita/Criatividade   â†’ GPT-5.2 / Claude
  [S] SeguranÃ§a/Audit        â†’ Claude (menor injection rate) / DeepSeek
  [P] Privacidade            â†’ Modelo Local (Llama/Qwen)
  [F] Velocidade crÃ­tica     â†’ Gemini Flash / Claude Haiku

DIMENSÃƒO 3: BUDGET
  Sem restriÃ§Ã£o â†’ usar o melhor para a task
  Budget mÃ©dio â†’ Sonnet > Gemini Pro > GPT standard
  Budget mÃ­nimo â†’ Gemini Flash / Haiku / DeepSeek / Local
```

---

## 3. O Algoritmo de Roteamento ðŸ”„

```python
def route_task(task: str, context: dict) -> ModelDecision:
    """
    Decide qual modelo usar para cada task.
    """
    # PASSO 1: Classificar o tipo de task
    task_type = classify_task(task)  # [C, A, M, R, E, S, P, F]
    
    # PASSO 2: Medir complexidade
    complexity = estimate_complexity(task, context)
    tokens_needed = estimate_tokens(task, context)
    
    # PASSO 3: Verificar restriÃ§Ãµes
    has_pii = detect_pii(task, context)
    has_sensitive_ip = detect_sensitive_ip(context)
    budget_remaining = get_budget_status()
    
    # PASSO 4: DecisÃ£o
    if has_pii or has_sensitive_ip:
        return ModelDecision(model="local_llama3", reason="PRIVACIDADE")
    
    if tokens_needed > 500_000:
        return ModelDecision(model="gemini_3_pro", reason="CONTEXTO MASSIVO")
    
    if task_type == "C" and complexity == "alta":
        return ModelDecision(model="claude_opus_4_5", reason="CÃ“DIGO COMPLEXO")
    
    if task_type == "M":
        return ModelDecision(model="gemini_3_pro", reason="MULTIMODAL")
    
    if task_type == "R" and complexity == "alta":
        return ModelDecision(model="gpt_5_2_thinking", reason="RACIOCÃNIO ABSTRATO")
    
    if budget_remaining < 0.3:  # menos de 30% do budget
        return ModelDecision(model="gemini_flash", reason="BUDGET PROTEGIDO")
    
    # Default: Sonnet para tasks mÃ©dias
    return ModelDecision(model="claude_sonnet_4_6", reason="DEFAULT BALANCEADO")
```

---

## 4. O Modo "Triple Stack" (Para Tasks CrÃ­ticas) ðŸ—ï¸

Para decisÃµes de alto impacto, usar mÃºltiplos modelos em sequÃªncia:

```markdown
TRIPLE STACK WORKFLOW:

FASE 1 â€” ARQUITETURA (GPT-5.2 Deep Think)
"Qual Ã© a melhor abordagem para este problema?"
â†’ RaciocÃ­nio abstrato, consideraÃ§Ã£o de trade-offs
â†’ Output: plano de alto nÃ­vel

FASE 2 â€” EXECUÃ‡ÃƒO (Claude Opus)
"Execute este plano"
â†’ ImplementaÃ§Ã£o de cÃ³digo, escrita tÃ©cnica
â†’ Output: entregÃ¡vel de alta qualidade

FASE 3 â€” AUDIT (DeepSeek / Claude)
"Encontre o que estÃ¡ errado nisto"
â†’ Review independente, edge cases, bugs
â†’ Output: lista de problemas + sugestÃµes

RESULTADO: Qualidade superior ao que qualquer modelo Ãºnico entregaria
CUSTO: Mais alto, mas justificado para decisions crÃ­ticas
```

---

## 5. O Consensus Mode (Para DecisÃµes de Alta Stakes) ðŸŽ¯

Para decisÃµes onde o erro Ã© caro, rodar a mesma query em mÃºltiplos modelos e fazer consensus:

```
CONSENSUS VOTING:
â†’ Enviar a mesma query para 3 modelos diferentes
â†’ Se 2/3 concordam: usar a resposta da maioria
â†’ Se todos discordam: escalar para humano (Human Delegation Protocol)
â†’ Ganho comprovado: +7-15 pontos de acurÃ¡cia vs melhor modelo Ãºnico
â†’ GPQA-diamond: 46.9% â†’ 68.2% com consensus entre 3 modelos
```

---

## 6. O Performance Tracker ðŸ“Š

Registrar em `.agent/meta/model_performance_log.json`:

```json
{
  "tracking_period": "2026-02",
  "routing_decisions": 1247,
  
  "model_usage": {
    "claude_opus_4_5": {"calls": 189, "success_rate": 0.97, "avg_cost": 0.031},
    "claude_sonnet_4_6": {"calls": 423, "success_rate": 0.94, "avg_cost": 0.008},
    "gemini_3_pro": {"calls": 312, "success_rate": 0.91, "avg_cost": 0.005},
    "gpt_5_2": {"calls": 98, "success_rate": 0.95, "avg_cost": 0.024},
    "deepseek_v3": {"calls": 156, "success_rate": 0.89, "avg_cost": 0.001},
    "gemini_flash": {"calls": 69, "success_rate": 0.86, "avg_cost": 0.0003}
  },
  
  "savings_vs_single_model": {
    "vs_all_claude_opus": "73% economia",
    "quality_maintained": "97%"
  },
  
  "routing_accuracy": "89% das tasks foram roteadas para o modelo Ã³timo"
}
```

---

## 7. Como Integrar via LiteLLM / OpenRouter ðŸ”Œ

```python
# Instalar: pip install litellm
from litellm import completion

def antigravity_route_and_call(task: str, context: dict) -> str:
    decision = route_task(task, context)
    
    model_map = {
        "claude_opus_4_5": "anthropic/claude-opus-4-5-20251101",
        "claude_sonnet_4_6": "anthropic/claude-sonnet-4-6",
        "gemini_3_pro": "gemini/gemini-2.5-pro",
        "gpt_5_2": "openai/gpt-5.2",
        "deepseek_v3": "deepseek/deepseek-chat",
        "gemini_flash": "gemini/gemini-2.5-flash"
    }
    
    response = completion(
        model=model_map[decision.model],
        messages=[{"role": "user", "content": task}]
    )
    
    log_routing_decision(decision, response)
    return response.choices[0].message.content
```

---

## 8. AtualizaÃ§Ã£o Mensal do Roteador ðŸ“…

O mercado de modelos muda mensalmente. Atualizar esta skill via Frontier Intelligence Monitor quando:
- Novo modelo frontier for lanÃ§ado
- Benchmark importante mudar o ranking
- PreÃ§o de algum modelo mudar >20%
- Novo caso de uso especÃ­fico for descoberto

---

## Trigger desta Skill

- **SEMPRE** â€” antes de qualquer chamada de LLM, classificar e rotear
- "Qual modelo devo usar para isto?"
- Quando o custo mensal de LLMs estÃ¡ alto (revisar roteamento)
- Quando qualidade de um tipo de output estÃ¡ baixa (pode ser modelo errado)
- Mensalmente: recalibrar baseado no performance tracker
---