---
name: Claude Console Code Guardian (O GuardiÃ£o de CÃ³digo dentro do Antigravity)
skill_id: 85
description: Define o protocolo exato de como Claude Console age como validador e aprovador de cÃ³digo dentro do ecossistema Antigravity com Gemini. O fluxo Ã©: Gemini raciocina â†’ Claude revisa/aprova â†’ Gemini executa â†’ Claude valida resultado. Nenhum cÃ³digo vai para produÃ§Ã£o sem aprovaÃ§Ã£o do Claude.
---

# Claude Console Code Guardian Protocol ðŸ›¡ï¸

**Objetivo:** "Gemini pensa rÃ¡pido e age. Claude pensa certo e aprova. Juntos, zero bugs chegam em produÃ§Ã£o."

## A DivisÃ£o de Responsabilidades

O Antigravity usa Gemini como motor principal (velocidade, contexto longo, multimodal). Mas Gemini pode alucinar em cÃ³digo, pular edge cases, ou gerar soluÃ§Ãµes frÃ¡geis. Claude Console, conectado via API, atua como o "Arquiteto SÃªnior" que revisa tudo antes de executar.

```
SEM ESTA SKILL:
Gemini â†’ [cÃ³digo] â†’ executa â†’ (problema em produÃ§Ã£o)

COM ESTA SKILL:
Gemini â†’ [cÃ³digo] â†’ Claude revisa â†’ aprovado? â†’ executa â†’ Claude valida resultado
                                    â†“ negado?
                                    Gemini refaz com feedback
```

---

## 1. Quando o Claude Console Ã© Ativado ðŸ””

```
TRIGGERS OBRIGATÃ“RIOS (Claude SEMPRE revisa):
â”œâ”€â”€ Qualquer cÃ³digo que vai para produÃ§Ã£o (VPS real)
â”œâ”€â”€ Queries SQL de escrita/deleÃ§Ã£o no banco de dados principal
â”œâ”€â”€ AlteraÃ§Ãµes em arquivos de configuraÃ§Ã£o (.env, docker-compose, nginx)
â”œâ”€â”€ Scripts que acessam dados de clientes
â”œâ”€â”€ Novos endpoints de API expostos ao pÃºblico
â”œâ”€â”€ MigraÃ§Ãµes de banco de dados
â””â”€â”€ Qualquer operaÃ§Ã£o irreversÃ­vel (drop, delete, format)

TRIGGERS OPCIONAIS (Claude revisa se complexo):
â”œâ”€â”€ CÃ³digo novo com mais de 50 linhas
â”œâ”€â”€ LÃ³gica de pagamento ou financeira
â”œâ”€â”€ CÃ³digo de autenticaÃ§Ã£o e autorizaÃ§Ã£o
â””â”€â”€ IntegraÃ§Ã£o com API externa nova

NÃƒO PRECISA DE REVISÃƒO (Gemini executa direto):
â”œâ”€â”€ Queries SELECT de leitura simples
â”œâ”€â”€ GeraÃ§Ã£o de conteÃºdo (texto, imagens)
â”œâ”€â”€ CÃ³digo em ambiente de desenvolvimento/teste
â””â”€â”€ Scripts de anÃ¡lise sem efeito colateral
```

---

## 2. O Protocolo de RevisÃ£o em 4 Etapas ðŸ“‹

### Etapa 1 â€” SubmissÃ£o (Gemini â†’ Claude)

```
FORMATO DA SUBMISSÃƒO PARA CLAUDE:

[REVISÃƒO SOLICITADA]
Tipo: {migration | api_endpoint | script_producao | config_change}
Impacto: {alto | mÃ©dio | baixo}
ReversÃ­vel: {sim | nÃ£o}
Ambiente: {produÃ§Ã£o | staging | dev}

CONTEXTO:
{Gemini explica em 1-3 parÃ¡grafos o que o cÃ³digo faz e por que}

CÃ“DIGO:
```{linguagem}
{cÃ³digo completo}
```

DEPENDÃŠNCIAS AFETADAS:
{lista de serviÃ§os, tabelas, arquivos afetados}

TESTE SUGERIDO:
{como Gemini sugere validar que funcionou}
```

### Etapa 2 â€” AnÃ¡lise Claude (o checklist secreto)

```python
# Claude avalia TODOS estes pontos antes de aprovar:

def claude_code_review(submission: CodeSubmission) -> ReviewResult:
    
    checklist = {
        # SEGURANÃ‡A
        "sem_credenciais_hardcoded": not has_hardcoded_secrets(submission.code),
        "sem_sql_injection": not has_sql_injection_risk(submission.code),
        "sem_path_traversal": not has_path_traversal(submission.code),
        "validacao_de_inputs": has_input_validation(submission.code),
        
        # QUALIDADE
        "sem_logica_infinita": not has_infinite_loop_risk(submission.code),
        "error_handling_adequado": has_error_handling(submission.code),
        "reversivel_se_necessario": is_reversible_or_has_rollback(submission.code),
        "sem_efeitos_colaterais_inesperados": no_unexpected_side_effects(submission.code),
        
        # BANCO DE DADOS
        "backup_antes_de_migration": has_backup_step_if_migration(submission),
        "sem_delete_sem_where": not has_delete_without_where(submission.code),
        "indices_adequados": has_adequate_indexes(submission.code),
        "transacao_para_operacoes_criticas": has_transaction_wrapper(submission.code),
        
        # ARQUITETURA
        "consistente_com_padrao_existente": is_consistent_with_codebase(submission.code),
        "nao_duplica_funcionalidade": not duplicates_existing_function(submission.code),
        "nomes_claros_e_descritivos": has_clear_naming(submission.code)
    }
    
    falhas = [k for k, v in checklist.items() if not v]
    
    if not falhas:
        return ReviewResult(
            status="APROVADO",
            comentario="CÃ³digo revisado e aprovado para produÃ§Ã£o.",
            melhorias_sugeridas=get_optional_improvements(submission.code)
        )
    else:
        return ReviewResult(
            status="NEGADO",
            comentario=f"Problemas encontrados: {', '.join(falhas)}",
            correcoes_obrigatorias=get_corrections(falhas, submission.code),
            exemplo_corrigido=generate_corrected_version(submission.code, falhas)
        )
```

### Etapa 3 â€” Resposta de Claude

```
SE APROVADO:
[APROVADO âœ…]
CÃ³digo revisado e aprovado para execuÃ§Ã£o em produÃ§Ã£o.

Pontos verificados:
âœ“ Sem credenciais expostas
âœ“ Tratamento de erros adequado
âœ“ OperaÃ§Ã£o reversÃ­vel (rollback: {instruÃ§Ã£o})
âœ“ Consistente com padrÃ£o do projeto

SugestÃµes opcionais (nÃ£o bloqueiam):
â†’ [sugestÃ£o opcional 1]
â†’ [sugestÃ£o opcional 2]

Prossiga com a execuÃ§Ã£o.

---

SE NEGADO:
[NEGADO âŒ â€” REVISAR ANTES DE EXECUTAR]

Problemas crÃ­ticos encontrados:

1. SQL INJECTION RISK (linha 23):
   Problema: f"SELECT * FROM users WHERE id = {user_id}"
   CorreÃ§Ã£o: cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))

2. OPERAÃ‡ÃƒO IRREVERSÃVEL SEM BACKUP (linha 45):
   Problema: DROP TABLE IF EXISTS old_data;
   CorreÃ§Ã£o: Adicionar backup antes:
   ```sql
   CREATE TABLE old_data_backup_YYYYMMDD AS SELECT * FROM old_data;
   DROP TABLE IF EXISTS old_data;
   ```

CÃ³digo corrigido sugerido:
```python
{cÃ³digo corrigido completo}
```

Resubmeta apÃ³s correÃ§Ãµes.
```

### Etapa 4 â€” ValidaÃ§Ã£o PÃ³s-ExecuÃ§Ã£o

```
APÃ“S GEMINI EXECUTAR O CÃ“DIGO APROVADO:

Gemini reporta resultado â†’ Claude valida:

[RESULTADO DA EXECUÃ‡ÃƒO]
Status: {sucesso | erro | parcial}
Output: {saÃ­da do comando}
MÃ©tricas: {tempo, registros afetados, etc.}
Erros: {se houver}

Claude analisa e responde:

SE SUCESSO:
âœ… ExecuÃ§Ã£o validada. Sistema funcionando conforme esperado.
Registrar em: .agent/meta/deploy_log.json

SE ERRO:
ðŸš¨ Erro detectado. AÃ§Ã£o imediata:
1. NÃƒO tente corrigir sem nova revisÃ£o
2. Execute rollback: {instruÃ§Ã£o de rollback}
3. Reporte o erro completo para nova anÃ¡lise
```

---

## 3. Conectar Claude Console via API no Antigravity ðŸ”Œ

```python
# claude_guardian/client.py
import anthropic
import os

client = anthropic.Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

def request_code_review(submission: dict) -> dict:
    """
    Envia cÃ³digo para Claude Console revisar via API.
    Gemini chama esta funÃ§Ã£o antes de executar qualquer cÃ³digo crÃ­tico.
    """
    
    system_prompt = """
    VocÃª Ã© o Code Guardian do sistema Antigravity. 
    Sua Ãºnica funÃ§Ã£o Ã© revisar cÃ³digo submetido pelo Gemini e aprovar ou negar.
    
    Seja rigoroso: em caso de dÃºvida, NEGUE e explique.
    NÃ£o deixe passar: SQL injection, credenciais expostas, ou operaÃ§Ãµes irreversÃ­veis sem backup.
    Seja Ãºtil: sempre que negar, forneÃ§a o cÃ³digo corrigido completo.
    """
    
    message = client.messages.create(
        model="claude-opus-4-6",  # Usar Opus para mÃ¡xima capacidade de revisÃ£o
        max_tokens=4096,
        system=system_prompt,
        messages=[
            {
                "role": "user",
                "content": format_submission(submission)
            }
        ]
    )
    
    response_text = message.content[0].text
    
    return {
        "status": "APROVADO" if "APROVADO" in response_text else "NEGADO",
        "full_review": response_text,
        "reviewed_by": "claude-opus-4-6",
        "timestamp": datetime.now().isoformat()
    }

# Skill de Gemini para chamar o guardian:
def execute_with_guardian(code: str, context: dict) -> dict:
    """
    Wrapper que todo cÃ³digo crÃ­tico deve passar antes de executar.
    """
    review = request_code_review({
        "code": code,
        "context": context,
        "environment": "producao"
    })
    
    if review['status'] == "APROVADO":
        result = execute_code(code)
        
        # Validar resultado tambÃ©m
        validation = request_execution_validation(code, result)
        
        log_to_supabase({
            "action": "code_executed",
            "approved_by": "claude-guardian",
            "code_hash": hash(code),
            "result": result,
            "validation": validation
        })
        
        return result
    else:
        raise GuardianRejectionError(review['full_review'])
```

---

## 4. VPS Seguro: Ambiente de Teste Antes da ProduÃ§Ã£o ðŸ§ª

```
ARQUITETURA DE 2 AMBIENTES:

AMBIENTE DE TESTE (VPS staging):
â†’ RÃ©plica fiel do ambiente de produÃ§Ã£o
â†’ Dados fictÃ­cios ou cÃ³pia anonimizada
â†’ Claude revisa mas com critÃ©rio menos rÃ­gido
â†’ Gemini pode testar mais livremente
â†’ ApÃ³s aprovaÃ§Ã£o no staging: submeter para produÃ§Ã£o

AMBIENTE DE PRODUÃ‡ÃƒO (VPS principal):
â†’ Claude Guardian obrigatÃ³rio para QUALQUER mudanÃ§a
â†’ Backup automÃ¡tico antes de qualquer operaÃ§Ã£o destrutiva
â†’ Log completo de todas as mudanÃ§as
â†’ Rollback em menos de 5 minutos

FLUXO COMPLETO:
Gemini gera cÃ³digo
    â†’ Testa no staging
    â†’ Claude revisa resultado no staging
    â†’ SE OK: submete para produÃ§Ã£o com aprovaÃ§Ã£o formal do Claude
    â†’ Executa em produÃ§Ã£o
    â†’ Claude valida resultado em produÃ§Ã£o
    â†’ Registra no log
```

---

## 5. Log de Auditoria ðŸ“

```sql
-- Tabela no Supabase para rastrear todas as revisÃµes
CREATE TABLE code_guardian_log (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    submissao_por TEXT DEFAULT 'gemini',
    tipo_operacao TEXT, -- migration, api, script, config
    ambiente TEXT,
    status_revisao TEXT, -- APROVADO, NEGADO
    problemas_encontrados TEXT[],
    codigo_hash TEXT,
    revisado_em TIMESTAMPTZ DEFAULT NOW(),
    executado_em TIMESTAMPTZ,
    resultado_execucao TEXT,
    validado_por_claude BOOLEAN DEFAULT FALSE
);
```

---

## Trigger desta Skill

- **Sempre** antes de executar qualquer cÃ³digo em produÃ§Ã£o
- "Gemini quer executar [script] no servidor"
- Qualquer migraÃ§Ã£o de banco de dados
- Deploy de novo cÃ³digo no VPS
- AlteraÃ§Ã£o de configuraÃ§Ã£o de serviÃ§os
- "Revisar este cÃ³digo antes de rodar"
---