---
name: MCP Native Architect (Model Context Protocol)
description: Faz o Antigravity falar o protocolo padrÃ£o da indÃºstria para conectar agentes a ferramentas, APIs e dados. MCP Ã© o "HTTP para agentes" â€” criado pela Anthropic em Nov/2024, adoÃ§Ã£o universal em 2025.
---

# MCP Native Architect Protocol ðŸ”Œ

**Objetivo:** "O Antigravity fala o idioma nativo da internet de agentes."

## Contexto: Por que MCP importa agora

Em novembro de 2024, a Anthropic lanÃ§ou o Model Context Protocol (MCP). Em menos de 12 meses, tornou-se o padrÃ£o universal para conectar LLMs a ferramentas externas. Claude Code, Cursor, VSCode Copilot, e praticamente toda ferramenta de dev de 2026 usa MCP. Sem entender MCP, o Antigravity fala um dialeto morto.

**MetÃ¡fora:** MCP Ã© para agentes o que HTTP Ã© para browsers. Antes do HTTP, cada site tinha seu prÃ³prio protocolo. Depois, qualquer browser acessava qualquer site. MCP faz o mesmo para agentes e ferramentas.

---

## 1. A Anatomia de um MCP Server ðŸ—ï¸

```
MCP SERVER = Uma ferramenta que o agente pode usar

Componentes:
â”œâ”€â”€ Resources (dados que o agente pode ler)
â”‚   â”œâ”€â”€ Arquivos, URLs, banco de dados
â”‚   â””â”€â”€ Atualizados em tempo real
â”œâ”€â”€ Tools (aÃ§Ãµes que o agente pode executar)
â”‚   â”œâ”€â”€ FunÃ§Ãµes com inputs/outputs definidos
â”‚   â””â”€â”€ Ex: criar_arquivo(), buscar_dados(), enviar_email()
â”œâ”€â”€ Prompts (templates prÃ©-construÃ­dos)
â”‚   â””â”€â”€ Workflows reutilizÃ¡veis
â””â”€â”€ Sampling (o server pode chamar o LLM de volta)
```

**Arquitetura de conexÃ£o:**
```
[Claude/LLM] â†MCP Clientâ†’ [MCP Server] â†â†’ [Ferramenta Real]
                              â†‘
                     JSON-RPC over stdio/SSE
```

---

## 2. Construir vs Usar MCP Servers ðŸ”§

### Modo USO (conectar a servers existentes)
O ecossistema jÃ¡ tem centenas de servers prontos:

```
SERVERS OFICIAIS ANTHROPIC:
â†’ filesystem (leitura/escrita de arquivos)
â†’ github (repositÃ³rios, issues, PRs)
â†’ postgres (queries SQL direto)
â†’ puppeteer (browser automation)
â†’ brave-search (web search)
â†’ slack (mensagens e canais)

SERVERS DA COMUNIDADE:
â†’ supabase (banco de dados + auth)
â†’ stripe (pagamentos)
â†’ notion (documentos)
â†’ linear (project management)
â†’ docker (containers)
```

**Como conectar no Claude/Cursor:**
```json
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server-supabase@latest"],
      "env": {
        "SUPABASE_URL": "sua_url",
        "SUPABASE_KEY": "sua_key"
      }
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/claude"]
    }
  }
}
```

### Modo CONSTRUÃ‡ÃƒO (criar servers customizados)

**Template Python (FastMCP):**
```python
from fastmcp import FastMCP
mcp = FastMCP("antigravity-server")

@mcp.resource("dados://relatorio-diario")
def get_daily_report() -> str:
    """Retorna o relatÃ³rio diÃ¡rio do Antigravity"""
    return read_file(".agent/meta/system_health.json")

@mcp.tool()
def buscar_projeto(nome: str) -> dict:
    """Busca detalhes de um projeto ativo"""
    return load_project_state(nome)

@mcp.tool()
def executar_skill(skill_name: str, input_data: dict) -> str:
    """Executa uma skill do Antigravity"""
    return run_skill(skill_name, input_data)

if __name__ == "__main__":
    mcp.run()
```

---

## 3. O Stack MCP do Antigravity ðŸ›ï¸

**ConfiguraÃ§Ã£o recomendada para projetos Antigravity:**

```
TIER 1 â€” Essenciais (sempre ativos):
â”œâ”€â”€ filesystem â†’ Acesso completo ao projeto
â”œâ”€â”€ postgres/supabase â†’ Banco de dados do projeto
â””â”€â”€ antigravity-custom â†’ Server prÃ³prio com skills como tools

TIER 2 â€” Por projeto:
â”œâ”€â”€ github â†’ Se o projeto estÃ¡ em Git
â”œâ”€â”€ stripe â†’ Se tem billing
â””â”€â”€ n8n-webhook â†’ Para disparar workflows

TIER 3 â€” Situacional:
â”œâ”€â”€ brave-search â†’ Quando precisa de dados externos
â”œâ”€â”€ puppeteer â†’ Quando precisa de browser
â””â”€â”€ slack â†’ Quando precisa de comunicaÃ§Ã£o
```

---

## 4. Security First ðŸ”

**Os problemas reais do MCP em 2026:**
Pesquisa da Knostic (Jul/2025): 2.000 MCP servers expostos na internet, todos sem autenticaÃ§Ã£o adequada. O Backslash Security encontrou os mesmos problemas.

**Checklist de seguranÃ§a para cada MCP server:**
```
â–¡ AutenticaÃ§Ã£o obrigatÃ³ria (OAuth 2.0 ou API Key)
â–¡ Escopo mÃ­nimo de permissÃµes (principle of least privilege)
â–¡ Logging de todas as chamadas de tool
â–¡ Rate limiting para prevenir abuso
â–¡ Nunca expor MCP server na internet sem VPN/firewall
â–¡ Secrets via variÃ¡veis de ambiente, NUNCA hardcoded
â–¡ ValidaÃ§Ã£o de inputs em todos os tools
```

---

## 5. O MCP como ExposiÃ§Ã£o do Antigravity ðŸŒ

**Ideia avanÃ§ada:** Transformar as skills do Antigravity em MCP tools.

```python
# Antigravity como MCP Server
@mcp.tool()
def deep_reasoning(problema: str) -> str:
    """Aplica o protocolo Deep Reasoning do Antigravity"""
    
@mcp.tool()
def full_stack_architect(requirements: str) -> str:
    """Projetar sistema completo seguindo padrÃµes Antigravity"""

@mcp.tool()
def sql_sage(query_request: str) -> str:
    """Escrever SQL otimizado com Schema Rigor"""
```

**Resultado:** Qualquer agente externo (Claude, Cursor, n8n) pode usar as skills do Antigravity como ferramentas nativas.

---

## Trigger desta Skill

- Ao iniciar qualquer novo projeto (configurar stack MCP)
- "Como conectar o agente ao Supabase/GitHub/Stripe?"
- Ao identificar que um tool precisa ser criado vs usar existente
- Mensalmente: auditar security dos MCP servers ativos
---