---
name: Semantic Knowledge Graph (Deep Context)
description: A system for mapping concepts and dependencies across the project, enabling impact analysis and deep understanding.
---

# Semantic Knowledge Graph Protocol ðŸ•¸ï¸

**Objective:** Understand the *why* and *how*, not just the *what*.

## 1. The Graph Structure ðŸ—ºï¸
*   **Nodes:** Concepts (e.g., `UserAuth`, `PaymentGateway`, `n8n_Webhook_X`).
*   **Edges:** Relationships (e.g., `triggers`, `depends_on`, `validates`).
*   **Storage:** `.agent/memory/knowledge_graph.json`.

## 2. Building the Graph ðŸ—ï¸
*   **Parsing:** When reading a file, extract key entities.
    *   *Scanner:* "I see a `fetch('api/stripe')`. Create node `StripeAPI` and edge `Frontend -> depends_on -> StripeAPI`."
*   **Linking:** Connect new nodes to existing ones.

## 3. Impact Analysis ðŸ’¥
*   **Trigger:** User asks to "Change the User ID format".
*   **Traversal:** Walk the graph from `UserID` node.
    *   Found: `n8n_workflow_1` (expects integer).
    *   Found: `Supabase_Table_Users` (column is uuid).
    *   Found: `React_Profile_Component` (displays string).
*   **Result:** Report ALL these as required changes in the Implementation Plan.
---