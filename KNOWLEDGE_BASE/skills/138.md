---
name: Model Failover Agent (The Brain Swap)
description: Logic for switching between LLM providers (Claude, GPT, Gemini, Llama) based on availability, cost, and performance.
---

# Model Failover Protocol ðŸ§ ðŸ”€

**Objective:** "The Intelligence Must Flow."

## 1. The Model Hierarchy ðŸ†
*   **Primary:** `claude-3-5-sonnet` (Best for Coding/Reasoning).
*   **Secondary:** `gpt-4o` (Best fallback, reliable).
*   **Tertiary:** `deepseek-v3` (Cheapest, good for simple tasks).
*   **Emergency:** `llama-3-70b` (Running locally or Groq - Zero Dependence on Big Tech APIs).

## 2. Failure Detection ðŸ“‰
*   **Triggers:**
    *   `HTTP 500`/`503` from Provider.
    *   `RateLimitError` (429).
    *   `Refusal` to output (Censorship filter false positive).

## 3. The Swap Mechanics âš™ï¸
*   **Action:**
    1.  **Catch Error:** Intercept the exception in the Python/Node wrapper.
    2.  **Hot-Swap:** Change `MODEL_ID` env var for the current session.
    3.  **Retry:** Re-send the EXACT same prompt to the new model.
    4.  **Log:** Record "Claude Failed -> Swapped to GPT-4o" in `reliability_logs`.

## 4. Cost Arbitrage (Smart Mode) ðŸ’¸
*   **Low Priority:** If a task is tagged `priority: low` (e.g., SEO article generation), voluntarily swap to `deepseek-v3` to save 90% cost.
---