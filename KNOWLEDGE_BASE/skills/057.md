---
name: Advanced Observability (Ver Tudo que Acontece em ProduÃ§Ã£o)
skill_id: 76
description: O sistema completo de observabilidade alÃ©m do Arize Phoenix (LLM monitoring). Grafana para dashboards, Prometheus para mÃ©tricas, OpenTelemetry para distributed tracing, alertas inteligentes e SLOs/SLAs â€” a diferenÃ§a entre descobrir problemas pelo cliente ou antes dele.
---

# Advanced Observability Protocol ðŸ“¡

**Objetivo:** "VocÃª nÃ£o pode melhorar o que nÃ£o mede. VocÃª nÃ£o pode proteger o que nÃ£o vÃª."

## A DiferenÃ§a do Command Center (Skill 19)

O Command Center usa Arize Phoenix â€” excelente para monitorar LLMs: hallucinations, token usage, prompt performance. Esta skill cobre a infraestrutura completa: a aplicaÃ§Ã£o, o banco de dados, a API, o n8n, a rede. SÃ£o complementares.

---

## 1. As 3 Categorias de Observabilidade ðŸ”

```
METRICS (o quÃª e quanto):
â†’ Prometheus coleta: CPU, memÃ³ria, latÃªncia, error rate, throughput
â†’ Grafana visualiza em dashboards em tempo real
â†’ Alertmanager dispara quando threshold Ã© atingido

LOGS (o que aconteceu):
â†’ Cada evento, erro, aÃ§Ã£o registrado com contexto
â†’ Centralizado no Supabase (para volume mÃ©dio) ou Loki (para volume alto)
â†’ Busca full-text para investigar incidentes

TRACES (como o pedido viajou pelo sistema):
â†’ OpenTelemetry rastreia um request atravÃ©s de mÃºltiplos serviÃ§os
â†’ "Este request levou 3.2s â€” onde passou esse tempo?"
â†’ Resposta: 2.1s no Supabase query, 0.8s na API do Claude, 0.3s no n8n
```

---

## 2. Stack de Observabilidade Completo ðŸ—ï¸

```yaml
# docker-compose.yml â€” Stack de observabilidade completo
version: '3.8'

services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    # Scrape mÃ©tricas a cada 15 segundos de todos os serviÃ§os
  
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
    depends_on: [prometheus]
    # Dashboard: https://grafana.com/grafana/dashboards/ (5000+ prontos)
  
  alertmanager:
    image: prom/alertmanager:latest
    volumes:
      - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml
    # Alertas via Slack, email, PagerDuty, WhatsApp
  
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    volumes:
      - ./otel-config.yaml:/etc/otelcol-contrib/config.yaml
    # Recebe traces, processa, envia para Grafana Tempo
```

---

## 3. OpenTelemetry: Distributed Tracing ðŸ”—

```python
# Instrumentar a aplicaÃ§Ã£o Python com OpenTelemetry
from opentelemetry import trace
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.exporter.otlp.proto.grpc.trace_exporter import OTLPSpanExporter
from opentelemetry.sdk.trace.export import BatchSpanProcessor
from opentelemetry.instrumentation.fastapi import FastAPIInstrumentor
from opentelemetry.instrumentation.httpx import HTTPXClientInstrumentor

# Configurar provider
provider = TracerProvider()
exporter = OTLPSpanExporter(endpoint="http://otel-collector:4317")
provider.add_span_processor(BatchSpanProcessor(exporter))
trace.set_tracer_provider(provider)

# Auto-instrumentar FastAPI e httpx (inclui chamadas de LLM)
FastAPIInstrumentor.instrument_app(app)
HTTPXClientInstrumentor().instrument()

# Criar span manual para operaÃ§Ãµes crÃ­ticas
tracer = trace.get_tracer(__name__)

async def process_user_request(user_id: str, query: str):
    with tracer.start_as_current_span("process_user_request") as span:
        span.set_attribute("user.id", user_id)
        span.set_attribute("query.length", len(query))
        
        with tracer.start_as_current_span("rag_retrieval"):
            context = await retrieve_rag_context(query)
            span.set_attribute("rag.chunks_retrieved", len(context))
        
        with tracer.start_as_current_span("llm_generation"):
            response = await call_llm(query, context)
            span.set_attribute("llm.tokens_used", response.usage.total_tokens)
        
        return response

# Resultado no Grafana: ver exatamente quanto tempo cada etapa levou
# "O RAG demorou 1.2s â€” 80% do tempo total. Otimizar o Ã­ndice vetorial."
```

---

## 4. SLOs e SLAs: Comprometer com Qualidade ðŸ“Š

```
SLI (Service Level Indicator): a mÃ©trica real
  â†’ LatÃªncia: P99 de response time
  â†’ Disponibilidade: % de requests com status < 500
  â†’ Error rate: % de requests que retornam erro

SLO (Service Level Objective): meta interna
  â†’ "99.5% de disponibilidade" (nosso objetivo)
  â†’ "P99 latÃªncia < 2 segundos"
  â†’ "Error rate < 0.1%"

SLA (Service Level Agreement): compromisso com cliente
  â†’ Normalmente mais conservador que SLO
  â†’ "99% de disponibilidade (ou crÃ©dito no prÃ³ximo mÃªs)"

ERROR BUDGET:
  â†’ SLO de 99.5% = 0.5% de budget para erros
  â†’ Em 30 dias: 0.5% de 30 * 24 * 60 min = 216 minutos de downtime "permitido"
  â†’ Se gastou o budget: congelar deploys, sÃ³ fixes crÃ­ticos
  â†’ Se sobrou budget: pode fazer deploys mais arriscados
```

---

## 5. Alertas Inteligentes (Sem Alert Fatigue) ðŸ””

```yaml
# prometheus_alerts.yml â€” Alertas que importam
groups:
  - name: critical_alerts
    rules:
      # Alerta CRÃTICO: serviÃ§o down
      - alert: ServiceDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "{{ $labels.job }} estÃ¡ DOWN"
          runbook: "https://notion.so/runbook/service-down"
          # AÃ§Ã£o: acordar alguÃ©m Ã s 3am se necessÃ¡rio
      
      # Alerta ALTO: error rate elevada
      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) / rate(http_requests_total[5m]) > 0.01
        for: 5m
        labels:
          severity: high
        annotations:
          summary: "Error rate > 1% nos Ãºltimos 5 minutos"
      
      # Alerta MÃ‰DIO: latÃªncia degradada
      - alert: HighLatency
        expr: histogram_quantile(0.99, rate(http_request_duration_seconds_bucket[5m])) > 2
        for: 10m
        labels:
          severity: medium
        annotations:
          summary: "P99 latÃªncia > 2s"

# alertmanager.yml â€” Roteamento de alertas
route:
  receiver: slack_critical
  routes:
    - match:
        severity: critical
      receiver: pagerduty        # acorda alguÃ©m
    - match:
        severity: high
      receiver: slack_urgent     # mensagem urgente
    - match:
        severity: medium
      receiver: slack_general    # canal de monitoramento
```

---

## Trigger desta Skill

- Ao colocar qualquer sistema em produÃ§Ã£o
- "Por que a aplicaÃ§Ã£o ficou lenta?"
- "Como sei quando algo quebrou antes do cliente reclamar?"
- "Quero criar SLAs para meu produto"
- Incident response: diagnosticar o que aconteceu
- RevisÃ£o trimestral de observabilidade e alertas

---
---