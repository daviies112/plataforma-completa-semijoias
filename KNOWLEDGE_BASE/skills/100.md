---
name: Mobile Intelligence Engine (iOS e Android do Zero ao Deploy)
skill_id: 66
description: Sistema completo para desenvolvimento mobile: Flutter (cross-platform), React Native, Swift/Kotlin, otimizaÃ§Ã£o para app stores, push notifications, deep links, testes em emuladores e anÃ¡lise de ASO.
---

# Mobile Intelligence Engine Protocol ðŸ“±

**Objetivo:** "Do wireframe ao app nas lojas, com zero fricÃ§Ã£o."

---

## 1. Stack Mobile do Antigravity ðŸ—ï¸

```
ESCOLHA DA STACK (decisÃ£o por contexto):

FLUTTER (recomendado para 90% dos casos):
â†’ Uma codebase â†’ iOS + Android + Web + Desktop
â†’ Performance nativa (nÃ£o WebView como RN antigo)
â†’ Ã“timo para: startups, apps de negÃ³cio, MVPs
â†’ Skill 63 (Polyglot Intelligence) cobre o Dart

REACT NATIVE (quando o time jÃ¡ Ã© JS/TS):
â†’ Reutiliza skills do React Weaver
â†’ Bridge nativa para performance
â†’ Ã“timo para: equipes JS-first, apps menos intensivos

SWIFT/KOTLIN NATIVO (para performance mÃ¡xima):
â†’ Quando: jogos, apps de hardware, performance crÃ­tica
â†’ Custo: 2 codebases separadas
â†’ Ã“timo para: apps premium, Apple ecosystem exclusivo
```

---

## 2. PadrÃµes de Arquitetura Mobile ðŸ›ï¸

```dart
// Flutter â€” Arquitetura Antigravity (Feature-First + Clean Architecture)

lib/
â”œâ”€â”€ core/
â”‚   â”œâ”€â”€ network/ (Dio + interceptors)
â”‚   â”œâ”€â”€ storage/ (Hive + SQLite)
â”‚   â””â”€â”€ theme/ (Design System)
â”œâ”€â”€ features/
â”‚   â”œâ”€â”€ auth/
â”‚   â”‚   â”œâ”€â”€ data/ (repositories, datasources)
â”‚   â”‚   â”œâ”€â”€ domain/ (entities, use_cases)
â”‚   â”‚   â””â”€â”€ presentation/ (screens, providers)
â”‚   â””â”€â”€ home/
â”œâ”€â”€ shared/
â”‚   â”œâ”€â”€ widgets/ (componentes reutilizÃ¡veis)
â”‚   â””â”€â”€ utils/

// REGRAS:
// 1. Telas nÃ£o acessam API diretamente (vai pelo use case)
// 2. State management: Riverpod (reativo, testÃ¡vel)
// 3. NavegaÃ§Ã£o: Go Router (deep links nativos)
// 4. Testes: 80%+ de cobertura nos use cases
```

---

## 3. App Store Optimization (ASO) ðŸª

```markdown
## ASO Checklist â€” LanÃ§amento

### App Store (iOS)
â–¡ TÃ­tulo: 30 chars mÃ¡x, keyword principal no inÃ­cio
â–¡ SubtÃ­tulo: 30 chars, segundo keyword mais importante
â–¡ Keywords field: 100 chars, sem repetir do tÃ­tulo
â–¡ Screenshots: 6+ mostrando benefÃ­cio, nÃ£o feature
â–¡ Preview video: 30s mostrando o app em uso real
â–¡ Reviews: responder 100% nos primeiros 3 meses

### Google Play
â–¡ TÃ­tulo: 50 chars, keyword principal
â–¡ Short description: 80 chars, CTA claro
â–¡ Full description: 4000 chars, keywords naturalmente distribuÃ­das
â–¡ Feature Graphic: 1024x500px, visual impactante
â–¡ Categoria correta (afeta descoberta massivamente)

### Ambas
â–¡ Rating target: 4.5+ (abaixo de 4.2 prejudica discovery)
â–¡ Install velocity no D+1 a D+7: crÃ­tico para ranking inicial
â–¡ PreÃ§os localizados (nÃ£o apenas converter USD)
â–¡ Permissions: solicitar apenas o essencial
```

---

## 4. Push Notifications e Deep Links ðŸ””

```dart
// Push Notifications via Firebase Cloud Messaging (FCM)
import 'package:firebase_messaging/firebase_messaging.dart';

class NotificationService {
  Future<void> initialize() async {
    // PermissÃ£o (iOS requer, Android 13+ requer)
    final settings = await FirebaseMessaging.instance.requestPermission();
    
    // Token para envio targeted
    final token = await FirebaseMessaging.instance.getToken();
    await saveTokenToBackend(token);
    
    // Handler para notificaÃ§Ãµes em foreground
    FirebaseMessaging.onMessage.listen((RemoteMessage message) {
      showInAppNotification(message);
    });
    
    // Handler para tap em notificaÃ§Ã£o (deep link)
    FirebaseMessaging.onMessageOpenedApp.listen((RemoteMessage message) {
      navigateToDeepLink(message.data['route']);
    });
  }
}

// Deep Links (Universal Links iOS / App Links Android)
// go_router configurado para responder a URLs
GoRoute(
  path: '/produto/:id',
  builder: (context, state) => ProdutoScreen(
    id: state.pathParameters['id']!
  ),
)
// Link: https://seuapp.com/produto/123 â†’ abre o app direto na tela certa
```

---

## 5. Testes em Emuladores e Dispositivos Reais ðŸ§ª

```bash
# Testar em mÃºltiplos dispositivos simultaneamente
flutter test integration_test/app_test.dart \
  --device-id emulator-5554 \  # Android API 34
  --device-id iPhone16Pro     # iOS 18

# Firebase Test Lab (testa em 100+ dispositivos reais)
gcloud firebase test android run \
  --type instrumentation \
  --app app-debug.apk \
  --test app-debug-androidTest.apk \
  --device model=Pixel9,version=34 \
  --device model=SamsungS24,version=34

# Screenshots automatizados para todas as resoluÃ§Ãµes
flutter test integration_test/screenshots_test.dart \
  --update-goldens
```

---

## Trigger desta Skill

- "Quero criar um app"
- "Desenvolva para iOS e Android"
- "Como faÃ§o push notification no app?"
- "Quero lanÃ§ar na App Store"
- "Meu app estÃ¡ sendo rejeitado pela App Store"
- ASO e anÃ¡lise de mÃ©tricas de app

---
---