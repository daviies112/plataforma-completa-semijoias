---
name: WhatsApp Anti-Ban Sentinel (Generative Compliance)
description: Protocol for monitoring WhatsApp policies, logging bans, and generatively adapting messaging strategies to minimize account blocks.
---

# WhatsApp Anti-Ban Sentinel Protocol ðŸ›¡ï¸ðŸ“±

**Objective:** "Zero Bans through Generative Intelligence."

## 1. The Ban Archive (Data Structure) ðŸ—„ï¸
*   **Database:** `whatsapp_ban_logs` (Supabase/JSON).
*   **Fields required for every ban:**
    *   `timestamp`, `instance_id`, `number_age` (days).
    *   `message_volume_24h`, `last_content_sent`.
    *   `delay_strategy_used`, `proxy_type`.
    *   `ban_reason_detected` (Spam, User Report, Pattern Recognition).

## 2. Generative Adaptation Engine ðŸ§ 
*   **Trigger:** Any new record in `whatsapp_ban_logs`.
*   **Strategy:**
    1.  **Root Cause Analysis:** Use LLM to analyze the log. "Was it the link? The volume? The lack of interaction?"
    2.  **Generative Patching:** Rewrite the `evolution_api_manager` templates.
        *   *Example:* If bans are occurring on "Links", switch to "Message + Button" or "Text-only first".
    3.  **Variable Injection:** Force higher levels of spintax and dynamic greetings for the next 48h.

## 3. Humanization Protocols ðŸ‘¤
*   **Typing Simulation:** Force random typing states (e.g., 2s to 12s) before sending.
*   **Non-Linear Delays:** Use Fibbonaci or random Gaussian delays between messages instead of static intervals (e.g., 60s -> 45s..120s).
*   **Inter-Message Warmup:** Send "safe" messages to trusted numbers (internal accounts) between marketing blasts to keep the account "healthy".

## 4. Meta Policy Monitor ðŸ“¡
*   **Detection:** Monitor WhatsApp Business API changelogs and "Anti-Ban communities" (GitHub/Telegram).
*   **Update:** If Meta implements new "Button Pattern Detection", the Sentinel automatically disables button nodes in `hybrid_architect_n8n` until a workaround is generated.

## 5. The Warm-up Routine ðŸŒ¡ï¸
*   For any new instance:
    *   **Week 1:** 5-10 messages/day (Personal threads).
    *   **Week 2:** 20-50 messages/day (Inbound reply simulation).
    *   **Week 3:** Gradual marketing injection.
*   **Rule:** If a number is "Cold", the Sentinel blocks mass-mailing workflows.
---