---
name: Advanced Observability (Ver Tudo que Acontece no Sistema)
skill_id: 76
description: AlÃ©m do Command Center (Arize Phoenix para LLMs), o sistema completo de observabilidade â€” Grafana + Prometheus para infra, OpenTelemetry para distributed tracing, SLOs/SLAs, error budgets e alertas inteligentes. Para sistemas que nÃ£o podem falhar.
---

# Advanced Observability Protocol ðŸ“Š

**Objetivo:** "Se algo vai falhar, saibamos antes do usuÃ¡rio."

---

## 1. Os 3 Pilares da Observabilidade

```
MÃ‰TRICAS (o quÃª): nÃºmeros agregados ao longo do tempo
â†’ Prometheus coleta + Grafana exibe
â†’ Ex: req/s, error rate, latÃªncia P99, CPU, memÃ³ria

LOGS (o quÃª aconteceu): eventos discretos com contexto
â†’ Loki (self-hosted) ou CloudWatch/Datadog
â†’ Ex: "UsuÃ¡rio X falhou ao pagar Ã s 14:32"

TRACES (por quÃª): jornada de uma requisiÃ§Ã£o pelo sistema
â†’ OpenTelemetry coleta + Jaeger ou Tempo exibe
â†’ Ex: uma chamada de API levou 800ms â€” 400ms foi no banco, onde?

REGRA: vocÃª precisa dos 3. MÃ©trica diz QUANDO, log diz O QUÃŠ, trace diz POR QUÃŠ.
```

---

## 2. Prometheus + Grafana: Setup Completo

```yaml
# docker-compose para stack de observabilidade
version: '3.8'
services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
  
  grafana:
    image: grafana/grafana:latest
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=senha_segura
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
  
  node-exporter:  # mÃ©tricas do servidor (CPU, memÃ³ria, disco)
    image: prom/node-exporter:latest
    ports:
      - "9100:9100"
  
  loki:  # logs
    image: grafana/loki:latest
    ports:
      - "3100:3100"
  
  promtail:  # coleta logs e envia para Loki
    image: grafana/promtail:latest
    volumes:
      - /var/log:/var/log
      - ./promtail-config.yml:/etc/promtail/config.yml
```

```python
# INSTRUMENTAR FASTAPI COM PROMETHEUS
from prometheus_fastapi_instrumentator import Instrumentator
from prometheus_client import Counter, Histogram, Gauge

# MÃ©tricas customizadas de negÃ³cio
workflows_executed = Counter(
    'antigravity_workflows_executed_total',
    'Total de workflows executados',
    ['status', 'workflow_name']
)

llm_cost = Counter(
    'antigravity_llm_cost_usd_total',
    'Custo total de LLM em USD',
    ['model', 'operation']
)

active_users = Gauge(
    'antigravity_active_users',
    'UsuÃ¡rios ativos nos Ãºltimos 5 minutos'
)

# Em cada execuÃ§Ã£o de workflow:
workflows_executed.labels(status='success', workflow_name='instagram_monitor').inc()
llm_cost.labels(model='claude-sonnet-4-6', operation='chat').inc(0.002)
```

---

## 3. SLOs (Service Level Objectives): Definir o que Ã© "Funcionar"

```yaml
# SLOs DO ANTIGRAVITY
slos:
  - name: "API LatÃªncia"
    objective: 99%           # 99% das requisiÃ§Ãµes abaixo do threshold
    threshold: 500ms         # P99 < 500ms
    window: 30d              # medido em janela de 30 dias
    error_budget: 1%         # 1% de "tempo para experimentar"
    
  - name: "API Disponibilidade"
    objective: 99.9%         # trÃªs noves
    metric: "1 - (erros 5xx / total requisiÃ§Ãµes)"
    window: 30d
    alert_burn_rate: 14.4x   # alertar se gastando budget 14x mais rÃ¡pido que normal
    
  - name: "Workflows Completados"
    objective: 99.5%
    metric: "workflows com status success / total executados"
    window: 7d

# ERROR BUDGET:
# Se SLO Ã© 99.9% em 30 dias â†’ error budget = 0.1% de 30 dias = 43.2 minutos
# Se vocÃª gastou 30 minutos de downtime este mÃªs â†’ sobraram 13 minutos
# Use o error budget para: deploys arriscados, experimentos, migraÃ§Ãµes
# Quando budget acaba: FREEZE em mudanÃ§as arriscadas atÃ© prÃ³ximo mÃªs
```

---

## 4. Alertas Inteligentes (sem alert fatigue)

```yaml
# alertmanager.yml â€” REGRAS DE ALERTA
groups:
  - name: "Antigravity Critical"
    rules:
      # CRÃTICO: responder em < 5 minutos
      - alert: APIDown
        expr: up{job="antigravity-api"} == 0
        for: 1m
        annotations:
          summary: "API estÃ¡ down!"
          runbook: "https://docs.antigravity.app/runbooks/api-down"
          
      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) / rate(http_requests_total[5m]) > 0.05
        for: 5m
        annotations:
          summary: "Error rate > 5% nos Ãºltimos 5 minutos"
          
      # AVISO: responder em < 1 hora
      - alert: HighLatency
        expr: histogram_quantile(0.99, rate(http_request_duration_seconds_bucket[5m])) > 1
        for: 10m
        annotations:
          severity: warning
          summary: "P99 de latÃªncia > 1 segundo"
          
      - alert: LLMCostSpike
        expr: increase(antigravity_llm_cost_usd_total[1h]) > 10
        annotations:
          summary: "Custo de LLM aumentou mais de $10 na Ãºltima hora"
          
      - alert: ErrorBudgetBurning
        expr: |
          (sum(rate(http_requests_total{status=~"5.."}[1h])) / sum(rate(http_requests_total[1h]))) 
          > (1 - 0.999) * 14.4
        for: 5m
        annotations:
          summary: "Queimando error budget 14x mais rÃ¡pido que o normal!"
```

---

## Trigger desta Skill

- "Meu sistema estÃ¡ lento mas nÃ£o sei onde"
- Antes de lanÃ§ar qualquer sistema em produÃ§Ã£o
- "Como configuro alertas para quando o sistema cair?"
- Ao escalar de MVP para produto com clientes pagantes
- Incidente em produÃ§Ã£o: usar traces para diagnÃ³stico rÃ¡pido

---
---