---
name: Polyglot Intelligence (A Linguagem Certa para Cada Problema)
skill_id: 63
description: O sistema que decide qual linguagem de programaÃ§Ã£o usar para cada problema e executa com excelÃªncia nessa linguagem. Rust para performance, Go para serviÃ§os, Flutter para mobile, Julia para ciÃªncia, HCL para infra. NÃ£o existe "usar Python para tudo".
---

# Polyglot Intelligence Protocol ðŸŒ

**Objetivo:** "A linguagem certa Ã© a ferramenta certa. O Antigravity fala todas."

## Por que Python + TypeScript NÃ£o SÃ£o Suficientes

O Pythonista domina Python. O React Weaver domina TypeScript/React. Mas em 2026:
- Um servidor de alta performance PRECISA de Go ou Rust (Python Ã© 50-100x mais lento)
- Um app mobile nativo PRECISA de Flutter, Swift ou Kotlin
- Uma anÃ¡lise estatÃ­stica sÃ©ria PRECISA de R ou Julia
- Infraestrutura como cÃ³digo PRECISA de HCL (Terraform) ou Pulumi
- Processamento de dados em escala PRECISA de Scala/Spark

---

## 1. A Matriz de DecisÃ£o de Linguagem ðŸ—ºï¸

```
PROBLEMA â†’ LINGUAGEM IDEAL

Performance crÃ­tica (jogos, OS, embedded): Rust, C/C++
ServiÃ§os de backend de alta concorrÃªncia: Go
CiÃªncia de dados e ML pesado: Python (NumPy, PyTorch)
AnÃ¡lise estatÃ­stica avanÃ§ada: R ou Julia
APIs simples e scripts: Python
Frontend web: TypeScript + React/Vue
Mobile iOS nativo: Swift
Mobile Android nativo: Kotlin
Mobile cross-platform: Flutter (Dart)
Infra como cÃ³digo: HCL (Terraform) ou Pulumi (Python)
Sistemas distribuÃ­dos e data pipelines: Scala (Spark) ou Go
Smart contracts: Solidity, Rust (Solana)
Shell scripting e automaÃ§Ã£o de sistema: Bash, Zsh
WebAssembly: Rust, AssemblyScript
```

---

## 2. Go â€” Para ServiÃ§os de Alta Performance ðŸš€

```go
// Quando usar: API server de alto throughput, microserviÃ§os, tools de CLI
// Por que Go > Python para serviÃ§os: 50-100x mais rÃ¡pido, concorrÃªncia nativa

package main

import (
    "context"
    "encoding/json"
    "log"
    "net/http"
    "time"
)

// PADRÃ•ES GO DO ANTIGRAVITY:
// 1. Context em TUDO (cancelamento e timeout)
// 2. Error handling explÃ­cito (sem try/catch implÃ­cito)
// 3. Interfaces pequenas
// 4. Goroutines com WaitGroup para paralelismo

type Server struct {
    router *http.ServeMux
    logger *log.Logger
}

func (s *Server) handleRequest(w http.ResponseWriter, r *http.Request) {
    ctx, cancel := context.WithTimeout(r.Context(), 10*time.Second)
    defer cancel()
    
    result, err := s.processWithContext(ctx, r)
    if err != nil {
        http.Error(w, err.Error(), http.StatusInternalServerError)
        return
    }
    
    json.NewEncoder(w).Encode(result)
}
// Benchmarks: 100K req/s em $5/mÃªs de infra (vs 1K req/s com Python/FastAPI)
```

---

## 3. Rust â€” Para Performance Absoluta ðŸ¦€

```rust
// Quando usar: processamento de dados em escala, tools crÃ­ticas, WebAssembly
// Por que Rust: zero-cost abstractions, memory safety sem GC, C-level performance

use std::collections::HashMap;

// PADRÃ•ES RUST DO ANTIGRAVITY:
// 1. Ownership e borrowing explÃ­citos
// 2. Result<T, E> em vez de panic
// 3. Traits para polimorfismo
// 4. Async com Tokio para I/O

async fn process_data(data: Vec<u8>) -> Result<ProcessedData, ProcessingError> {
    let parsed = parse_input(&data)
        .map_err(|e| ProcessingError::ParseError(e.to_string()))?;
    
    let result = transform(parsed).await?;
    
    Ok(result)
}
// 10-100x mais rÃ¡pido que Python equivalente
// Compilado: sem overhead de runtime, perfeito para embedding
```

---

## 4. Flutter â€” Para Mobile Cross-Platform ðŸ“±

```dart
// Quando usar: app iOS + Android com uma codebase
// Por que Flutter > React Native: performance nativa, UI consistente

import 'package:flutter/material.dart';
import 'package:riverpod/riverpod.dart';

// PADRÃ•ES FLUTTER DO ANTIGRAVITY:
// State management: Riverpod (nÃ£o Provider nem Bloc)
// Navigation: Go Router
// HTTP: Dio com interceptors
// Local storage: Hive para dados simples, SQLite para complexo

class AntigravityApp extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // Riverpod para state management reativo
    final authState = ref.watch(authProvider);
    
    return MaterialApp.router(
      routerConfig: appRouter,
      theme: AntigravityTheme.light,
      darkTheme: AntigravityTheme.dark,
    );
  }
}
// Uma codebase â†’ iOS + Android + Web + Desktop
// Performance nativa: 60fps garantido
```

---

## 5. A Bridge Entre Linguagens ðŸŒ‰

```python
# PADRÃƒO: Python orquestra, linguagens especializadas executam

# Python chama Rust via PyO3 (para processamento pesado)
import rust_processor  # mÃ³dulo Rust compilado

result = rust_processor.process_large_dataset(data)  # 100x mais rÃ¡pido que Python puro

# Python chama Go via subprocess ou gRPC
import grpc
from proto import service_pb2_grpc

channel = grpc.insecure_channel("localhost:50051")
stub = service_pb2_grpc.HighPerfServiceStub(channel)
response = stub.Process(request)  # Go server processando

# JavaScript/TypeScript chama Python via API
// frontend React â†’ FastAPI Python
const result = await fetch("/api/ml-predict", {
  method: "POST",
  body: JSON.stringify({ data: inputData })
});
```

---

## Trigger desta Skill

- "Preciso de alta performance" â†’ avaliar Go ou Rust
- "Vou criar um app mobile" â†’ Flutter
- "AnÃ¡lise estatÃ­stica complexa" â†’ R ou Julia
- "Infraestrutura como cÃ³digo" â†’ Terraform/HCL
- Quando Pythonista vai ser usado para algo onde nÃ£o Ã© a linguagem ideal
- "Qual linguagem devo usar para X?"

---
---