---
name: API Design & Documentation (APIs que Desenvolvedores Amam Usar)
skill_id: 73
description: O sistema completo para projetar, documentar e versionar APIs â€” OpenAPI spec, REST best practices, webhooks, rate limiting, SDKs, e documentaÃ§Ã£o interativa que elimina o support ticket. A diferenÃ§a entre uma API que frustra e uma que encanta.
---

# API Design & Documentation Protocol ðŸ”Œ

**Objetivo:** "Uma boa API Ã© a melhor documentaÃ§Ã£o que vocÃª pode escrever."

## Por que Esta Skill Era NecessÃ¡ria

O Full-Stack Architect desenha sistemas. O Pythonista escreve FastAPI. Mas nenhum deles tinha o protocolo para: decidir o design da API (REST? GraphQL? RPC?), versionar sem quebrar clientes, gerar SDKs automaticamente, escrever documentaÃ§Ã£o que realmente funciona, ou implementar webhooks robustos.

Uma API mal desenhada Ã© dÃ­vida tÃ©cnica permanente. Uma boa API Ã© um produto em si â€” e em 2026, APIs sÃ£o a forma como sistemas de IA se conectam.

---

## 1. REST vs GraphQL vs RPC: A DecisÃ£o Certa ðŸ¤”

```
USE REST QUANDO:
â†’ CRUD simples sobre recursos (users, products, orders)
â†’ Cacheabilidade Ã© importante (CDN pode cachear GET requests)
â†’ Clientes variados (mobile, web, terceiros) com necessidades diferentes
â†’ Time tem experiÃªncia com REST
â†’ PadrÃ£o: GET /users/{id}, POST /orders, PATCH /products/{id}

USE GRAPHQL QUANDO:
â†’ Frontend precisa de dados muito variÃ¡veis (dashboard com muitos widgets)
â†’ MÃºltiplos clientes com necessidades muito diferentes (mobile vs web)
â†’ Reduzir over-fetching e under-fetching Ã© crÃ­tico
â†’ VocÃª tem um Ãºnico backend servindo muitos tipos de cliente
â†’ Cuidado: complexidade maior, caching mais difÃ­cil

USE gRPC (RPC) QUANDO:
â†’ ComunicaÃ§Ã£o inter-serviÃ§os (microserviÃ§os internos)
â†’ Performance crÃ­tica (serializaÃ§Ã£o com Protocol Buffers)
â†’ Streaming bidirecional necessÃ¡rio
â†’ NÃ£o para APIs pÃºblicas voltadas a desenvolvedores externos

REGRA PARA O ANTIGRAVITY:
â†’ APIs pÃºblicas: REST
â†’ APIs internas entre serviÃ§os: gRPC se performance crÃ­tica, REST se simplicidade prioridade
â†’ Dashboard com muitos dados variÃ¡veis: GraphQL
```

---

## 2. O Design REST PadrÃ£o-Ouro ðŸ†

```
NOMENCLATURA DE ROTAS:
â†’ Recursos no plural: /users, /orders (nÃ£o /user, /getUser)
â†’ Hierarquia reflete relaÃ§Ã£o: /users/{userId}/orders
â†’ Snake_case nas responses, kebab-case nas URLs
â†’ Sem verbos nas rotas: /orders (nÃ£o /getOrders, nÃ£o /createOrder)
â†’ Verbo HTTP expressa a aÃ§Ã£o:
  GET    â†’ Leitura (idempotente, cacheÃ¡vel)
  POST   â†’ CriaÃ§Ã£o (nÃ£o idempotente)
  PUT    â†’ SubstituiÃ§Ã£o completa (idempotente)
  PATCH  â†’ AtualizaÃ§Ã£o parcial
  DELETE â†’ RemoÃ§Ã£o (idempotente)

ESTRUTURA DE RESPONSE PADRÃƒO:
{
  "data": { ... },           // o recurso/lista em si
  "meta": {                  // informaÃ§Ãµes extras
    "total": 1250,
    "page": 1,
    "per_page": 25
  },
  "links": {                 // navegaÃ§Ã£o (HATEOAS light)
    "self": "/users?page=1",
    "next": "/users?page=2"
  }
}

ESTRUTURA DE ERROR PADRÃƒO:
{
  "error": {
    "code": "VALIDATION_ERROR",           // cÃ³digo machine-readable
    "message": "Email is required",       // mensagem human-readable
    "field": "email",                     // campo especÃ­fico se aplicÃ¡vel
    "docs_url": "https://api.seusite.com/docs/errors#VALIDATION_ERROR"
  }
}

HTTP STATUS CODES CORRETOS:
200 OK           â†’ GET/PATCH/PUT bem-sucedido
201 Created      â†’ POST bem-sucedido (incluir Location header)
204 No Content   â†’ DELETE bem-sucedido
400 Bad Request  â†’ Dados invÃ¡lidos (validaÃ§Ã£o falhou)
401 Unauthorized â†’ NÃ£o autenticado (sem token ou token invÃ¡lido)
403 Forbidden    â†’ Autenticado mas sem permissÃ£o
404 Not Found    â†’ Recurso nÃ£o existe
409 Conflict     â†’ Conflito (email jÃ¡ cadastrado, etc.)
422 Unprocessable â†’ Semanticamente errado (entidade nÃ£o processÃ¡vel)
429 Too Many Requests â†’ Rate limit atingido
500 Internal Error â†’ Erro do servidor (nunca expor detalhes)
```

---

## 3. OpenAPI Spec: A DocumentaÃ§Ã£o que Se Torna CÃ³digo ðŸ“„

```yaml
# openapi.yaml â€” documento central que gera tudo
openapi: 3.1.0
info:
  title: Antigravity API
  version: "1.0"
  description: |
    API REST para integraÃ§Ã£o com o sistema Antigravity.
    
    ## AutenticaÃ§Ã£o
    Todas as rotas requerem Bearer token no header Authorization.
    
    ## Rate Limits
    - Free tier: 100 requests/hora
    - Pro tier: 10.000 requests/hora
    
    ## Versionamento
    VersÃ£o atual: v1. VersÃµes antigas suportadas por 12 meses apÃ³s deprecaÃ§Ã£o.
  contact:
    email: api@seusite.com
  
servers:
  - url: https://api.seusite.com/v1
    description: ProduÃ§Ã£o
  - url: http://localhost:3000/v1
    description: Desenvolvimento

security:
  - bearerAuth: []

paths:
  /users/{userId}:
    get:
      summary: Buscar usuÃ¡rio por ID
      description: Retorna dados completos de um usuÃ¡rio especÃ­fico.
      tags: [Users]
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          example: "123e4567-e89b-12d3-a456-426614174000"
      responses:
        "200":
          description: UsuÃ¡rio encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
              example:
                data:
                  id: "123e4567-e89b-12d3-a456-426614174000"
                  name: "JoÃ£o Silva"
                  email: "joao@example.com"
                  created_at: "2026-01-15T10:30:00Z"
        "404":
          $ref: '#/components/responses/NotFound'
        "401":
          $ref: '#/components/responses/Unauthorized'

components:
  schemas:
    User:
      type: object
      required: [id, name, email, created_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 100
        email:
          type: string
          format: email
        created_at:
          type: string
          format: date-time
  
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
```

```bash
# DO OpenAPI spec, gerar automaticamente:

# DocumentaÃ§Ã£o interativa (Scalar â€” mais moderna que Swagger UI)
npx @scalar/api-reference --input openapi.yaml --port 9000

# SDK Python
openapi-generator-cli generate \
  -i openapi.yaml \
  -g python \
  -o ./sdks/python \
  --additional-properties=packageName=antigravity_sdk

# SDK TypeScript
openapi-generator-cli generate \
  -i openapi.yaml \
  -g typescript-fetch \
  -o ./sdks/typescript

# Testes automatizados (Schemathesis â€” fuzzing contra a spec)
schemathesis run openapi.yaml --url http://localhost:3000
```

---

## 4. Rate Limiting: Proteger sem Frustrar ðŸ›¡ï¸

```python
# ImplementaÃ§Ã£o com Redis (FastAPI + slowapi)
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address
from slowapi.errors import RateLimitExceeded
from fastapi import FastAPI, Request

limiter = Limiter(key_func=get_remote_address)
app = FastAPI()
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)

# Rate limits por tier (lidos do JWT do usuÃ¡rio)
def get_user_limit(request: Request) -> str:
    tier = request.state.user.tier  # "free" | "pro" | "enterprise"
    limits = {
        "free": "100/hour",
        "pro": "10000/hour", 
        "enterprise": "1000000/hour"
    }
    return limits.get(tier, "100/hour")

@app.get("/api/v1/data")
@limiter.limit(get_user_limit)
async def get_data(request: Request):
    return {"data": "..."}

# Headers de resposta (OBRIGATÃ“RIOS para boa DX):
# X-RateLimit-Limit: 100
# X-RateLimit-Remaining: 87
# X-RateLimit-Reset: 1708966800  (Unix timestamp do reset)

# Quando rate limit Ã© atingido (429):
{
  "error": {
    "code": "RATE_LIMIT_EXCEEDED",
    "message": "Rate limit exceeded. Try again in 47 seconds.",
    "retry_after": 47
  }
}
```

---

## 5. Webhooks: Eventos em Tempo Real para IntegraÃ§Ãµes ðŸ””

```python
# Webhook: notificar sistemas externos quando algo acontece
# Ã‰ o oposto de polling: em vez de "tem alguma novidade?", vocÃª avisa

# ENVIANDO Webhooks (vocÃª avisa os clientes):
import httpx
import hmac
import hashlib
import json

async def dispatch_webhook(
    endpoint_url: str,
    secret: str,
    event: dict
):
    payload = json.dumps(event)
    
    # Assinar o payload com HMAC-SHA256 (seguranÃ§a crucial)
    signature = hmac.new(
        secret.encode(),
        payload.encode(),
        hashlib.sha256
    ).hexdigest()
    
    async with httpx.AsyncClient() as client:
        response = await client.post(
            endpoint_url,
            content=payload,
            headers={
                "Content-Type": "application/json",
                "X-Webhook-Signature": f"sha256={signature}",
                "X-Webhook-ID": str(uuid4()),  # idempotency
                "X-Webhook-Timestamp": str(int(time.time()))
            },
            timeout=10.0
        )
    
    # Retry se falhou (com exponential backoff)
    if response.status_code >= 500:
        await retry_webhook_with_backoff(endpoint_url, payload, signature)
    
    return response

# Eventos a expor via webhook:
WEBHOOK_EVENTS = {
    "user.created",
    "user.deleted", 
    "order.created",
    "order.status_changed",
    "payment.succeeded",
    "payment.failed"
}

# RECEBENDO Webhooks de terceiros (Stripe, GitHub, etc.):
@app.post("/webhooks/stripe")
async def stripe_webhook(request: Request):
    payload = await request.body()
    sig_header = request.headers.get("stripe-signature")
    
    # SEMPRE verificar assinatura antes de processar
    try:
        event = stripe.Webhook.construct_event(
            payload, sig_header, os.getenv("STRIPE_WEBHOOK_SECRET")
        )
    except stripe.error.SignatureVerificationError:
        raise HTTPException(status_code=400, detail="Invalid signature")
    
    # Processar evento de forma idempotente
    # (webhooks podem chegar duplicados)
    if await is_already_processed(event.id):
        return {"status": "already_processed"}
    
    await process_stripe_event(event)
    await mark_as_processed(event.id)
    
    return {"status": "ok"}
```

---

## 6. Versionamento de API: Sem Quebrar Clientes ðŸ“…

```
ESTRATÃ‰GIAS DE VERSIONAMENTO:

1. URL Versioning (RECOMENDADO para APIs pÃºblicas)
   /v1/users, /v2/users
   âœ“ Claro, explÃ­cito, fÃ¡cil de entender
   âœ“ Pode ter diferentes versÃµes rodando simultÃ¢neas
   
2. Header Versioning
   Accept: application/vnd.api+json;version=1
   âœ“ URL fica limpa
   âœ— Menos descobrÃ­vel, mais complexo de cachear

POLÃTICA DE DEPRECAÃ‡ÃƒO (comunicar claramente):
â†’ Anunciar deprecaÃ§Ã£o 6 meses antes
â†’ Adicionar header de aviso: Deprecation: true, Sunset: [date]
â†’ Email para todos os usuÃ¡rios da versÃ£o antiga
â†’ Manter versÃ£o antiga por 12 meses apÃ³s deprecaÃ§Ã£o
â†’ Documentar migration guide: o que mudou, como migrar

BREAKING vs NON-BREAKING CHANGES:

NON-BREAKING (pode fazer sem nova versÃ£o):
â†’ Adicionar novo endpoint
â†’ Adicionar campo opcional no response
â†’ Adicionar novo valor num enum
â†’ Melhorar mensagens de erro

BREAKING (requer nova versÃ£o):
â†’ Remover endpoint ou campo
â†’ Mudar tipo de campo (string â†’ number)
â†’ Mudar autenticaÃ§Ã£o
â†’ Mudar semÃ¢ntica de um endpoint
â†’ Remover valor de enum
```

---

## 7. DocumentaÃ§Ã£o que Developers Realmente Usam ðŸ“–

```markdown
## Checklist de DocumentaÃ§Ã£o

### Estrutura (baseada no que funciona no Stripe, Twilio, Notion)
â–¡ Getting Started em < 5 minutos (primeiro sucesso rÃ¡pido)
â–¡ AutenticaÃ§Ã£o: como conseguir API key + como usar
â–¡ Quickstart com cÃ³digo real (copy-paste que funciona)
â–¡ Reference completa de endpoints (gerada do OpenAPI spec)
â–¡ Guias por caso de uso ("Como fazer webhooks", "Como paginar")
â–¡ Exemplos em mÃºltiplas linguagens (Python, Node, curl)
â–¡ Changelog com o que mudou em cada versÃ£o
â–¡ Status page com uptime histÃ³rico

### Qualidade
â–¡ Todos os exemplos sÃ£o testados e funcionam
â–¡ Nenhum endpoint sem exemplo de request e response
â–¡ Erros documentados com soluÃ§Ãµes
â–¡ SDKs disponÃ­veis (gerados do OpenAPI spec)
â–¡ Playground interativo (Scalar ou Swagger UI)

### SEO da DocumentaÃ§Ã£o
â–¡ Cada endpoint tem URL prÃ³pria e indexÃ¡vel
â–¡ Titles e descriptions otimizadas
â–¡ GEO: documentaÃ§Ã£o clara e estruturada para IAs citarem
```

---

## Trigger desta Skill

- Ao projetar qualquer nova API
- "Como versiono a API sem quebrar os clientes?"
- "Como implemento webhooks?"
- "Como documento esta API?"
- "Qual o rate limit ideal para meu produto?"
- Ao revisar API existente: auditoria de design e documentaÃ§Ã£o
- Quando parceiros reclamam que a integraÃ§Ã£o Ã© complexa
---