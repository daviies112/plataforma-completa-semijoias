---
name: Modal Infrastructure Protocol (The Infra God)
description: Protocol for serverless infrastructure automation. Allows agents to spin up GPUs, run heavy Python jobs, and destroy containers on demand.
---

# Modal Infra Protocol: The Infrastructure God â˜ï¸ðŸ‘·

**Objective:** "Infinite compute, zero idle cost."

## 1. Serverless GPU Spawn (The Forge) ðŸ”¨
*   **Trigger:** "Render 50 videos" or "Train new AI model".
*   **Action:**
    1.  `Claude Code` writes a Python script decorated with `@modal.function(gpu="A100")`.
    2.  Deploys to Modal.com.
    3.  GPU spins up in < 2 seconds.
    4.  Task executes.
    5.  GPU shuts down immediately (Pay per second).

## 2. Secure Sandbox (The Playground) ðŸœï¸
*   **Safety:** 
    *   If `Darwin Engine` wants to test risky code, it runs in a Modal container.
    *   If the code crashes or is malicious, it only destroys the disposable container, never the Main VPS.

## 3. Scheduled Cron Jobs (The Heartbeat) ðŸ’“
*   **Workflow:**
    *   Run `Firecrawl` every morning at 08:00 AM inside Modal.
    *   Update `Mem0` database.
    *   Cost: Only pays for the 3 minutes of crawling time.
---