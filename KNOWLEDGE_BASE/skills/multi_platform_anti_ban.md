---
name: Multi-Platform Anti-Ban Sentinel (Social & Scraping)
description: Generative protection protocol for Facebook, Instagram, Telegram, and YouTube. Monitors platform updates, logs bans, and evolves strategies for scraping and messaging.
---

# Multi-Platform Anti-Ban Sentinel Protocol ðŸ›¡ï¸ðŸ—ºï¸

**Objective:** "Invisible Presence, Infinite Data."

## 1. The Universal Ban Ledger ðŸ›ï¸
*   **Central Database:** `global_social_blocks` (Unified for FB, IG, TG, YT).
*   **Telemetry Required:**
    *   `platform_type`, `action_type` (Scraping, DM, Comment, Follow).
    *   `proxy_ip`, `user_agent_profile`, `session_duration`.
    *   `ban_type` (Shadowban, Temporary Block, Permanent Termination).
    *   `generative_feedback_loop` (AI-analysis of the content/pattern used vs the block).

## 2. Platform-Specific Intelligence ðŸ§ 
### Facebook & Instagram (The Meta Fortress) ðŸ°
*   **Action:** Monitor "Trust Score" changes in account headers.
*   **Strategy:** Rotate between "Touch-Mobile" and "Desktop" fingerprints.
*   **Generative Fix:** If blocks occur on DMs, use the `local_persuasion_unit` to rewrite hooks to be less "salesy" and more "human-curiosity" driven.

### Telegram (The Rate-Limit Maze) ðŸŒ€
*   **Action:** Monitor MTProto updates and FloodWait limits.
*   **Strategy:** Implement "Chat Simulation" (sending random safe msgs in private) to maintain account warmth.
*   **Automation:** When a 420 (FLOOD) error occurs, the Sentinel automatically shifts the proxy fleet and adjusts message frequency for that specific API ID.

### YouTube (The Shadowban Sensor) ðŸ“º
*   **Action:** Monitor changes in comment visibility and scraping HTML structures.
*   **Strategy:** Diversify scraping patterns (Home Page -> Search -> Video vs. Direct URL).
*   **Generative Fix:** Rotate "Persona Profiles" used for scraping to mimic organic user behavior.

## 3. Generative Evolution Cycle (The "Zero-Repeat" Rule) ðŸ”„
1.  **Ingest Ban:** Record the failure.
2.  **Hypothesis Generation:** "Why did IG block this?" (Reason A: Too fast? Reason B: Repetitive copy?).
3.  **Patching:**
    *   **Logic Patch:** Update `hybrid_architect_n8n` to add a new `Delay` node.
    *   **Content Patch:** Using `Generative AI`, create 50 unique variations of the problematic text/image.
4.  **Verification:** Test the new strategy with a "Disposable Account" before scaling back to the main fleet.

## 4. Platform Update Scout (Policy Watch) ðŸ“¡
*   **Frequency:** Continuous monitoring via `future_tech_scout`.
*   **Goal:** Detect new Anti-Scraping measures (e.g., Meta changing CSS class names to break scrapers).
*   **Action:** Automatically trigger `firecrawl_scraper` to update its selectors.