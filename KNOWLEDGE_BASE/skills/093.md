---
name: Video Intelligence Studio (Criar, Editar e Extrair InteligÃªncia de VÃ­deo)
skill_id: 62
description: O sistema completo para trabalhar com vÃ­deo: criar Reels e TikToks, editar conteÃºdo existente, extrair clips virais de vÃ­deos longos, transcrever com timestamps, e gerar legendas animadas. A camada de vÃ­deo que faltava.
---

# Video Intelligence Studio Protocol ðŸŽ¬

**Objetivo:** "VÃ­deo Ã© o formato que domina 2026. O Antigravity domina vÃ­deo."

## Por que Esta Skill Era Urgente

O Multimodal Voice processa ÃUDIO (entrada e saÃ­da de voz). O External Design Arsenal simula Figma/Firefly para imagens estÃ¡ticas. Mas vÃ­deo Ã© diferente â€” e em 2026, vÃ­deo Ã© 82% de todo o trÃ¡fego da internet.

NÃ£o existia nenhuma skill para:
- Receber um vÃ­deo de 1 hora e extrair os 5 melhores clips de 30 segundos
- Criar um Reels com cortes automÃ¡ticos no beat da mÃºsica
- Adicionar legendas animadas sincronizadas automaticamente
- Gerar um vÃ­deo do zero a partir de um roteiro

---

## 1. As 4 Capacidades do Video Studio ðŸŽ¯

### A. ExtraÃ§Ã£o de Clips (Opus Clip-style)
```python
# Long-form video â†’ viral short clips
def extract_viral_clips(
    video_path: str,
    target_duration: int = 30,  # segundos
    platform: str = "instagram_reels"
) -> list[VideoClip]:
    
    # 1. Transcrever o vÃ­deo completo
    transcript = transcribe_video(video_path)
    
    # 2. LLM analisa: quais momentos sÃ£o mais interessantes?
    hooks = identify_hook_moments(transcript, video_path)
    # CritÃ©rios: laughter, "a-ha" moments, quotable statements, visual peaks
    
    # 3. Para cada momento identificado:
    clips = []
    for moment in hooks.top_10:
        clip = extract_clip(
            video=video_path,
            start=moment.start - 2,  # 2s antes para contexto
            end=moment.start + target_duration,
            add_captions=True,
            optimize_for=platform
        )
        clips.append(clip)
    
    # 4. Ranquear por viral potential
    return rank_by_viral_potential(clips)
```

### B. CriaÃ§Ã£o de VÃ­deo do Roteiro
```python
def create_video_from_script(
    script: str,
    style: str = "talking_head",  # ou "slides", "screencast", "b_roll"
    voice: str = "professional_pt_br",
    platform: str = "tiktok"
) -> Video:
    
    # 1. Adaptar roteiro para o formato da plataforma
    optimized_script = adapt_script_for_platform(script, platform)
    
    # 2. Gerar narraÃ§Ã£o com ElevenLabs (via Multimodal Voice skill)
    audio = generate_voice(optimized_script, voice=voice)
    
    # 3. Gerar ou selecionar visuais (via External Design Arsenal + image gen)
    visuals = generate_visuals(optimized_script)
    
    # 4. Montar vÃ­deo: sincronizar Ã¡udio + visuais + cortes
    video = assemble_video(
        audio=audio,
        visuals=visuals,
        cut_on_beat=True,
        add_animated_captions=True
    )
    
    # 5. Otimizar para a plataforma (aspect ratio, duration, compression)
    return optimize_for_platform(video, platform)
```

### C. TranscriÃ§Ã£o Inteligente + Timestamps
```python
def transcribe_and_timestamp(video_path: str) -> Transcript:
    """
    Usa Whisper para transcriÃ§Ã£o com timestamps precisos.
    Ãštil para: podcasts, entrevistas, aulas, webinars.
    """
    return {
        "full_text": "...",
        "chapters": [
            {"start": "00:00", "end": "05:30", "title": "IntroduÃ§Ã£o"},
            {"start": "05:30", "end": "18:00", "title": "O problema"}
        ],
        "key_quotes": ["Frase mais importante 1", "Frase mais importante 2"],
        "summary": "Resumo em 3 parÃ¡grafos",
        "action_items": ["Item 1", "Item 2"],
        "srt_file": "/tmp/video.srt"  # para upload no YouTube
    }
```

### D. EdiÃ§Ã£o Automatizada
```python
def auto_edit_video(
    raw_video: str,
    remove_silences: bool = True,
    add_b_roll: bool = True,
    style: str = "dynamic"  # ou "corporate", "minimal", "viral"
) -> Video:
    """
    EdiÃ§Ã£o automÃ¡tica que transforma vÃ­deo bruto em produÃ§Ã£o profissional:
    - Remove silÃªncios e hesitaÃ§Ãµes ("Ã£Ã£", "nÃ©", "tipo")
    - Ajusta ritmo de cortes
    - Adiciona legendas animadas
    - Adiciona B-roll quando disponÃ­vel
    - Normaliza Ã¡udio
    - Color grade consistente
    """
```

---

## 2. Stack de Ferramentas de VÃ­deo ðŸ”§

```
FERRAMENTAS INTEGRADAS:

TranscriÃ§Ã£o: OpenAI Whisper (local, gratuito, melhor qualidade)
EdiÃ§Ã£o programÃ¡tica: FFmpeg (open source, sem limites)
GeraÃ§Ã£o de voz: ElevenLabs API (via Multimodal Voice skill)
Legendas animadas: AssemblyAI + custom renderer
AnÃ¡lise de viral potential: Claude Vision + Gemini (analysis)
Color grading: FFmpeg filtros + LUTs prÃ©-definidos
Background removal: rembg (Python, local)
GeraÃ§Ã£o de thumbnail: via External Design Arsenal
```

---

## 3. Fluxo: Video para Redes Sociais ðŸ“±

```
INPUT: GravaÃ§Ã£o bruta de 20 minutos (ex: podcast, live, aula)

OUTPUT AUTOMÃTICO:
1. TikTok #1 (30s): Melhor momento + legenda animada + mÃºsica trending
2. TikTok #2 (30s): Segunda melhor cena
3. Instagram Reels (60s): VersÃ£o mais longa do melhor momento
4. YouTube Shorts (60s): Com tÃ­tulo e thumbnail otimizados
5. LinkedIn Video (2min): VersÃ£o mais corporativa/educacional
6. TranscriÃ§Ã£o completa para blog post
7. 5 tweets/posts baseados nas frases mais impactantes

Tempo estimado: 15-20 minutos com o stack automatizado
Tempo sem a skill: 2-3 horas de ediÃ§Ã£o manual
```

---

## Trigger desta Skill

- Qualquer pedido envolvendo vÃ­deo
- "Corte este vÃ­deo em clips para TikTok"
- "Transcreva esta reuniÃ£o com timestamps"
- "Crie um Reels sobre [tema]"
- "Edite este vÃ­deo para remover os silÃªncios"
- Quando Social Media Intelligence detecta que vÃ­deo seria o formato ideal

---
---