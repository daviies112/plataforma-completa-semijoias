---
name: n8n Master Architect (A Espinha Dorsal do Antigravity)
skill_id: 68
description: O manual completo para construir, escalar e manter automaÃ§Ãµes de alto nÃ­vel no n8n. Cobre os 4 padrÃµes agenticos de 2026, memÃ³ria avanÃ§ada, MCP server trigger, error handling profissional, sub-workflows reutilizÃ¡veis e deploy seguro. A ferramenta mais usada do stack finalmente tem sua skill.
---

# n8n Master Architect Protocol âš¡

**Objetivo:** "O n8n nÃ£o Ã© uma ferramenta de automaÃ§Ã£o. Ã‰ o sistema nervoso do Antigravity."

## Por que Esta Era a Lacuna Mais CrÃ­tica

Em 2026, n8n integrations evoluÃ­ram para um sofisticado sistema nervoso para negÃ³cios â€” nÃ£o construÃ­mos mais "flows", construÃ­mos "times" de entidades digitais que pensam, delegam e se recuperam de erros autonomamente. Apesar disso, nÃ£o existia nenhuma skill dedicada ao n8n â€” a ferramenta que aparece em quase toda skill do Antigravity como infraestrutura central.

---

## 1. Os 4 PadrÃµes Agenticos Fundamentais ðŸ—ï¸

Existem 4 padrÃµes-chave para workflows agenticos no n8n, identificados atravÃ©s de uso extensivo da ferramenta.

### PadrÃ£o 1 â€” Chained Requests (SequÃªncia Predefinida)
```
QUANDO USAR: SÃ©rie de comandos a modelos diferentes em ordem especÃ­fica
EXEMPLO: Ãudio â†’ TranscriÃ§Ã£o (Whisper) â†’ Resumo (Claude) â†’ Banco de Dados

ESTRUTURA n8n:
[Chat Trigger] â†’ [Whisper Node] â†’ [Claude Node] â†’ [Supabase Node]

REGRAS:
â†’ Cada node processa e passa para o prÃ³ximo
â†’ Pode misturar diferentes LLMs e serviÃ§os em cada etapa
â†’ Standard nodes (HTTP, Transform) podem aparecer em qualquer ponto
â†’ Ideal quando os passos sÃ£o previsÃ­veis e determinÃ­sticos
```

### PadrÃ£o 2 â€” Single Agent com Estado
```
QUANDO USAR: Um agente mantÃ©m estado e toma decisÃµes ao longo de todo o workflow
EXEMPLO: Agente de suporte que lembra o histÃ³rico da conversa

ESTRUTURA n8n:
[Chat Trigger] â†’ [AI Agent Node] â†’ [Memory Buffer Node] â†—
                       â†“
               [Tools: Supabase, HTTP, Code]

MEMÃ“RIA DISPONÃVEL EM 2026:
â†’ Redis Chat Memory: sub-milissegundo, ideal para voz
â†’ Postgres/Supabase Vector Memory: RAG de longo prazo
â†’ Simple Window Memory: multi-turn rÃ¡pido e contextual

CONFIGURAÃ‡ÃƒO DO AI AGENT NODE:
- System Prompt: define quem o agente Ã© e o que pode fazer
- Memory: escolher o tipo certo para o caso de uso
- Tools: o que o agente pode usar (HTTP, DB, outros workflows)
- Model: usar Intelligence Router para escolher o modelo certo
```

### PadrÃ£o 3 â€” Multi-Agent com Gatekeeper (Controle Centralizado)
```
QUANDO USAR: Delegar tarefas especializadas mantendo controle central
EXEMPLO: Agente de e-commerce que delega para sub-agentes de pedidos, polÃ­tica, etc.

ESTRUTURA n8n:
[Webhook] â†’ [Code Node: limpar HTML] â†’ [AI Node: classificar intenÃ§Ã£o]
                                              â†“
                                    [Switch Node: roteador]
                                    â†™        â†“         â†˜
                           [Sub-Workflow:  [Sub-Workflow:  [Sub-Workflow:
                            Pedidos]        PolÃ­tica]       Outros]
                                    â†˜        â†“         â†™
                              [AI Node: consolidar resposta]

IMPLEMENTAÃ‡ÃƒO:
â†’ "Execute Workflow" node chama sub-workflows especializados
â†’ Cada sub-workflow tem seu prÃ³prio system prompt focado
â†’ Gatekeeper consolida e valida respostas antes de entregar
â†’ Adicionar Human-in-the-Loop no gatekeeper para decisÃµes crÃ­ticas
```

### PadrÃ£o 4 â€” Multi-Agent Teams (DecisÃ£o DistribuÃ­da)
```
QUANDO USAR: Agentes trabalham em paralelo e combinam resultados
EXEMPLO: Research que busca em paralelo em mÃºltiplas fontes

ESTRUTURA n8n:
[Trigger] â†’ [Orchestrator Agent]
                    â†“
         [Split in Batches / Parallel]
         â†™              â†“              â†˜
[Research Agent 1] [Research Agent 2] [Research Agent 3]
         â†˜              â†“              â†™
         [Merge Results / Aggregator]
                    â†“
         [Synthesizer Agent]
                    â†“
         [Output]

DICA CRÃTICA DA PESQUISA:
"Para workflows agenticos complexos, criar um loop com divisÃ£o direta de
responsabilidades funciona muito mais confiÃ¡vel do que pedir para um Ãºnico
agente planejar, executar e inspecionar seu progresso."
```

---

## 2. MCP em n8n: O Poder do Model Context Protocol ðŸ”Œ

O n8n conecta a LLMs, serviÃ§os, fontes de dados, MCP servers e atÃ© outros agentes. Sistemas externos podem chamar workflows do n8n usando o MCP Server Trigger.

### Expor o n8n como MCP Server
```
CONFIGURAÃ‡ÃƒO:
1. Adicionar "MCP Server Trigger" node ao workflow
2. Definir as "ferramentas" expostas (cada sub-workflow vira uma tool)
3. Claude, Cursor, outros agentes podem chamar o n8n via MCP

EXEMPLO PRÃTICO:
MCP Tool "buscar_cliente":
  Input: customer_id (string)
  AÃ§Ã£o: Query no Supabase â†’ retorna dados do cliente
  Output: JSON estruturado

MCP Tool "criar_tarefa":
  Input: titulo, descricao, responsavel, prazo
  AÃ§Ã£o: Criar task no Linear/Notion/Asana
  Output: ID da tarefa criada

RESULTADO: Claude consegue dizer "busca o cliente #123 e cria uma tarefa
para o responsÃ¡vel" â€” e o n8n executa tudo autonomamente.
```

### Consumir MCP Servers no n8n
```
â†’ Adicionar "MCP Client Tool" node ao AI Agent
â†’ Configurar URL do MCP server (Supabase MCP, GitHub MCP, etc.)
â†’ O agente decide automaticamente qual tool usar

DIFERENÃ‡A entre MCP Tool e Sub-Agente:
â†’ MCP: acesso a ferramentas (mais simples, sem system prompt prÃ³prio)
â†’ Sub-Agente: agente completo com system prompt, memÃ³ria e LLM prÃ³prio
â†’ Ã‰ possÃ­vel conectar MCP Client a um sub-agente quando precisa de ambos
```

---

## 3. MemÃ³ria AvanÃ§ada: O Stack de 2026 ðŸ§ 

Em 2026, "automaÃ§Ã£o stateless" estÃ¡ morta. Os workflows precisam de memÃ³ria. n8n suporta nodes de memÃ³ria sofisticados: Redis Chat Memory para tempos de resposta sub-milissegundo em agentes de voz, Postgres/Supabase Vector Memory para RAG de longo prazo, e Simple Window Memory para conversas multi-turn rÃ¡pidas e contextuais.

```javascript
// ESCOLHA DE MEMÃ“RIA POR CASO DE USO:

// 1. CONVERSAS SIMPLES (chat bÃ¡sico, suporte)
// â†’ Simple Window Memory
// ConfiguraÃ§Ã£o: Window Size = 10 (Ãºltimas 10 mensagens)
// PrÃ³s: simples, rÃ¡pido
// Contras: sem persistÃªncia entre sessÃµes

// 2. AGENTES COM HISTÃ“RICO PERSISTENTE
// â†’ Postgres Chat Memory (via Supabase)
// Tabela criada automaticamente: n8n_chat_histories
// PrÃ³s: persiste entre sessÃµes, pode consultar histÃ³rico
// Contras: latÃªncia de banco de dados

// 3. AGENTES COM RAG (consultar documentos)
// â†’ Supabase Vector Memory + pgvector
// Armazena embeddings das conversas
// Permite busca semÃ¢ntica no histÃ³rico
// PrÃ³s: encontra contexto relevante mesmo de conversas antigas
// Contras: custo de embeddings, maior complexidade

// 4. AGENTES DE VOZ (latÃªncia crÃ­tica)
// â†’ Redis Chat Memory
// Sub-milissegundo de latÃªncia
// Compartilhado entre agentes do mesmo usuÃ¡rio
```

---

## 4. Error Handling Profissional âš ï¸

Cada workflow deve ter um Error Trigger â€” um fluxo dedicado que roda sempre que qualquer outro workflow falha. Deve logar o erro em uma Data Table e alertar o time via canal "War Room" no Slack ou Discord.

```javascript
// PADRÃƒO DE ERROR HANDLING NO ANTIGRAVITY:

// 1. ERROR TRIGGER GLOBAL
// Workflow dedicado que captura erros de TODOS os outros
// Nodes:
//   [Error Trigger] â†’ [Set: formatar mensagem] â†’ [Supabase: log] â†’ [Slack: alertar]

// Mensagem padrÃ£o de erro:
{
  "workflow": "{{ $workflow.name }}",
  "error": "{{ $json.error.message }}",
  "timestamp": "{{ $now }}",
  "execution_id": "{{ $executionId }}",
  "node_failed": "{{ $json.error.node.name }}",
  "input_data": "{{ $json.error.context }}"
}

// 2. TRY-CATCH EM WORKFLOWS CRÃTICOS
// Usar "On Error" output dos nodes para tratar falhas localmente
// [Node crÃ­tico] â†’ sucesso â†’ [prÃ³ximo node]
//              â†˜ erro â†’ [node de fallback] â†’ [log + continuar]

// 3. RETRY AUTOMÃTICO
// Configurar em nodes de HTTP/API: Max Tries = 3, Wait = 1000ms
// Para operaÃ§Ãµes idempotentes: usar "Continue on Fail"
// Para operaÃ§Ãµes nÃ£o-idempotentes: NUNCA usar retry sem verificar estado

// 4. REGRA DE OURO
// Use "Execute Workflow" com sub-workflows isolados
// Se um sub-workflow falha, nÃ£o derruba o workflow pai
```

---

## 5. Sub-Workflows: A Arquitetura de Lego ðŸ§±

```
PRINCÃPIO: "Don't build a 50-node 'Mega-Workflow'. It's a nightmare to debug."

ESTRUTURA RECOMENDADA:

/workflows
  /core
    error_handler.json          â†’ captura todos os erros
    data_formatter.json         â†’ formata dados em padrÃ£o comum
    human_approval.json         â†’ fluxo de aprovaÃ§Ã£o humana
  /integrations
    supabase_read.json          â†’ leitura padronizada do Supabase
    supabase_write.json         â†’ escrita com validaÃ§Ã£o e log
    slack_notify.json           â†’ notificaÃ§Ãµes padronizadas
  /agents
    research_agent.json         â†’ pesquisa na web
    code_agent.json             â†’ geraÃ§Ã£o e execuÃ§Ã£o de cÃ³digo
    rag_agent.json              â†’ consulta base de conhecimento
  /products
    [nome_do_produto]/
      main_workflow.json        â†’ orquestrador do produto
      onboarding.json
      billing.json

CHAMAR SUB-WORKFLOW:
[Execute Workflow node]
â†’ Workflow: data_formatter
â†’ Wait for Sub-Workflow: true (aguarda resultado)
â†’ Field: passar dados necessÃ¡rios
```

---

## 6. SeguranÃ§a de ProduÃ§Ã£o ðŸ”

Em 2026, hardcodar API keys em workflows Ã© prÃ¡tica amadora. Integrar o n8n com HashiCorp Vault ou AWS Secrets Manager. O suporte nativo a "External Secrets" do n8n garante que credenciais nunca sÃ£o armazenadas no JSON do workflow.

```bash
# DEPLOY SEGURO DO n8n (self-hosted)

# docker-compose.yml
services:
  n8n:
    image: n8nio/n8n:latest
    environment:
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - N8N_EXTERNAL_SECRETS_BACKEND=vault  # HashiCorp Vault
      - VAULT_ADDR=${VAULT_ADDR}
      - VAULT_TOKEN=${VAULT_TOKEN}
      - N8N_LOG_LEVEL=info
      - N8N_LOG_OUTPUT=file
      - N8N_METRICS=true  # Prometheus metrics endpoint
    volumes:
      - n8n_data:/home/node/.n8n
    networks:
      - internal

# REGRAS DE SEGURANÃ‡A:
# 1. Credenciais NUNCA no JSON do workflow
# 2. Usar variables do n8n ou External Secrets
# 3. Webhook URLs: sempre com autenticaÃ§Ã£o (Header Auth ou Signature)
# 4. Acesso ao n8n: VPN ou IP whitelist â€” NUNCA expor porta diretamente
# 5. Backups automÃ¡ticos dos workflows (git ou S3)
```

---

## 7. PadrÃµes de IntegraÃ§Ã£o com o Stack Antigravity ðŸ”—

```
n8n + SUPABASE (skills 68 + 69):
â†’ Supabase node nativo: leitura/escrita direta
â†’ Supabase Realtime: trigger webhook quando dado muda no banco
â†’ pgvector via HTTP node para busca semÃ¢ntica customizada
â†’ Edge Functions via HTTP node para lÃ³gica complexa

n8n + MCP (skill 40):
â†’ MCP Server Trigger: expÃµe n8n como servidor MCP
â†’ MCP Client Tool: n8n consome servidores MCP externos
â†’ Claude consegue chamar n8n via MCP nativamente

n8n + INTELLIGENCE ROUTER (skill 48):
â†’ Definir model_name como variÃ¡vel no AI Agent node
â†’ Workflow de roteamento decide qual modelo usar antes do AI Agent
â†’ Passar model_name como parÃ¢metro para o AI Agent

n8n + HUMAN DELEGATION (skill 38):
â†’ Human-in-the-Loop node para aprovaÃ§Ãµes
â†’ Webhook de retorno quando humano aprova/rejeita
â†’ Timeout configurable: se nÃ£o aprovado em Xh, escalar ou cancelar

n8n + LONG-HORIZON TASKS (skill 44):
â†’ Schedule trigger para retomar tarefas diariamente
â†’ Salvar state em Supabase entre execuÃ§Ãµes
â†’ Sub-workflow de checkpoint: "onde paramos ontem?"
```

---

## 8. Debugging e Observabilidade ðŸ”

```
FERRAMENTAS DE DEBUGGING:

1. Execution History: ver cada run com dados de entrada/saÃ­da
2. "Pin Data": fixar dados em um node para testar sem refazer tudo
3. "Partial execution": rodar de um node especÃ­fico em diante
4. Expression Tester: testar expressÃµes JavaScript antes de salvar

OBSERVABILIDADE EM PRODUÃ‡ÃƒO:
â†’ n8n expÃµe /metrics endpoint para Prometheus
â†’ Dashboards no Grafana: executions/min, error rate, latÃªncia por workflow
â†’ Alertas: se error_rate > 5% ou latÃªncia > 10s â†’ notificar via Slack
â†’ Log streaming para qualquer aggregator (Datadog, Loki, CloudWatch)

QUERIES ÃšTEIS DE MONITORAMENTO:
// Workflows com maior taxa de erro (Ãºltimas 24h)
SELECT workflow_name, 
       COUNT(*) as total,
       SUM(CASE WHEN status = 'error' THEN 1 ELSE 0 END) as errors,
       ROUND(100.0 * SUM(CASE WHEN status = 'error' THEN 1 ELSE 0 END) / COUNT(*), 2) as error_rate
FROM n8n_executions
WHERE started_at > NOW() - INTERVAL '24 hours'
GROUP BY workflow_name
ORDER BY error_rate DESC;
```

---

## Trigger desta Skill

- **Sempre** ao criar, modificar ou debugar qualquer workflow n8n
- "Como construo este workflow no n8n?"
- "Meu n8n estÃ¡ com erro em..."
- Ao integrar um novo serviÃ§o ao stack
- Ao criar um agente de IA no n8n
- Semanalmente: revisÃ£o de workflows lentos ou com erros frequentes
---