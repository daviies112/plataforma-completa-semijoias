---
name: Supabase Full-Stack Engineer (O Backend Completo do Antigravity)
skill_id: 69
description: O manual definitivo do Supabase como backend de produÃ§Ã£o. Cobre RLS (Row Level Security) para multi-tenancy real, pgvector + hybrid search, Edge Functions em Deno, Auth com mÃºltiplos providers, Realtime para 10K+ conexÃµes, Database Branching, e o MCP server do Supabase lanÃ§ado em 2026.
---

# Supabase Full-Stack Engineer Protocol ðŸ—„ï¸

**Objetivo:** "Um Postgres com superpoderes. O backend que qualquer produto SaaS precisa."

## Por que Esta Era a Segunda Lacuna Mais CrÃ­tica

Supabase evoluiu muito alÃ©m da label "alternativa ao Firebase". Em 2026, Ã© uma plataforma backend de cÃ³digo aberto construÃ­da sobre PostgreSQL, projetada para aplicaÃ§Ãµes AI-nativas, seguras e escalÃ¡veis. O n8n Ã© mencionado em todas as skills como automaÃ§Ã£o. O Supabase Ã© mencionado em todas como banco de dados. Nenhum tinha skill prÃ³pria.

A pesquisa recente confirma: "O desempenho Ã© excelente com tempos de query abaixo de 50ms para buscas indexadas. A autenticaÃ§Ã£o Ã© production-grade. O pgvector para AI embeddings tem desempenho que rivaliza com bancos vetoriais dedicados como Pinecone, mas com a simplicidade de queries SQL."

---

## 1. Row Level Security (RLS): A SeguranÃ§a Real ðŸ”

RLS permite aplicar polÃ­ticas de autorizaÃ§Ã£o diretamente no nÃ­vel do banco de dados. Sem isso, qualquer um com sua chave anon pode ler todos os dados.

```sql
-- PADRÃƒO MULTI-TENANCY COM RLS
-- Contexto: SaaS onde cada usuÃ¡rio sÃ³ vÃª seus prÃ³prios dados

-- 1. HABILITAR RLS NA TABELA (obrigatÃ³rio)
ALTER TABLE projects ENABLE ROW LEVEL SECURITY;

-- 2. POLÃTICA: usuÃ¡rio vÃª apenas seus projetos
CREATE POLICY "users_own_projects" ON projects
  FOR ALL
  USING (auth.uid() = owner_id);

-- 3. POLÃTICA PARA TIMES (multi-usuÃ¡rio no mesmo recurso)
CREATE POLICY "team_members_access" ON projects
  FOR SELECT
  USING (
    owner_id = auth.uid()  -- Ã© o dono
    OR
    EXISTS (  -- ou Ã© membro do time
      SELECT 1 FROM team_members
      WHERE team_members.project_id = projects.id
      AND team_members.user_id = auth.uid()
    )
  );

-- 4. POLÃTICA COM ROLES CUSTOMIZADOS
CREATE ROLE admin_role;
GRANT SELECT, UPDATE ON ALL TABLES IN SCHEMA public TO admin_role;

CREATE POLICY "admins_see_all" ON projects
  FOR ALL
  USING (
    auth.jwt() ->> 'role' = 'admin'  -- role no JWT token
    OR auth.uid() = owner_id
  );

-- ARMADILHA CRÃTICA: RLS em produÃ§Ã£o pode ser lento
-- SEMPRE testar com EXPLAIN ANALYZE:
EXPLAIN ANALYZE
SELECT * FROM projects WHERE true;
-- Se aparecer "Seq Scan" em tabelas grandes: adicionar Ã­ndice no owner_id!

CREATE INDEX idx_projects_owner ON projects(owner_id);
```

**Regra de ouro:** Nunca usar a service_role key no cÃ³digo do cliente. Ela bypassa RLS e dÃ¡ acesso total ao banco.

```javascript
// CORRETO: client-side com chave anon (RLS protege)
const supabase = createClient(url, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)

// ERRADO NO CLIENTE: service_role key bypassa RLS!
// const supabase = createClient(url, process.env.SUPABASE_SERVICE_ROLE_KEY)

// Service role key APENAS em: Edge Functions server-side, n8n server-side
```

---

## 2. pgvector + Hybrid Search: RAG de ProduÃ§Ã£o ðŸ”

Em 2026, hybrid search Ã© considerado best practice. Em vez de depender apenas de similaridade vetorial, os sistemas combinam busca por keyword e busca por embedding â€” mesclando BM25 keyword ranking e ranking de similaridade vetorial para retrieval mais forte sem bancos vetoriais externos.

```sql
-- 1. SCHEMA PARA HYBRID SEARCH (conforme docs oficiais Supabase 2026)
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE documents (
  id     BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  content TEXT NOT NULL,
  
  -- Full-text search (keyword search - BM25)
  fts    TSVECTOR GENERATED ALWAYS AS (to_tsvector('portuguese', content)) STORED,
  
  -- Vector search (semantic search)
  embedding VECTOR(512),  -- 512 dims para text-embedding-3-large truncado
  
  -- Metadados
  metadata JSONB,
  owner_id UUID REFERENCES auth.users(id),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Ãndices obrigatÃ³rios para performance
CREATE INDEX idx_documents_fts ON documents USING GIN(fts);
CREATE INDEX idx_documents_embedding ON documents 
  USING hnsw (embedding vector_cosine_ops);  -- HNSW: melhor recall
  
-- Habilitar RLS nos documentos tambÃ©m!
ALTER TABLE documents ENABLE ROW LEVEL SECURITY;
CREATE POLICY "users_own_documents" ON documents FOR ALL USING (auth.uid() = owner_id);

-- 2. FUNÃ‡ÃƒO DE HYBRID SEARCH (Reciprocal Rank Fusion)
CREATE OR REPLACE FUNCTION hybrid_search(
  query_text TEXT,
  query_embedding VECTOR(512),
  match_count INT DEFAULT 10,
  full_text_weight FLOAT DEFAULT 1.0,
  semantic_weight FLOAT DEFAULT 1.0,
  rrf_k INT DEFAULT 50
)
RETURNS SETOF documents
LANGUAGE SQL
AS $$
WITH full_text AS (
  SELECT id, ROW_NUMBER() OVER (ORDER BY ts_rank(fts, websearch_to_tsquery(query_text)) DESC) AS rank_ix
  FROM documents
  WHERE fts @@ websearch_to_tsquery(query_text)
  ORDER BY rank_ix LIMIT LEAST(match_count, 30) * 2
),
semantic AS (
  SELECT id, ROW_NUMBER() OVER (ORDER BY embedding <#> query_embedding) AS rank_ix
  FROM documents
  ORDER BY rank_ix LIMIT LEAST(match_count, 30) * 2
)
SELECT documents.*
FROM full_text
FULL OUTER JOIN semantic ON full_text.id = semantic.id
JOIN documents ON COALESCE(full_text.id, semantic.id) = documents.id
ORDER BY
  COALESCE(1.0 / (rrf_k + full_text.rank_ix), 0.0) * full_text_weight +
  COALESCE(1.0 / (rrf_k + semantic.rank_ix), 0.0) * semantic_weight DESC
LIMIT match_count;
$$;
```

```typescript
// 3. EDGE FUNCTION: Hybrid Search Endpoint (Deno)
// /supabase/functions/search/index.ts

import { createClient } from 'npm:@supabase/supabase-js@2'
import OpenAI from 'npm:openai'

const supabaseUrl = Deno.env.get('SUPABASE_URL')!
const supabaseKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
const openaiKey = Deno.env.get('OPENAI_API_KEY')!

Deno.serve(async (req) => {
  const { query, userId } = await req.json()
  
  // Gerar embedding da query
  const openai = new OpenAI({ apiKey: openaiKey })
  const { data: [{ embedding }] } = await openai.embeddings.create({
    model: 'text-embedding-3-large',
    input: query,
    dimensions: 512  // truncar para economizar espaÃ§o
  })
  
  // Chamar hybrid search com RLS do usuÃ¡rio
  const supabase = createClient(supabaseUrl, supabaseKey)
  const { data: documents } = await supabase
    .rpc('hybrid_search', {
      query_text: query,
      query_embedding: embedding,
      match_count: 10
    })
    .eq('owner_id', userId)  // garantia extra alÃ©m do RLS
  
  return new Response(JSON.stringify(documents), {
    headers: { 'Content-Type': 'application/json' }
  })
})
```

---

## 3. Auth: Multi-Provider Production-Grade ðŸ”‘

```typescript
// PADRÃ•ES DE AUTH DO ANTIGRAVITY:

// 1. Magic Link (recomendado para B2B SaaS)
const { error } = await supabase.auth.signInWithOtp({
  email: 'user@example.com',
  options: {
    emailRedirectTo: `${window.location.origin}/auth/callback`,
  }
})

// 2. OAuth (Google, GitHub, etc.)
const { error } = await supabase.auth.signInWithOAuth({
  provider: 'google',
  options: {
    redirectTo: `${window.location.origin}/auth/callback`,
    scopes: 'email profile',
  }
})

// 3. SSO para Enterprise (SAML)
// Configurar via Supabase Dashboard > Authentication > SSO Providers

// 4. JWT CUSTOMIZADO (quando auth estÃ¡ fora do Supabase)
// Ãštil para: migraÃ§Ã£o de outro sistema, auth corporativo
import { createClient } from '@supabase/supabase-js'
import jwt from 'jsonwebtoken'

const customToken = jwt.sign(
  { 
    sub: userId,
    role: 'authenticated',
    user_metadata: { plan: 'pro' }
  },
  process.env.SUPABASE_JWT_SECRET!,
  { expiresIn: '1h' }
)

const supabase = createClient(url, anonKey, {
  global: { headers: { Authorization: `Bearer ${customToken}` } }
})

// 5. ROLES NO JWT (para controle granular)
// Adicionar metadata no sign-up:
const { data } = await supabase.auth.signUp({
  email, password,
  options: {
    data: { 
      role: 'admin',
      organization_id: 'org_123'
    }
  }
})
// Acessar no RLS: auth.jwt() -> 'user_metadata' -> 'role'
```

---

## 4. Realtime: 10K+ ConexÃµes SimultÃ¢neas âš¡

```typescript
// SUBSCRIBE A MUDANÃ‡AS NO BANCO EM TEMPO REAL

// 1. SUBSCRIBE A UMA TABELA ESPECÃFICA
const channel = supabase
  .channel('orders-realtime')
  .on(
    'postgres_changes',
    { 
      event: 'INSERT',     // INSERT | UPDATE | DELETE | *
      schema: 'public', 
      table: 'orders',
      filter: `status=eq.pending`  // filtrar no servidor = eficiente
    },
    (payload) => {
      console.log('Novo pedido pendente:', payload.new)
      updateDashboard(payload.new)
    }
  )
  .subscribe((status) => {
    if (status === 'SUBSCRIBED') console.log('Conectado ao Realtime')
  })

// 2. BROADCAST (mensagens entre clientes sem banco)
const broadcastChannel = supabase.channel('room-1')
broadcastChannel
  .on('broadcast', { event: 'cursor_moved' }, ({ payload }) => {
    updateCursor(payload.userId, payload.x, payload.y)
  })
  .subscribe()

// Enviar evento de broadcast
broadcastChannel.send({
  type: 'broadcast',
  event: 'cursor_moved',
  payload: { userId: myId, x: cursorX, y: cursorY }
})

// 3. PRESENCE (quem estÃ¡ online agora)
const presenceChannel = supabase.channel('online-users')
presenceChannel
  .on('presence', { event: 'sync' }, () => {
    const onlineUsers = presenceChannel.presenceState()
    updateOnlineList(Object.values(onlineUsers).flat())
  })
  .subscribe(async (status) => {
    if (status === 'SUBSCRIBED') {
      await presenceChannel.track({ user: currentUser, online_at: new Date() })
    }
  })

// ARMADILHA: NÃ£o fazer subscribe sem filtros em tabelas grandes!
// .on('postgres_changes', { event: '*', schema: 'public', table: 'big_table' })
// Isso vai sobrecarregar conexÃµes. SEMPRE usar filter quando possÃ­vel.
```

---

## 5. Edge Functions: LÃ³gica Server-Side em Deno ðŸŒ

```typescript
// TEMPLATE DE EDGE FUNCTION (Deno - nÃ£o Node.js!)
// Deploy: supabase functions deploy nome-da-funcao

import { createClient } from 'npm:@supabase/supabase-js@2'
import { corsHeaders } from '../_shared/cors.ts'

// _shared/cors.ts (reutilizar entre functions)
export const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

Deno.serve(async (req) => {
  // Handle CORS preflight
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }

  try {
    const { data: requestData } = await req.json()
    
    // SEMPRE usar service_role em Edge Functions (server-side)
    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
    )
    
    // Verificar auth do usuÃ¡rio que chamou
    const authHeader = req.headers.get('Authorization')!
    const { data: { user } } = await supabase.auth.getUser(
      authHeader.replace('Bearer ', '')
    )
    if (!user) throw new Error('Unauthorized')
    
    // LÃ³gica da funÃ§Ã£o
    const result = await processRequest(requestData, user)
    
    return new Response(JSON.stringify({ data: result }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      status: 200
    })
    
  } catch (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      status: 400
    })
  }
})

// OTIMIZAÃ‡Ã•ES PARA COLD START:
// â†’ Evitar imports pesados no top-level (importar dentro da funÃ§Ã£o se possÃ­vel)
// â†’ Usar design modular
// â†’ Keep-alive: fazer requests regulares para manter a function "quente"
```

---

## 6. Database Branching: Dev Seguro sem Tocar ProduÃ§Ã£o ðŸŒ¿

```bash
# Database Branching: um dos recursos mais estratÃ©gicos do Supabase
# Reduz risco de migraÃ§Ã£o, times podem iterar sem comprometer produÃ§Ã£o

# 1. CRIAR BRANCH (Supabase CLI)
supabase branches create feature/nova-funcionalidade

# 2. WORK ON BRANCH
supabase db push --db-url $BRANCH_DB_URL  # aplicar migrations na branch

# 3. TESTAR (branch tem URL prÃ³pria para dev)
SUPABASE_URL=$BRANCH_URL  # usar no .env.local de desenvolvimento

# 4. MERGE PARA PRODUÃ‡ÃƒO
supabase branches merge feature/nova-funcionalidade
# â†’ Supabase detecta conflitos antes de aplicar em produÃ§Ã£o

# FLUXO RECOMENDADO:
# main â†’ produÃ§Ã£o
# develop â†’ staging (branch)
# feature/* â†’ dev local ou branch temporÃ¡ria

# MIGRATIONS CORRETAS:
# SEMPRE: supabase migration new nome_descritivo
# NUNCA: editar schema direto na produÃ§Ã£o sem migration
# SEMPRE: rodar em staging antes de produÃ§Ã£o
```

---

## 7. Stripe Sync Engine + MCP Server ðŸ’³

```sql
-- STRIPE SYNC ENGINE (feature de 2026 â€” integraÃ§Ã£o one-click no dashboard)
-- ApÃ³s habilitar: dados do Stripe ficam disponÃ­veis como tabelas SQL

-- Consultar clientes do Stripe com SQL nativo:
SELECT 
  c.id as customer_id,
  c.email,
  c.name,
  s.status as subscription_status,
  s.current_period_end,
  p.unit_amount / 100.0 as price_usd
FROM stripe.customers c
JOIN stripe.subscriptions s ON s.customer = c.id
JOIN stripe.prices p ON p.id = s.price
WHERE s.status = 'active'
ORDER BY s.current_period_end;

-- Combinar dados do Stripe com dados da app:
SELECT 
  u.email,
  u.created_at as user_since,
  sc.name as plan_name,
  COUNT(o.id) as total_orders
FROM auth.users u
JOIN stripe.customers sc ON sc.email = u.email
JOIN orders o ON o.user_id = u.id
GROUP BY u.email, u.created_at, sc.name;
```

```javascript
// SUPABASE MCP SERVER (lanÃ§ado em 2026)
// Conectar Claude ao Supabase via MCP:
// "Conecte seus projetos Supabase ao Claude e gerencie seu banco dizendo ao Claude o que precisa"

// ConfiguraÃ§Ã£o no Claude MCP settings:
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["@supabase/mcp-server", "--project-ref", "seu-project-ref"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "seu-token"
      }
    }
  }
}

// Com isso, Claude pode:
// "Crie uma tabela de usuÃ¡rios com RLS habilitado"
// "Mostre as queries mais lentas do banco"
// "Adicione um Ã­ndice na coluna email da tabela users"
```

---

## 8. Checklist de ProduÃ§Ã£o âœ…

```markdown
## Supabase Production Checklist

### SeguranÃ§a (CRÃTICO)
â–¡ RLS habilitado em TODAS as tabelas com dados de usuÃ¡rio
â–¡ service_role key NUNCA no cÃ³digo client-side
â–¡ anon key configurada (segura porque RLS protege)
â–¡ PolÃ­ticas testadas como usuÃ¡rio diferente (verificar vazamento de dados)
â–¡ Backups configurados (mÃ­nimo 7 dias, melhor 30 dias)

### Performance
â–¡ Ãndices em colunas de FK (owner_id, user_id, etc.)
â–¡ Ãndice HNSW em colunas de embedding (vector)
â–¡ Ãndice GIN em colunas tsvector (full-text search)
â–¡ EXPLAIN ANALYZE nas queries crÃ­ticas (sem Seq Scan em tabelas grandes)
â–¡ Connection pooling configurado (PgBouncer para alta carga)

### Realtime
â–¡ Subscriptions com filtros (nÃ£o escutar tabela inteira)
â–¡ NÃ£o usar Realtime em tabelas que tÃªm muitas escritas/segundo
â–¡ Monitorar nÃºmero de conexÃµes simultÃ¢neas

### Migrations
â–¡ Ambientes separados: local, staging (branch), produÃ§Ã£o
â–¡ Toda mudanÃ§a de schema via migration (nunca manual em produÃ§Ã£o)
â–¡ Migrations testadas em staging antes da produÃ§Ã£o
â–¡ Rollback planejado para cada migration crÃ­tica

### Monitoramento
â–¡ Query performance dashboard ativado
â–¡ Logs sendo exportados para sistema externo
â–¡ Alertas para conexÃµes perto do limite
â–¡ Alertas para disco > 80%
```

---

## Trigger desta Skill

- **Sempre** ao trabalhar com o banco de dados do projeto
- "Como configuro RLS para..."
- "Como implemento busca semÃ¢ntica no Supabase?"
- "Minha query estÃ¡ lenta..."
- Ao criar novo projeto ou feature com backend
- Ao integrar Stripe com o produto
- Ao configurar autenticaÃ§Ã£o
---