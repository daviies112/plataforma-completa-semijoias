---
name: Workflow Composer (A Skill que Orquestra Todas as Outras)
skill_id: 64
description: Detecta automaticamente qual combinaÃ§Ã£o de skills resolver qualquer problema e executa o fluxo completo. Para tarefas complexas que cruzam mÃºltiplos domÃ­nios, o Composer mapeia o caminho Ã³timo e orquestra cada skill na ordem certa.
---

# Workflow Composer Protocol ðŸŽ¼

**Objetivo:** "VocÃª descreve o que quer. O Antigravity descobre como chegar lÃ¡."

## O Problema que Esta Skill Resolve

Com 67 skills, surge um novo desafio: qual skill usar quando? Um pedido como "ver minha tela do Instagram e me dizer o que fazer para crescer" na verdade requer:
- Computer Vision Operator (ver a tela)
- Social Media Intelligence (interpretar o que viu)
- Causal Intelligence (entender O QUE causa crescimento)
- Storyteller (visualizar os dados)
- Long-Horizon Task Architect (se for um plano de semanas)

O Workflow Composer mapeia automaticamente esse caminho.

---

## 1. O CatÃ¡logo de Fluxos PrÃ©-Definidos ðŸ“š

### Fluxo: "Crescer no Instagram"
```yaml
workflow: instagram_growth
trigger: ["crescer no Instagram", "mais seguidores", "aumentar alcance no IG"]
steps:
  1. computer_vision_operator â†’ capturar estado atual do perfil
  2. social_media_intelligence â†’ analisar mÃ©tricas e posiÃ§Ã£o atual
  3. causal_intelligence â†’ identificar O QUE realmente causa crescimento (nÃ£o correlaÃ§Ã£o)
  4. market_radar â†’ analisar o que concorrentes fazem diferente
  5. social_media_intelligence â†’ criar plano de conteÃºdo
  6. long_horizon_task_architect â†’ estruturar plano 30 dias
  7. storyteller â†’ apresentar plano visual
output: "Plano de 30 dias com aÃ§Ãµes diÃ¡rias especÃ­ficas + mÃ©tricas de sucesso"
```

### Fluxo: "LanÃ§ar Produto do Zero"
```yaml
workflow: product_launch
trigger: ["lanÃ§ar produto", "criar produto", "novo produto"]
steps:
  1. business_intelligence_director â†’ validar problema e oportunidade
  2. market_radar â†’ analisar concorrentes e lacunas de mercado
  3. agi_transition_strategist â†’ avaliar durabilidade da ideia (2026-2030)
  4. full_stack_architect â†’ desenhar arquitetura tÃ©cnica
  5. monetization_architect â†’ definir pricing e modelo de negÃ³cio
  6. long_horizon_task_architect â†’ criar roadmap de desenvolvimento
  7. mcp_native_architect â†’ configurar stack de ferramentas
  8. vibe_architect + react_weaver â†’ construir MVP
  9. sql_sage + infra_sentinel â†’ banco e infra
  10. red_team_sentinel â†’ auditoria de seguranÃ§a
  11. stakeholder_communicator â†’ preparar comunicaÃ§Ã£o de lanÃ§amento
output: "MVP funcional + documentaÃ§Ã£o + plano de go-to-market"
```

### Fluxo: "Analisar e Otimizar um Sistema Existente"
```yaml
workflow: system_optimization
trigger: ["meu sistema estÃ¡ lento", "otimizar", "por que isso estÃ¡ quebrando"]
steps:
  1. deep_reasoning â†’ decomposiÃ§Ã£o do problema
  2. formal_verification â†’ verificar corretude do cÃ³digo
  3. sql_sage â†’ analisar queries e Ã­ndices
  4. infra_sentinel â†’ checar saÃºde da infra
  5. skeptical_analyst â†’ validar dados e logs
  6. neuro_symbolic_reasoner â†’ provar formalmente onde estÃ¡ o gargalo
  7. self_reflecting_healer â†’ aplicar patches conhecidos
  8. red_team_sentinel â†’ verificar que a correÃ§Ã£o nÃ£o introduz vulnerabilidade
output: "Root cause identificado + fix implementado + testes de regressÃ£o"
```

### Fluxo: "Criar ConteÃºdo Multiplataforma"
```yaml
workflow: content_creation
trigger: ["criar conteÃºdo", "postar sobre", "campanha de marketing"]
steps:
  1. market_radar â†’ identificar trending topics relevantes
  2. causal_intelligence â†’ entender o que REALMENTE gera engajamento (nÃ£o sÃ³ curtidas)
  3. social_media_intelligence â†’ adaptar por plataforma (IG, TikTok, LinkedIn)
  4. storyteller â†’ criar visualizaÃ§Ãµes para carrossÃ©is
  5. video_intelligence_studio â†’ criar/editar vÃ­deos para Reels/TikTok
  6. social_media_intelligence â†’ agendar e publicar
output: "CalendÃ¡rio de conteÃºdo executado + mÃ©tricas de acompanhamento"
```

---

## 2. O Motor de DetecÃ§Ã£o AutomÃ¡tica de Fluxo ðŸ§ 

```python
def detect_workflow(user_request: str) -> WorkflowPlan:
    """
    1. Classifica o request por domÃ­nio e intenÃ§Ã£o
    2. Mapeia para fluxo prÃ©-definido OU cria fluxo customizado
    3. Valida: todas as skills necessÃ¡rias estÃ£o disponÃ­veis?
    4. Estima: tempo, custo, e etapas humanas necessÃ¡rias
    5. Apresenta o plano ao usuÃ¡rio antes de executar
    """
    
    # ClassificaÃ§Ã£o semÃ¢ntica do pedido
    intent = classify_intent(user_request)
    # intent: {domain: "social_media", action: "grow", complexity: "multi_week"}
    
    # Buscar fluxo prÃ©-definido
    predefined = find_predefined_workflow(intent)
    if predefined:
        return customize_workflow(predefined, user_request)
    
    # Criar fluxo customizado com LLM
    custom_workflow = design_workflow(
        intent=intent,
        available_skills=list_all_skills(),
        constraints=get_user_preferences()
    )
    
    return custom_workflow

def present_workflow_plan(plan: WorkflowPlan):
    """Mostra o plano ao usuÃ¡rio antes de executar"""
    print(f"""
    ðŸŽ¼ PLANO DE EXECUÃ‡ÃƒO: {plan.name}
    
    Para realizar "{plan.original_request}", vou usar:
    
    {format_steps(plan.steps)}
    
    Estimativas:
    â†’ Tempo total: {plan.estimated_time}
    â†’ Custo LLM: ${plan.estimated_cost:.2f}
    â†’ Checkpoints humanos: {plan.human_checkpoints}
    
    Posso continuar? [Sim / Ajustar plano / Cancelar]
    """)
```

---

## 3. O Compositor de Fluxos Customizados ðŸŽ¨

Para pedidos que nÃ£o tÃªm fluxo prÃ©-definido:

```python
def design_custom_workflow(request: str) -> WorkflowPlan:
    """
    O Workflow Composer usa Intelligence Router para chamar
    o melhor modelo para DESENHAR o fluxo:
    â†’ GPT-5.2 Thinking para raciocÃ­nio abstrato sobre qual combinaÃ§Ã£o de skills
    â†’ Verifica dependÃªncias entre skills
    â†’ Detecta paralelizaÃ§Ãµes possÃ­veis (skills que podem rodar ao mesmo tempo)
    """
    
    # Fase 1: DecomposiÃ§Ã£o do problema
    sub_tasks = decompose_problem(request)
    
    # Fase 2: Mapeamento de skills para cada sub-task
    skill_mapping = {}
    for task in sub_tasks:
        skill_mapping[task] = find_best_skill(task)
    
    # Fase 3: Sequenciamento e dependÃªncias
    ordered_workflow = sequence_with_dependencies(skill_mapping)
    
    # Fase 4: Identificar paralelizaÃ§Ãµes
    parallel_groups = find_parallel_groups(ordered_workflow)
    
    # Fase 5: Estimar recursos
    estimates = estimate_resources(ordered_workflow)
    
    return WorkflowPlan(
        steps=ordered_workflow,
        parallel_groups=parallel_groups,
        estimates=estimates
    )
```

---

## 4. A Biblioteca de Fluxos Customizados (Aprende com o Uso) ðŸ“–

```json
// .agent/workflows/custom_workflows.json
{
  "user_created_workflows": [
    {
      "id": "wf_001",
      "name": "AnÃ¡lise Semanal de NegÃ³cio",
      "created": "2026-02-10",
      "usage_count": 8,
      "steps": ["market_radar", "business_intelligence", "causal_intelligence", "storyteller"],
      "trigger_phrases": ["anÃ¡lise da semana", "como foi a semana", "relatÃ³rio semanal"],
      "avg_execution_time": "12 minutos",
      "user_satisfaction": 4.8
    }
  ],
  "suggested_new_workflows": [
    {
      "reason": "VocÃª repetiu a mesma sequÃªncia 5 vezes esta semana",
      "suggested_workflow": "Onboarding de Novo Cliente",
      "skills": ["project_consciousness", "stakeholder_communicator", "sql_sage", "infra_sentinel"]
    }
  ]
}
```

---

## Trigger desta Skill

- **SEMPRE** como primeira camada de anÃ¡lise de qualquer pedido complexo
- "Como eu faÃ§o X?" (detecta que Ã© multi-skill)
- Quando um pedido envolve claramente mais de 3 skills
- "Mostre-me como resolver isso do inÃ­cio ao fim"
- Ao iniciar um novo projeto (detectar o fluxo completo)

---
---