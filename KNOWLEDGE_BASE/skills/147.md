---
name: Master UI Weaver Protocol (The Factory)
description: Protocol for generating full-stack applications (React/Node/SQL) on the fly to solve user problems. Not just screens, but working software.
---

# Master UI Weaver Protocol ðŸ•¸ï¸ðŸ­

**Objective:** "The Software adapts to the User, not the User to the Software."

## 1. The Blueprint Generator ðŸ“
*   **Input:** User says "I need a dashboard to track my gold stock."
*   **Architect:**
    1.  Define Data Model: `Table: gold_stock (id, weight, purity, price)`.
    2.  Define UI Components: `ChartWidget`, `StockTable`, `AddMaterialForm`.
    3.  Define API Routes: `GET /api/stock`, `POST /api/add`.

## 2. The Code Fabrication (The Loom) ðŸ§¶
*   **Execution:**
    1.  **Database:** Run SQL `CREATE TABLE IF NOT EXISTS gold_stock...` on Supabase.
    2.  **Backend:** Generate Next.js API route `pages/api/stock.ts`.
    3.  **Frontend:** Generate React Page `pages/dashboard/gold.tsx` using Tailwind CSS.
    4.  **Integration:** Connect Frontend `fetch()` to Backend API.

## 3. The Deployment (Live Launch) ðŸš€
*   **Action:**
    1.  Commit to Git (`feature/gold-dashboard`).
    2.  Trigger Vercel Build.
    3.  **Return:** "Here is your live dashboard: `subdomain.antigravity.com/gold`"

## 4. The Self-Destruct (Ephemeral Apps) ðŸ’£
*   **Concept:** "Disposable Software".
*   **Usage:** Used for a specific analysis (e.g., "Black Friday Audit").
*   **Action:** After the event/task is done, archive the code and drop the tables to keep the system lean.
---