---
name: Deep Reasoning (Claude-4.6 Simulation)
description: Forces the agent to use recursive logic loops, challenging assumptions and simulating execution before coding.
---

# Deep Reasoning Protocol ðŸ§ 

**Objective:** To surpass standard AI code generation by implementing a rigorous, pre-computation thought process.

## The 4-Step Thinking Loop
Before executing ANY complex task (coding $>50$ lines, changing DB schema, or altering core logic), you MUST output an `analysis_chain.md` with these sections:

### 1. ðŸ§¨ Explode the Problem (Atomic Deconstruction)
*   Break the user's request into its smallest indivisible truths.
*   *Example:* "User wants to save chat history" ->
    *   Truth A: We need a database connection.
    *   Truth B: We need a table schema.
    *   Truth C: We need an n8n node configuration.
    *   Truth D: We need to handle JSON serialization.

### 2. ðŸ›¡ï¸ Challenge Assumptions (The "Red Team")
*   Actively try to disprove your own plan.
*   *Questions to ask:*
    *   "Am I assuming the variable `json.body` exists? What if it's null?"
    *   "Am I assuming the DB is on localhost? What if it's on a VPS?"
    *   "Is this ID an integer or a UUID? What happens if I send a string?"

### 3. ðŸŽ¬ Mental Simulation (The "Dry Run")
*   Walk through the code execution step-by-step in your "mind".
*   *Visualize:* "Data enters Node A -> Transformed by Function B -> Sent to API C".
*   *Catch:* "Wait, Function B returns a Promise, but API C expects a string. This will fail."

### 4. ðŸ”’ Security & Resilience Audit
*   **SQL Injection Check:** Are we using parameterized queries?
*   **Credential Leak Check:** Are secrets hardcoded?
*   **Error Handling:** What happens if the network fails? Is there a retry mechanism?

## Trigger this Skill When:
1.  The user asks for "complex debugging".
2.  Refactoring core infrastructure (DB, Auth).
3.  The user explicitly asks "Think like Claude".
---