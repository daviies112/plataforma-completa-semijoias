---
name: WhatsApp Business Intelligence (AutomaÃ§Ã£o Profissional via WhatsApp)
skill_id: 74
description: Sistema completo para automatizar WhatsApp Business â€” API oficial, chatbots com IA e RAG, broadcast de mensagens, templates aprovados, integraÃ§Ã£o com n8n e CRM. O canal de comunicaÃ§Ã£o #1 do Brasil finalmente com skill dedicada.
---

# WhatsApp Business Intelligence Protocol ðŸ“±

**Objetivo:** "O WhatsApp nÃ£o Ã© sÃ³ mensagem. Ã‰ o canal de vendas mais poderoso do Brasil."

---

## 1. APIs DisponÃ­veis e Quando Usar

```
OPÃ‡ÃƒO 1: Meta Cloud API (oficial, mais estÃ¡vel)
â†’ Gerenciada pela Meta diretamente
â†’ Requer Business Verification
â†’ Custo: por conversa (nÃ£o por mensagem)
â†’ Ideal para: empresas sÃ©rias com volume mÃ©dio-alto

OPÃ‡ÃƒO 2: WhatsApp Business API (on-premise â€” legado)
â†’ Hospedagem prÃ³pria
â†’ Mais complexo de manter
â†’ Sendo descontinuado gradualmente

OPÃ‡ÃƒO 3: Provedores BSP (Business Solution Providers)
â†’ Twilio, Vonage, Zendesk, Respond.io
â†’ Mais fÃ¡cil de configurar
â†’ Custo adicional, mas suporte melhor
â†’ Ideal para: comeÃ§ar rÃ¡pido sem complexidade tÃ©cnica

PARA O ANTIGRAVITY: Meta Cloud API via n8n (controle mÃ¡ximo)
```

---

## 2. ConfiguraÃ§Ã£o n8n + WhatsApp

```javascript
// WORKFLOW n8n: Receber e responder mensagens do WhatsApp

// 1. WEBHOOK TRIGGER (Meta envia eventos aqui)
// URL: https://seu-n8n.com/webhook/whatsapp
// VerificaÃ§Ã£o: Meta envia GET com hub.verify_token
// Mensagens: Meta envia POST com payload

// 2. VERIFICAÃ‡ÃƒO DO WEBHOOK (necessÃ¡ria na configuraÃ§Ã£o inicial)
// Node: Respond to Webhook
if (req.query['hub.mode'] === 'subscribe' && 
    req.query['hub.verify_token'] === process.env.WHATSAPP_VERIFY_TOKEN) {
  return res.send(req.query['hub.challenge'])
}

// 3. PROCESSAR MENSAGEM RECEBIDA
const message = $json.entry[0].changes[0].value.messages[0]
const from = message.from  // nÃºmero do usuÃ¡rio
const text = message.text?.body || ''
const messageId = message.id

// 4. MARCAR COMO LIDA (boa prÃ¡tica)
await sendWhatsAppAPI('POST', '/messages', {
  messaging_product: 'whatsapp',
  status: 'read',
  message_id: messageId
})

// 5. PROCESSAR COM IA + RAG (conectar ao RAG skill 70)
const ragResponse = await callRAGAgent(text, from)

// 6. RESPONDER
await sendWhatsAppAPI('POST', '/messages', {
  messaging_product: 'whatsapp',
  to: from,
  type: 'text',
  text: { body: ragResponse }
})
```

---

## 3. Templates de Mensagem Aprovados

```javascript
// Templates sÃ£o obrigatÃ³rios para iniciar conversa (nÃ£o responder)
// Precisam ser aprovados pela Meta (geralmente 24-48h)

// CRIAR TEMPLATE VIA API:
const template = {
  name: "confirmacao_pedido",
  language: "pt_BR",
  category: "TRANSACTIONAL",
  components: [
    {
      type: "HEADER",
      format: "TEXT",
      text: "Pedido Confirmado! ðŸŽ‰"
    },
    {
      type: "BODY",
      text: "OlÃ¡ {{1}}! Seu pedido #{{2}} foi confirmado.\nValor: R$ {{3}}\nPrevisÃ£o de entrega: {{4}}",
      example: {
        body_text: [["JoÃ£o", "12345", "99,90", "20/02/2026"]]
      }
    },
    {
      type: "FOOTER",
      text: "Antigravity Commerce"
    },
    {
      type: "BUTTONS",
      buttons: [
        {
          type: "URL",
          text: "Rastrear Pedido",
          url: "https://antigravity.app/pedido/{{1}}"
        }
      ]
    }
  ]
}

// ENVIAR TEMPLATE:
await sendWhatsAppAPI('POST', '/messages', {
  messaging_product: 'whatsapp',
  to: customerPhone,
  type: 'template',
  template: {
    name: 'confirmacao_pedido',
    language: { code: 'pt_BR' },
    components: [
      {
        type: 'body',
        parameters: [
          { type: 'text', text: customer.name },
          { type: 'text', text: order.id },
          { type: 'text', text: order.total },
          { type: 'text', text: order.delivery_date }
        ]
      }
    ]
  }
})
```

---

## 4. Chatbot WhatsApp com IA e RAG

```
ARQUITETURA DO CHATBOT:

[UsuÃ¡rio envia mensagem no WhatsApp]
â†“
[Meta Cloud API â†’ Webhook n8n]
â†“
[n8n: identificar usuÃ¡rio e contexto]
â†“
[Supabase: buscar histÃ³rico de conversa]
â†“
[RAG: buscar contexto relevante da base de conhecimento]
â†“
[AI Agent (Claude/GPT via Intelligence Router)]
  - System prompt com identidade do chatbot
  - HistÃ³rico da conversa (Ãºltimas 10 mensagens)
  - Contexto RAG (documentos relevantes)
â†“
[Verificar: precisa de humano? (escalaÃ§Ã£o)]
â†“
[Enviar resposta via WhatsApp API]
â†“
[Supabase: salvar na conversa]

ESCALAÃ‡ÃƒO PARA HUMANO:
â†’ UsuÃ¡rio pede explicitamente ("falar com atendente")
â†’ IntenÃ§Ã£o de compra detectada (webhook para CRM)
â†’ ReclamaÃ§Ã£o grave (alerta imediato para time)
â†’ Pergunta que o bot nÃ£o conseguiu responder 3x
```

---

## Trigger desta Skill

- "Quero criar um chatbot no WhatsApp"
- "Como envio mensagens automÃ¡ticas no WhatsApp?"
- Campanha de marketing via WhatsApp
- NotificaÃ§Ãµes transacionais via WhatsApp (pedidos, lembretes)
- Atendimento ao cliente automatizado

---
---