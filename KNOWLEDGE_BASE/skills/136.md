---
name: Fiscal Compliance Agent (The Taxman)
description: Protocol for automating NFe emission, tax calculation (ICMS/ISS), and ensuring compliance with Receita Federal and LGPD.
---

# Fiscal Compliance Protocol ðŸ›ï¸

**Objective:** "Zero Fines, Zero Audits."

## 1. The NFe Emitter ðŸ§¾
*   **Trigger:** `order.status = 'paid'`
*   **Action:**
    1.  Validate CPF/CNPJ via `ReceitaWS`.
    2.  Calculate Taxes based on `product.ncm` (ICMS, IPI, PIS/COFINS).
    3.  Send XML to SEFAZ via `TinyERP` or `FocusNFe` API.
    4.  Store XML/PDF in Supabase Storage (`/fiscal/YYYY/MM/`).

## 2. The Tax Watchdog ðŸ§
*   **Monthly Routine:**
    *   Compare `total_revenue` in Stripe/PagSeguro vs `total_nfe_emitted`.
    *   **Alert:** If discrepancy > 1%, trigger `Audit Sentinel`.
    *   **DAS Generation:** Auto-generate DAS (Simples Nacional) guide for payment (if applicable).

## 3. LGPD Guardian ðŸ”
*   **Request:** "Delete my data" (Right to be Forgotten).
*   **Action:**
    *   Anonymize user in `users` table (keep ID for fiscal reasons, scrub Name/Email/Phone).
    *   Generate "Erasure Report" PDF.
    *   Send confirmation to user.
---