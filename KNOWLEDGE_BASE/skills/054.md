---
name: Accessibility & Inclusive Design (Produtos para Todos)
skill_id: 77
description: WCAG 2.2, screen readers, navegaÃ§Ã£o por teclado, contraste de cores, ARIA labels, e design inclusivo. AlÃ©m de ser Ã©tico, accessibility Ã© exigÃªncia legal em vÃ¡rios paÃ­ses e aumenta SEO. Um produto acessÃ­vel alcanÃ§a 15% mais usuÃ¡rios.
---

# Accessibility & Inclusive Design Protocol â™¿

**Objetivo:** "Construir para todos nÃ£o Ã© limitaÃ§Ã£o. Ã‰ excelÃªncia de design."

---

## 1. Os 4 PrincÃ­pios WCAG 2.2 (POUR)

```
PERCEPTÃVEL: informaÃ§Ã£o deve ser apresentada de forma que usuÃ¡rios possam perceber
OPERÃVEL: componentes de UI devem ser operÃ¡veis por todos
COMPREENSÃVEL: informaÃ§Ã£o e operaÃ§Ã£o da UI devem ser compreensÃ­veis
ROBUSTO: conteÃºdo deve ser interpretado por assistive technologies

NÃVEIS:
A: mÃ­nimo (legal em alguns paÃ­ses)
AA: padrÃ£o de mercado (meta para todo produto)
AAA: excelÃªncia (para produtos especializados)

LEGAL:
â†’ Brasil: Lei Brasileira de InclusÃ£o (LBI) exige acessibilidade digital
â†’ UE: European Accessibility Act (EAA) vigente desde 2025
â†’ EUA: ADA Section 508
â†’ Risco de processo: empresas foram multadas por sites inacessÃ­veis
```

---

## 2. Contraste de Cores (WCAG AA)

```javascript
// REGRA: contraste mÃ­nimo de 4.5:1 para texto normal, 3:1 para texto grande

// Verificar contraste programaticamente:
function getContrastRatio(color1: string, color2: string): number {
  const lum1 = getRelativeLuminance(color1)
  const lum2 = getRelativeLuminance(color2)
  const lighter = Math.max(lum1, lum2)
  const darker = Math.min(lum1, lum2)
  return (lighter + 0.05) / (darker + 0.05)
}

// Exemplos de combinaÃ§Ãµes:
// âœ… #000000 (preto) em #FFFFFF (branco): 21:1 â€” excelente
// âœ… #1a1a1a em #f5f5f5: 14.7:1 â€” Ã³timo
// âš ï¸ #767676 em #FFFFFF: 4.48:1 â€” passa AA mas nÃ£o AAA
// âŒ #999999 em #FFFFFF: 2.85:1 â€” FALHA em AA

// FERRAMENTA: color contrast checker no Figma ou
// https://webaim.org/resources/contrastchecker/

// Tailwind classes seguras para texto:
// text-gray-900 em bg-white: âœ…
// text-gray-600 em bg-white: âœ…
// text-gray-400 em bg-white: âŒ (sÃ³ 4.09:1)
```

---

## 3. Componentes React AcessÃ­veis

```tsx
// FORMULÃRIO ACESSÃVEL
function AccessibleForm() {
  return (
    <form noValidate>
      {/* 1. Sempre associar label ao input via htmlFor */}
      <div>
        <label htmlFor="email" id="email-label">
          Email <span aria-hidden="true">*</span>
          <span className="sr-only">(obrigatÃ³rio)</span>
        </label>
        <input
          id="email"
          type="email"
          aria-labelledby="email-label"
          aria-describedby="email-hint email-error"
          aria-required="true"
          aria-invalid={hasError ? "true" : "false"}
          autoComplete="email"
        />
        <p id="email-hint" className="text-sm text-gray-600">
          Usamos apenas para enviar sua senha
        </p>
        {hasError && (
          <p id="email-error" role="alert" className="text-red-600">
            Por favor, insira um email vÃ¡lido
          </p>
        )}
      </div>
      
      {/* 2. BotÃµes sempre com texto descritivo */}
      <button type="submit" aria-busy={isLoading}>
        {isLoading ? (
          <>
            <span aria-hidden="true">â³</span>
            <span>Enviando...</span>
          </>
        ) : (
          "Criar conta"
        )}
      </button>
    </form>
  )
}

// MODAL ACESSÃVEL
function AccessibleModal({ isOpen, onClose, title, children }) {
  const titleId = useId()
  
  // Trap focus dentro do modal
  useEffect(() => {
    if (isOpen) {
      const modal = document.getElementById('modal')
      const focusable = modal?.querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])')
      const firstFocusable = focusable?.[0] as HTMLElement
      firstFocusable?.focus()
    }
  }, [isOpen])
  
  return (
    <div
      role="dialog"
      aria-modal="true"
      aria-labelledby={titleId}
      id="modal"
    >
      <h2 id={titleId}>{title}</h2>
      {children}
      <button onClick={onClose} aria-label="Fechar modal">Ã—</button>
    </div>
  )
}
```

---

## 4. NavegaÃ§Ã£o por Teclado

```css
/* NUNCA remover o focus outline sem substituir */

/* âŒ ERRADO â€” remove visibilidade do foco para todos */
* { outline: none; }
*:focus { outline: none; }

/* âœ… CORRETO â€” melhorar sem remover */
:focus-visible {
  outline: 2px solid #4F46E5;  /* cor de brand com contraste */
  outline-offset: 2px;
  border-radius: 4px;
}

/* Skip link â€” para pular navegaÃ§Ã£o e ir ao conteÃºdo principal */
.skip-link {
  position: absolute;
  top: -100%;
  left: 0;
  padding: 8px 16px;
  background: #4F46E5;
  color: white;
  z-index: 9999;
  transition: top 0.1s;
}

.skip-link:focus {
  top: 0;  /* aparece quando recebe foco via Tab */
}
```

```html
<!-- PRIMEIRO elemento do body sempre: -->
<a href="#main-content" class="skip-link">Pular para o conteÃºdo principal</a>

<!-- Target do skip link: -->
<main id="main-content" tabindex="-1">
  <!-- conteÃºdo principal -->
</main>
```

---

## 5. Checklist de Accessibility antes de Deploy

```markdown
## Accessibility Checklist â€” [Feature/Page]

### AutomÃ¡tico (rodar com Axe DevTools ou Lighthouse)
â–¡ Zero erros crÃ­ticos (Axe)
â–¡ Score accessibility > 90 (Lighthouse)

### Visual
â–¡ Contraste de texto â‰¥ 4.5:1 (texto normal), â‰¥ 3:1 (texto grande/negrito)
â–¡ InformaÃ§Ã£o nÃ£o transmitida apenas por cor (ex: erro = vermelho + Ã­cone + texto)
â–¡ Tamanho de fonte mÃ­nimo 16px para corpo de texto
â–¡ NÃ£o depender de hover para funcionalidade crÃ­tica

### Teclado
â–¡ Todos os interativos alcanÃ§Ã¡veis via Tab
â–¡ Ordem lÃ³gica do foco (segue fluxo visual)
â–¡ Skip link presente
â–¡ Nenhuma "armadilha de teclado" (modal fecha com Esc)

### Screen Reader
â–¡ Imagens tÃªm alt text descritivo (decorativas: alt="")
â–¡ Ãcones sozinhos tÃªm aria-label
â–¡ FormulÃ¡rios: labels associados a inputs
â–¡ Erros de validaÃ§Ã£o anunciados (aria-live ou role="alert")
â–¡ Estrutura de headings lÃ³gica (H1 â†’ H2 â†’ H3, sem pular)

### ConteÃºdo
â–¡ Linguagem do documento definida (lang="pt-BR")
â–¡ Textos de link descritivos ("Saiba mais sobre acessibilidade" vs "Clique aqui")
â–¡ Tabelas com headers (th) e caption quando necessÃ¡rio
```

---

## Trigger desta Skill

- Ao criar qualquer componente de UI
- Review de PR que inclui UI
- Antes de qualquer lanÃ§amento pÃºblico
- "Meu produto precisa ser acessÃ­vel por lei?"
- Ao receber reclamaÃ§Ã£o de usuÃ¡rio com deficiÃªncia
- Auditoria trimestral de acessibilidade
---