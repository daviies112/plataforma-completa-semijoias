---
name: Self-Evolving Code Protocol (The Darwin Engine)
description: Protocol for Recursive Self-Improvement. The system analyzes its own execution logs, identifies inefficiencies, and rewrites its own code/prompts to be better next time.
---

# Self-Evolving Code Protocol ðŸ§¬

**Objective:** "Code that writes itself."

## 1. The Feedback Loop (Metacognition) ðŸ§ 
*   **Trigger:** Every 24h or after a "Failed Task".
*   **Input:** `execution_logs` (Success/Fail status, Tokens Used, User Corrections).
*   **Analysis:**
    *   "Why did I fail to sell to Client X?"
    *   "Why did this query cost $0.50 instead of $0.05?"

## 2. The Mutation Engine (Code Rewrite) ðŸ§¬
*   **Action:**
    1.  **Draft:** Create a new version of the specific skill file (e.g., `sales_agent_v2.md`).
    2.  **Optimize:** Remove unsuccessful prompts, add new winning arguments found in successful chats.
    3.  **Refactor:** If a Python script failed, rewrite the `try/catch` block to handle the specific error encountered.

## 3. The Natural Selection (A/B Testing) âš–ï¸
*   **Deployment:**
    *   Run `v2` on 10% of requests (Canary Deployment).
    *   Compare KPI (Conversion Rate, Speed, Cost) vs `v1`.
*   **Survival:**
    *   If `v2 > v1`: **Overwrite** the original file. The system just evolved.
    *   If `v2 < v1`: Discard `v2`, log "Failed Mutation", and try a different approach.

## 4. Safety Lock ðŸ”’
*   **Rule:** The Evolution Agent CANNOT rewrite the `Constitution` (Shielded Functions) or the `Master Orchestrator` core logic without Human Approval.
---