---
name: A2A Protocol Commander (Agent-to-Agent Orchestration)
description: Permite que o Antigravity se comunique e coordene com agentes de qualquer outro vendor ou plataforma usando o protocolo padrÃ£o Google A2A. O salto de "time de skills" para "exÃ©rcito de agentes".
---

# A2A Protocol Commander Protocol ðŸ¤

**Objetivo:** "Um Antigravity, mil agentes especializados trabalhando juntos."

## Contexto: O que A2A muda

O Multi-Agent Orchestrator original (skill 6) simula personas dentro do mesmo contexto. A2A Ã© diferente: sÃ£o agentes reais, independentes, potencialmente de outros vendors, com seus prÃ³prios modelos, ferramentas e memÃ³ria â€” comunicando-se via protocolo padronizado.

**Analogia:** O Multi-Agent Orchestrator Ã© uma pessoa interpretando personagens. A2A Ã© uma empresa com departamentos reais que se comunicam via email e reuniÃµes formais.

**AdoÃ§Ã£o em 2026:** Google criou em Abr/2025. 50+ parceiros: Salesforce, SAP, MongoDB, PayPal, Workday, Atlassian, Box, Cohere, ServiceNow.

---

## 1. Como A2A Funciona ðŸ—ï¸

### O Agent Card (CartÃ£o de Visita Digital)
```json
// /.well-known/agent.json â€” identidade pÃºblica do agente
{
  "name": "Antigravity SQL Sage",
  "description": "Especialista em otimizaÃ§Ã£o de banco de dados",
  "version": "2.1",
  "capabilities": {
    "tasks": ["query_optimization", "schema_design", "migration"],
    "input_formats": ["natural_language", "sql", "json_schema"],
    "output_formats": ["sql", "markdown", "json"]
  },
  "endpoint": "https://antigravity.ai/agents/sql-sage",
  "authentication": {
    "type": "oauth2",
    "scopes": ["read:schema", "execute:query"]
  }
}
```

### O Ciclo de Vida de uma Task A2A
```
ESTADO          SIGNIFICADO
submitted   â†’   Recebida, na fila
working     â†’   Sendo executada
input-req   â†’   Precisa de mais info do client
completed   â†’   Terminada com sucesso
failed      â†’   Erro, com mensagem de falha

FLUXO TÃPICO:
[Orchestrator] â†’ POST /tasks {task_id, instruction}
[Agent]        â†’ Response: {status: "submitted"}
[Agent]        â†’ SSE Stream: {status: "working", progress: ...}
[Agent]        â†’ SSE Stream: {status: "completed", result: ...}
```

---

## 2. PadrÃµes de OrquestraÃ§Ã£o ðŸŽ¯

### PadrÃ£o 1: Hub-and-Spoke (mais comum)
```
         [Antigravity Orchestrator]
              /      |       \
     [SQL Agent] [UI Agent] [Security Agent]
```
*Quando usar:* O Antigravity Ã© o maestro. Cada especialista recebe uma sub-task e retorna o resultado.

### PadrÃ£o 2: Pipeline Sequential
```
[Scraper] â†’ [Cleaner] â†’ [Analyzer] â†’ [Reporter]
```
*Quando usar:* Dados fluem por etapas definidas. Cada agente transforma e passa adiante.

### PadrÃ£o 3: Parallel Sprint
```
[Antigravity] 
    â†’ [Agent A: frontend]   â”
    â†’ [Agent B: backend]    â”œâ”€â”€ simultÃ¢neos
    â†’ [Agent C: tests]      â”˜
    â† merge e review
```
*Quando usar:* Tasks independentes que podem rodar em paralelo. Reduz tempo total drasticamente.

### PadrÃ£o 4: Recursive Delegation
```
[Orchestrator] â†’ [Sub-orchestrator] â†’ [Leaf Agents]
```
*Quando usar:* Projetos muito grandes onde sub-times precisam de autonomia.

---

## 3. ImplementaÃ§Ã£o PrÃ¡tica com n8n + Antigravity ðŸ”§

**Como o Antigravity orquestra agentes via A2A no n8n:**

```javascript
// n8n Custom Code Node â€” A2A Task Dispatcher
const { task_type, instruction, priority } = $input.first().json;

// 1. Descobrir agente adequado
const agentRegistry = {
  "sql_optimization": "http://sql-agent:9001",
  "ui_generation": "http://ui-agent:9002",
  "security_audit": "http://security-agent:9003",
  "data_analysis": "http://analyst-agent:9004"
};

const agentUrl = agentRegistry[task_type];

// 2. Criar task A2A
const taskResponse = await fetch(`${agentUrl}/tasks`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${process.env.A2A_SECRET}`
  },
  body: JSON.stringify({
    task_id: crypto.randomUUID(),
    instruction: instruction,
    priority: priority || 'normal',
    callback_url: `${process.env.N8N_WEBHOOK}/task-complete`
  })
});

// 3. Retornar task_id para tracking
return { task_id: taskResponse.task_id, status: 'submitted' };
```

---

## 4. O Agent Card do PrÃ³prio Antigravity ðŸªª

**Expor o Antigravity como um agente A2A para outros sistemas:**

```json
{
  "name": "Antigravity Master Agent",
  "description": "Agente full-stack de alto nÃ­vel com 47 skills especializadas",
  "version": "3.0",
  "capabilities": {
    "tasks": [
      "full_stack_development",
      "data_analysis",
      "system_architecture",
      "autonomous_ops",
      "security_audit",
      "ui_design"
    ],
    "languages": ["pt-BR", "en-US"],
    "models_used": ["claude-sonnet-4-6", "claude-opus-4-6"],
    "max_task_duration": "72h",
    "supports_streaming": true
  },
  "skills": {
    "total": 47,
    "categories": ["engineering", "business", "operations", "ai-frontier"]
  }
}
```

---

## 5. ResoluÃ§Ã£o de Conflitos entre Agentes âš–ï¸

Quando dois agentes retornam resultados conflitantes:

```
PROTOCOLO DE CONFLITO:

1. DETECTAR: "Agent A diz X, Agent B diz Y â€” sÃ£o contraditÃ³rios?"
   
2. CLASSIFICAR:
   â†’ Conflito de dados: verificar qual tem a fonte mais recente
   â†’ Conflito de abordagem: ambos corretos, escolher pela prioridade
   â†’ Conflito real: escalar para humano (Human Delegation Protocol)

3. RESOLVER:
   â†’ Para dados: confidence score + recÃªncia da fonte
   â†’ Para cÃ³digo: rodar testes e escolher o que passa
   â†’ Para arquitetura: @Architect tem prioridade (DB constraints > tudo)

4. DOCUMENTAR: Registrar conflito e resoluÃ§Ã£o no knowledge_graph.json
```

---

## 6. Monitoramento de Agentes Remotos ðŸ“Š

```json
// .agent/memory/active_a2a_tasks.json
{
  "active_tasks": [
    {
      "task_id": "uuid-123",
      "agent": "sql-optimizer",
      "status": "working",
      "started": "2026-02-17T14:00:00",
      "estimated_completion": "2026-02-17T14:15:00",
      "progress": "70%"
    }
  ],
  "completed_today": 47,
  "failed_today": 2,
  "avg_completion_time": "8.3 minutes"
}
```

---

## Trigger desta Skill

- "Quero que vÃ¡rios agentes trabalhem em paralelo"
- Projetos grandes que excedem o contexto de uma sessÃ£o
- Quando o Antigravity precisa integrar com ferramentas de outros vendors
- "Como faÃ§o para o meu agente falar com outros agentes?"
- Setup inicial de arquitetura multi-agente
---