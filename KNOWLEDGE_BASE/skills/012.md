---
name: Evolution API Manager (WhatsApp)
description: Protocol for managing WhatsApp instances, sending messages, and handling webhooks via Evolution API.
---

# Evolution API Manager Protocol ðŸ“±

**Objective:** "Total Control of WhatsApp Communication."

## 1. Instance Management ðŸ¤–
*   **Create Instance:**
    *   **Endpoint:** `POST /instance/create`
    *   **Payload:** `{ "instanceName": "niche_name_01", "qrcode": true }`
    *   **Action:** When scaling to a new niche, automatically provision a new instance.
*   **Status Check:**
    *   **Endpoint:** `GET /instance/fetchInstances`
    *   **Monitor:** Ensure `status: "open"` (connected). If `close`, trigger re-connection alert.

## 2. Messaging Operations ðŸ’¬
*   **Send Text:**
    *   **Endpoint:** `POST /message/sendText/{instance}`
    *   **Payload:** `{ "number": "5511999999999", "text": "Hola!" }`
*   **Send Media:**
    *   **Endpoint:** `POST /message/sendMedia/{instance}`
    *   **Use Case:** Sending the "PDF de PeÃ§as" or "Comprovante".

## 3. Webhook Integration (The Ear) ðŸ‘‚
*   **Config:** Set `webhook_url` to your N8n webhook node (`https://n8n.seudominio.com/webhook/whatsapp`).
*   **Events:** Listen for `messages.upsert` to trigger the AI Agent response logic.

## 4. Safety & Compliance ðŸ›¡ï¸
*   **Rate Limiting:** Max 1 message every 5-10 seconds to avoid ban.
*   **Human Handoff:** If sentiment analysis (via `deep_reasoning`) detects anger, pause the bot and alert a human.
---